# ARC implementation progress

The approved scope and acceptance gates are in [ARC-PLAN.md](ARC-PLAN.md). Check alignment every two implementation work cycles. Public release is not yet verified.

## Current phase

Phase 4 is in progress. Versioned Team Builder authoring, orchestration settings, Git graph execution, native main-composer orchestration and serial ordinary-folder execution now have recorded source/browser/native evidence. The latest directory run passed with three workers, one counted main response, real failure/repair/review, durable approval/restart and independently verified original/candidate inventories. The continuation preflight passed 411 ARC tests and both package typechecks. The subsequently corrected repair-stage ceiling passed all 318 Workflows tests and 114 affected ARC tests. Checkpoints 20, 26 and 32 identify the distinct graph, main-composer and directory gates.

Reviewed instruction-body continuation has native functional and read-only UI evidence with an independent audit; that attempt's missing live-animation observations remain recorded. Reviewed operational-rule continuation now passed its native restart, cumulative-budget, exact-candidate, counted-main and durable live-handoff gates with an independent audit. Contributor-based review grants are enforced at publication, admission and verdict boundaries. The retained Team graph now works beside real worker conversations, with 64 focused passing cases and actual completed-run browser checks at all three target sizes. Remaining native autonomy/delegation variants and combined graph/chat accessibility/performance gates stay open. Independent Phase 5B work has connected project Context through the UI, SDK, CLI and local model, with 151 focused passing tests, integrated typechecks, refreshed packaged inference and two complete Windows application launches. Immutable agent Context binding, document formats, knowledge authority and retrieval quality/scale remain incomplete, followed by Factory, appearance and public release. The current package is unsigned and verified on this developer machine; it does not certify a clean Windows installer. Source WNDR remains separate, BB dark is the default and Workflows is the only scheduler.

## Evidence

| Work                                           | Status                                                    | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------------------------- | --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Current WNDR working-tree snapshot             | Verified                                                  | 6,018 copied regular files, six tracked deletions, zero excluded candidates; content hash and Git status comparison confirmed source unchanged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Source ancestry                                | Recorded                                                  | BB commit `06aeaa994942ae7527dc49d2268c1f801e8542a0`, local `arc/main`; provenance in `ARC-UPSTREAM.json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Existing ARC artwork                           | Preserved                                                 | Original `ARC Graphics` folder retained                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ARC identity and forecasting removal           | Targeted package gate passed                              | Separate application/data/update identity, supplied ARC assets, forecasting plugin and Python packaging removed, default BB dark palette; strict two-launch result below                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Dependency installation                        | Verified                                                  | Pinned pnpm 9.15.0 install passed; process-local Corepack shims keep Turbo descendants on the pinned version                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Desktop/config/process checks                  | Passed                                                    | 9/9 Turbo tasks; desktop 301 passed/3 platform skips, config 110 passed, process utilities 19 passed/9 platform skips; all three typechecks passed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Required Workflows outcomes                    | Passed                                                    | Additive settled outcomes and required-success gates; 232 tests across 14 files and typecheck passed; legacy helpers retained for compatibility                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Provider readiness                             | Observed                                                  | Codex CLI 0.147.0 and Claude Code 2.1.226 report authenticated; this is readiness, not ARC session acceptance                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Agent Studio data, SDK and CLI                 | Targeted checks passed                                    | 34 tests including real SQLite, CLI file transfers and nine UI regressions; latest ARC typecheck passed; `.arc-verification/studio-review-fixes-tests.log`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| First-turn binding                             | Targeted checks passed                                    | Persisted owner-bound context before dispatch; full definitions and missing-owner failure verified; server 54, DB 450, contract 65, SDK 103 and plugin API map 72 tests passed                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Agent Studio browser                           | Passed                                                    | Actual create/edit/save/attach/publish/reload and Assistant navigation; 1366x768, 1920x1080 and 2560x1440, keyboard tabs, reduced motion, no outer overflow or console errors; `.arc-verification/studio-browser-result.json`                                                                                                                                                                                                                                                                                                                                                                                                             |
| Sidebar reload regression                      | Passed                                                    | Restored single panel follows host selection; existing multi-panel behavior retained; 83 tests across SidebarSplitContainer, sidebarSplitLayout and PluginPanelRightPanelHost                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Consolidated typechecks                        | Passed                                                    | 12/12 Turbo tasks across app, server, CLI, desktop, ARC, Automations and Pi; `.arc-verification/consolidated-typecheck.log`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Claude Studio provider acceptance              | Passed                                                    | Real 8,483-character pinned definition and original reference after newer revision; cancellation stopped descendant in 4,183 ms, resume retained snapshot, assistant proposal applied to draft only; provider-claude-code/2026-09-10T04-57-12-103Z/result.json                                                                                                                                                                                                                                                                                                                                                                            |
| Studio history and saved chats                 | Passed                                                    | Real saved assistant/test transcripts, discard saved draft, older revision restore as a new version, project copy, archive/restore and reload; no browser console errors; `.arc-verification/studio-advanced-browser-result.json`                                                                                                                                                                                                                                                                                                                                                                                                         |
| Codex Studio provider acceptance               | Passed after completion fix                               | Quiet full run provider-codex/2026-09-10T05-13-33-540Z/result.json passed unchanged assertions: 8,465-character pinned definition, marker at 7,690 before the snapshot tool result, original references after newer revision, cancellation in 404 ms, resume and reviewed instructions-only proposal applied without publishing. Earlier full pass and failed timeout attempt remain separate artifacts                                                                                                                                                                                                                                   |
| Windows production package                     | Strict two-launch acceptance passed                       | All 12 build tasks passed. Electron 41.7.0 package passes untouched initial sidebar/composer/plugin recovery, persisted project restart and Agent Studio create/edit/save/reload; `.arc-verification/ARC packaged smoke Δ hVG1ag/result.json`. Actual screenshots inspected. Authenticode remains NotSigned                                                                                                                                                                                                                                                                                                                               |
| Packaged Windows terminal and CLI              | Strict two-launch acceptance passed                       | Real ConPTY output and Unicode file write, packaged `bb.cmd` running through the exact selected ARC executable with Node absent from PATH, persisted project read and graceful server/daemon/runtime/terminal cleanup on both launches                                                                                                                                                                                                                                                                                                                                                                                                    |
| Project reads during provider startup          | Targeted checks passed                                    | Removed live-provider admission policy from read-only project/default endpoints. New three regression tests and 13 existing defaults tests passed; server typecheck 5/5 tasks passed. The adjacent host-path failures discovered during this run are now resolved below                                                                                                                                                                                                                                                                                                                                                                   |
| Remote host path dialects                      | Passed                                                    | Remote storage/worktree/raw file reads preserve POSIX, Windows drive and UNC paths. All 79 thread-data tests and three new route regressions passed (82 total); all 12 previously observed failures resolved. Server/domain typecheck 6/6 tasks passed; `.arc-verification/host-path-tests.log` and `host-path-typecheck.log`                                                                                                                                                                                                                                                                                                             |
| Rebuilt Windows first launch                   | Startup corrected; plugin warning failed                  | Build passed 12 tasks. Untouched initial sidebar bootstrap returns 200 and the welcome action opens a visible composer. Strict smoke retains a stale 13-plugin warning while the API reports healthy enabled plugins; `.arc-verification/ARC packaged smoke Δ DLgZA2/result.json`                                                                                                                                                                                                                                                                                                                                                         |
| Native Windows continuation                    | Diagnostic-only pass                                      | Separate run explicitly reloads after backend startup. Two native launches, real CLI/ConPTY, Unicode paths, project persistence, graceful child-process cleanup and Agent Studio create/native edit/canonical save/reload passed; `.arc-verification/ARC packaged smoke Δ OIGB3X/result.json`. This does not satisfy first-launch acceptance                                                                                                                                                                                                                                                                                              |
| Plugin connection recovery                     | Targeted and native verification passed                   | Three new tests reproduce missing plugin-cache refresh before the correction. The final fix also retains a preboot reconciliation request without eager imports and waits for pending inventory before refreshing. All 223 tests across 20 scoped files and app typecheck 3/3 tasks pass; `.arc-verification/plugin-connect-vitest.log` and `plugin-connect-typecheck.log`. Final package recovers naturally on both strict launches                                                                                                                                                                                                      |
| Packaged plugin-warning diagnosis              | Confirmed harness lifecycle                               | The untouched renderer received final plugin-change events while its native window/document were hidden. Established diagnostic focus/background-throttling controls made the renderer visible and the warning cleared in 518 ms without reload or query mutation. `.arc-verification/ARC observer Δ 6YFQEn/plugin-lifecycle-summary.json` and `renderer-focus-transition.json`                                                                                                                                                                                                                                                           |
| WNDR source recheck                            | Passed                                                    | Current verification found all 6,018 original files unchanged, six tracked deletions still absent, no new source files and original HEAD unchanged; `.arc-verification/source-recheck.json`                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Codex native completion correction             | Focused checks passed; broader platform failures recorded | Removed silence-based completion; ten lifecycle regressions, canonical/recorded conformance and typecheck passed. Full suite: 245 passed, eight failed (six Windows symlink privilege fixtures, two POSIX signal-log fixtures); `.arc-verification/codex-native-turn-final.log`                                                                                                                                                                                                                                                                                                                                                           |
| Team builder through release                   | Pending                                                   | Phases 4–8 remain open; see phase gates in ARC-PLAN                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Initial runtime and Studio regression          | Targeted pass; acceptance pending                         | ARC typecheck 4/4 tasks and 48 tests across six files passed, including eight native Git/process tests, existing Studio coverage and six compiler/store tests; `.arc-verification/arc-runtime-typecheck.log` and `arc-runtime-tests.log`. Subsequent receipt/identity/pause review fixes require another scoped run                                                                                                                                                                                                                                                                                                                       |
| Owned Workflows regression                     | Passed                                                    | Full 257 tests across 17 files, including 25 owned-run checks and seven actual QuickJS/private RPC worker flows; typecheck 4/4. Pause during preparation, old-control replay and persisted-ledger reload covered. `.arc-verification/owned-workflow-full-vitest.log` and `owned-workflow-final-typecheck.log`                                                                                                                                                                                                                                                                                                                             |
| Native commit and inventory regressions        | Passed                                                    | Ten native tests, including a real Git reference-transaction hook moving HEAD after the CAS commit and 6,000 actual files exceeding the public output limit. Tail-file changes invalidate state; bounded inventory overflow rejects. `.arc-verification/arc-runtime-final-tests.log`                                                                                                                                                                                                                                                                                                                                                      |
| Final ARC runtime and Studio checks            | Passed across scoped runs                                 | 73 distinct checks: the full run passed 67 and exposed three compiler metadata failures; the corrected compiler passed all six through the actual Workflows parser/QuickJS. Three additional execution-default service checks passed against real SQLite. Recovery 10, native host 10, Studio UI 9, runtime UI 3, agent server 10, adapter 4, agent data 15, runtime data 3, compiler 6 and runtime service 3. Typecheck 4/4. `arc-runtime-final-tests.log`, `arc-runtime-parser-tests.log`, `phase3-runtime-service-tests.log`, `phase3-runtime-service-typecheck.log`                                                                   |
| Core prepared workers and public surfaces      | Passed                                                    | 107 core behavior checks, 19 authoring documentation checks, 154 database checks including 48 migration checks, 12 API-map checks, 69 SDK fake-host checks and typecheck 9/9. `.arc-verification/phase3-core-regressions-final.log`, `phase3-core-docs-final.log`, `phase3-core-db-final.log`, `phase3-core-db-map-tests.log`, `phase3-core-typecheck-final.log`                                                                                                                                                                                                                                                                          |
| Runs browser                                   | Passed                                                    | Real project/HEAD and published agents, preserved selections during search, builder add/remove, three desktop sizes, keyboard and reduced motion. Final run shows all 17 effects; native evidence opens the exact final receipt and Chat opens the real reviewer transcript. No browser console errors. `.arc-verification/runtime-browser-result.json` and `runtime-completed-browser-result.json`; actual screenshots inspected                                                                                                                                                                                                         |
| Native two-writer runtime                      | Passed                                                    | Real Codex writers in distinct detached worktrees, required join, serial integration, deliberately failed native check, one actual repair, passing sealed check, explicit review and final current-candidate receipt. Original checkout unchanged. Six charged agent calls including two interrupted attempts. `.arc-verification/team-runtime/2026-09-10T07-29-14-289Z-43a94d2a/result.json`                                                                                                                                                                                                                                             |
| Native pause and restart                       | Passed                                                    | Both first writer turns were natively accepted, then interrupted by durable pause. Actual server and daemon stop/restart preserved run/workflow/effect IDs, plan hash, limits, two charged calls and 13,737 ms charged active time. Resume created new attempts and completed the same run. Report above plus `listeners-before-restart.json` and `listeners-after-restart.json`                                                                                                                                                                                                                                                          |
| Runtime CLI on Windows                         | Passed after graceful-exit correction                     | Two compressed HTTP responses followed by forced process exit reproduced Node 24.14's UV_HANDLE_CLOSING failure. Letting plugin command handling finish with process.exitCode corrects teardown. 41 focused tests, including four real compressed-output child-process regressions, and CLI build/typecheck 5/5 passed. Twelve real CLI checks pass across fresh Node, fresh Electron and the existing packaged CLI against the dev server; all three run results are byte-identical. `.arc-verification/cli-crash-readonly-Ak4Tvt/fixed-parity-result.json` and `fixed-show-content-comparison.json`. The package itself was not rebuilt |
| Workspace and full ARC regression              | Passed                                                    | All 86 tests across 12 files, including nine real-SQLite Workspace projection checks, four Workspace UI checks and ten actual native Git/process checks; typecheck 4/4. `.arc-verification/workspace-full-tests.log` and `workspace-service-typecheck.log`                                                                                                                                                                                                                                                                                                                                                                                |
| Live Workspace browser acceptance              | Passed                                                    | Actual Edge UI at 1366x768, 1920x1080 and 2560x1440: one main composer, read-only worker transcripts, provider logos, retained attempts, keyboard/focus/activity and reduced motion; no console/page/HTTP errors. `.arc-verification/workspace-browser-2026-09-10T08-02-19-691Z/result.json`. Final selection-state and adapter changes pass 40 scoped ARC tests and typecheck 4/4 in `workspace-final-scoped-tests.log` and `workspace-final-typecheck.log`.                                                                                                                                                                             |
| Owner-controlled parent notifications          | Passed scoped and native checks                           | New ARC preparations seal passive child notifications; ordinary BB behavior and omitted legacy hashes remain compatible. Core 34 focused tests, 59 regressions plus one corrected Windows documentation fixture, 14 renderer tests, 72 API-map tests and 9 typecheck tasks pass. Exact commands, independent review and preserved failed logs: `.arc-verification/phase4-parent-notification-handoff.md`.                                                                                                                                                                                                                                 |
| Native Workspace handoffs and parent isolation | Passed                                                    | Run `run_8c254e3d-64f0-4497-9cc7-701fb779b264`, two native worker calls, eight effects, two completed 650 ms animations and zero replay after reload. Parent accepted/completed sets remain 6/6 with two passive completion notices. Check, review and final candidate agree on `f745fe3f234209e387323e469db60d7a5b3aba10`; original checkout unchanged. `.arc-verification/workspace-native/2026-09-10T08-28-37-438Z-71478fbe/result.json`, finished `2026-09-10T08:32:39Z`.                                                                                                                                                             |
| Workspace CLI and SDK parity                   | Passed                                                    | Windows Node 24.14 CLI exited 0 with empty stderr; its six retained attempts/17 effects/current verification projection exactly matched the live typed SDK response. `.arc-verification/workspace-cli-parity-result.json`.                                                                                                                                                                                                                                                                                                                                                                                                                |

## Alignment checkpoint 1

The foundation is BB/WNDR's current Windows working tree, not MonoCode. BB internal namespaces remain compatible. The destination is independent, and source WNDR remains untouched. New user features wait on a verified extracted foundation. Existing WNDR test/release documents describe historical WNDR artifacts and are not ARC evidence.

## Release prerequisites still requiring evidence

Actual ARC source/release destination and signing identity; real sessions for each advertised provider; clean Windows installer/update/uninstall verification; the complete project/factory and retrieval acceptance suites. These are unverified gates, not completed work.

## Alignment checkpoint 2

Desktop identity and forecast packaging extraction have targeted evidence. Package and actual application gates remain open. Agent Studio code is being prepared in parallel, and is not yet enabled as a verified product feature. It reuses BB plugin storage, model picker and actual ThreadChat, with no second scheduler or simulated chat. The first-turn instruction race is being addressed with an immutable owner-bound context ID stored before dispatch and fail-closed configuration. The default BB palette is retained and first launch chooses dark mode.

An initial dependency prepare failed because Turbo selected ambient pnpm 11; a local Corepack 9 shim fixed it. An initial desktop browser test timed out under concurrent native work; isolated and final coordinated reruns passed without weakening its assertions. Local verification logs are excluded from Git under `.arc-verification/`.

## Alignment checkpoint 3

Studio stores canonical definitions, immutable revisions and references, and uses actual BB assistant/test tasks. References are currently stored bytes with paged UTF-8 access; this does not claim the later shared retrieval system. Workflow outcome helpers preserve explicit failures, but worktree isolation, durable pause, the team canvas and factory are still pending. The first browser run exposed a live FileList reset race; copying the selection before asynchronous saving fixed it. A subsequent run hit a plugin rebuild 503; another exercised reference attachment and publishing successfully. Acceptance continues against a stable app, including laptop panel widths.

The namespace review found remaining upstream self-update fallbacks and a missing packaged Windows CLI shim. Those are being corrected before package acceptance. Full plugin SDK tests also found Windows symlink privilege limits and an atomic-promotion rename race; targeted feature tests do not stand in for that broader gate.

## Alignment checkpoint 4

The verified Studio browser path uses the actual application and persisted data. A restored single sidebar panel could disagree with its host's selected tab; the correction passed the existing panel tests and real reload interaction. The file input is hidden behind its accessible Add files button, preventing a clipped native input from extending the outer page. These fixes retain the shared pane system and BB dark design.

Windows drive, UNC, spaced and Unicode project-path regressions passed 39 checks. Actual Claude acceptance now passes; Codex has a confirmed inherited false-completion path still being fixed. Current provider checks run in independent disposable Git repositories and never in ARC's own checkout. Changes and package builds are coordinated around live-provider runs to keep development reloads out of acceptance. The packaged executable and full installer gates remain open.

## Alignment checkpoint 5

Agent Studio review found two editing defects: metadata fields trimmed spaces during typing, and a saved unpublished draft could not be discarded in favor of the current published version. Both are corrected and covered by the latest tests and actual browser interactions. The existing pane system, canonical Markdown, explicit publication and immutable running snapshots remain intact.

The Codex bridge no longer fabricates completion after 250 milliseconds without events. Its first post-fix native turn verified the full pinned definition and references, but the subsequent cancellation probe did not start within the harness bound. That attempt remains a failure; the earlier full pass is not substituted for post-fix acceptance. A package build overlapped this run after a coordination handoff; no development reload errors were observed, and a further native run must use a quiet build window.

Phase 3 investigation confirms that the existing Workflows scheduler must be extended with owner-bound execution and durable attempts/effects. Current response caching and process-local Git locks alone cannot prove exact-candidate checks or safe recovery. The next implementation contract will preserve that scheduler, keep host effects separate from product policy, and require actual worktree, check-failure repair and pause/recovery evidence. No team graph, shared Context or factory completion is claimed.

## Alignment checkpoint 6

The quiet post-fix Codex run passed the full unchanged native acceptance. The failed timeout attempt remains in the evidence trail. Windows packaging then exposed a real first-launch failure: sidebar bootstrap returned `409 no_provider_available` while provider plugins were still registering. The read route incorrectly used thread-start admission policy. Returning persisted project defaults directly fixes the cause without weakening provider requirements for starting a task; the new regression reproduced the 409 before the correction.

The package harness also incorrectly required a composer on the intentional welcome screen and treated retained offscreen panel text as visible UI. Its corrected acceptance follows the visible Start a new conversation action and then requires a visible composer. A separate diagnostic confirmed the actual packaged terminal and CLI path. First-load renderer checks must run before project creation, since that mutation can invalidate and conceal stale startup data.

Phase 3's contract proposal is retained in `.arc-verification/phase3-contract.md`; no runtime production changes have begun. Package correctness takes precedence over starting the team graph. A broader adjacent test run revealed a real host-path dialect bug: Windows server path joins were rewriting remote POSIX paths. The correction extracts and reuses the existing host-path logic across storage and file reads; its scoped tests pass, including all 12 earlier failures. It changes no SDK or daemon wire fields. The combined Windows rebuild includes both startup and path corrections.

## Alignment checkpoint 7

The combined package confirms that project reads now work during provider startup. Its next strict gate found a stale plugin warning after the backend recovered. Connection reconciliation omitted plugin inventory and related queries; targeted regressions fail before the correction and pass afterward. The observed renderer made only one plugin-list request. A missed startup notification is consistent with the source and observed state, but the original browser socket timing was not captured.

The correction reuses the existing query invalidation and deferred plugin frontend reconciliation paths. It adds no polling loop and does not suppress real plugin failures. A separately labeled native diagnostic proves the terminal, packaged CLI, restart persistence and Studio workflow, while retaining the unresolved first-launch result. Phase 3 still uses Workflows as its only scheduler; no visual graph, Context or factory completion is claimed.

The refined [runtime contract](ARC-RUNTIME.md) records the core preparation hold, immutable first input, owner-bound internal RPC, Workflows attempts and budgets, and native effect receipts. Preparation is a required addition because ordinary spawn immediately dispatches and current provisioning context is partly process-local. A missing acknowledgment will require reconciliation instead of an automatic worktree/setup replay.

## Alignment checkpoint 8

The next strict package run still timed out on the warning, so passing cache tests were not treated as native acceptance. Early CDP observation then captured the actual socket, subscriptions, inventory responses and native visibility. The renderer received the final plugin-change events while `document.visibilityState` was `hidden`; its intentional visibility buffer deferred invalidation. This disproves the earlier missed-before-socket hypothesis for that reproduction.

The harness now uses the existing desktop browser diagnostic focus and background-throttling pattern. In the held reproduction, visibility changed to visible and the warning cleared naturally in 518 ms, without a reload or direct cache change. Production hidden-page buffering remains unchanged. The separately verified connection-cache and lazy-boot regressions remain valid recovery fixes, but they are not described as the cause of this hidden-window failure. Full fresh-profile, two-launch acceptance still needs its final result.

## Alignment checkpoint 9

The final strict fresh-profile smoke passed both launches before any reload, with natural plugin recovery, native CLI/terminal operations, project and Unicode file persistence, Studio editing and graceful cleanup. Native screenshots show the existing BB dark panes and actual Studio controls. Early renderer capture recorded no console calls or exceptions; startup network diagnostics still include an aborted fetch, provider discovery 502 and two connection-refused fetches, so no zero-network-error claim is made. The failed and diagnostic-only attempts remain preserved separately.

Phase 3 implementation is now authorized by the passed earlier gates. Work is partitioned across core preparation/internal RPC, the existing Workflows scheduler, ARC host effects, and ARC's immutable run definition/compiler/adapter. Managed-worktree provisioning currently resolves a moving branch, so ARC must compare the prepared HEAD with the run's sealed source OID before releasing an agent; this remains preflight, not an atomic daemon guarantee. No graph, Context, factory or public-release completion is claimed.

## Alignment checkpoint 10

The runtime now seals project agent revisions, source state, commands, execution settings and its bounded compiled manifest. Its initial path is parallel prepared writers, required join, serial native integration, check-failure repairs, explicit reviewer approval and unchanged final candidate. Runs, typed SDK/RPC, CLI and native evidence inspection are connected in code, with actual end-to-end acceptance still pending. Workflows is enabled by default for new installations; existing explicit plugin preferences are preserved.

ARC now creates its run-owned detached worktrees at an exact commit through the journaled host effect, then registers those paths through core's existing unmanaged-environment provisioning. This avoids moving branch resolution and `worktree add -B` for ARC runs. Core still preflights before release; no atomic expected-SHA guarantee at provider dispatch is claimed. The host uses actual Git CAS commits and real process receipts. Eight native tests passed, including two writers, integration, check, interruption and crash no-replay; a fixture-only line-ending discrepancy was corrected without changing product Git behavior.

Review identified additional correctness work after the first green run: first terminal check observations must honor current receipt validity; dependencies must resolve their sealed attempt IDs; canonical worktree identity must survive path replacement; and advancing observation must be cancelled by run pause. Those fixes and core dispatch-hold/recovery tests are ongoing. This is not yet Phase 3 acceptance, and later phases remain unverified.

## Alignment checkpoint 11

The full Workflows regression suite passes without a second scheduler or relaxation of required gates. Core's automatic first-turn release and existing dispatch/provisioning regression batch also passed; remaining database and type checks are being finished. ARC's three new UI checks pass for uncertain start/control retries and displaying current verification separately from historical success. Six initial recovery fixtures incorrectly omitted bound resources required by the contract; those fixtures were corrected, with their failures retained in the log, and the final expanded run remains pending.

Cross-component review found two actual integration gaps before native acceptance. The generated ARC program now includes the metadata header required by Workflows' source parser, and compiler tests go through that parser before QuickJS. A failed check consumed by an admitted repair must remain historical evidence after the repair changes files. ARC verifies the exact dependency hash, sealed candidate, prepared thread and native accepted request before treating it as consumed; successful checks and final verification retain current-candidate requirements. Queue cancellation before client-request allocation also becomes a confirmed interruption rather than indefinite reconciliation.

The next gate remains a real two-writer run with native pause, application restart, retained-run resume, a failed check, repair and explicit review. The native host now has a separate bounded 4 MiB filename-inventory capture while public check receipts retain their 64 KiB output limit; overflow still rejects instead of hashing an incomplete inventory. These changes await final tests and actual application evidence. The visual team graph, shared Context, factory and public release remain later phases.

## Alignment checkpoint 12

The real Phase 3 acceptance completed at `2026-09-10T07:37:33Z`. Run `run_0291b227-ac73-4a9d-bbe4-b721065533ff` resumed after an actual server/daemon restart and finished through the existing Workflows scheduler. Two writers produced separate frontend/backend commits; their first accepted attempts were interrupted by pause. Serial integration exposed the deliberately retained pricing defect. One repair corrected it, the exact sealed native command passed, and the reviewer approved commit `01124708e324156692f948b2cdf45a8caa3b6eb3`. The check, review and final snapshot agree on that commit and workspace state. The original checkout is unchanged. This is local runtime acceptance, not merge, deployment or whole-factory acceptance.

The first launch attempt returned a plugin-loading 503 before creating an agent, thread or run; its separate failure report remains under `team-runtime/2026-09-10T07-27-58-159Z-737d17b7`. Final parser and service corrections were verified in scoped runs after the broader test run; the earlier failed log is retained rather than described as a clean full-suite run. Saved project execution choices take precedence, with parent-thread settings used only when the project has no defaults. Published agent overrides remain first and every choice is sealed before execution.

Actual browser inspection confirms the operational Runs surface and real reviewer chat. The visual graph, colored team workspace, delegation configuration and animated handoffs remain Phase 4 work. Read-only CLI checks found a separate system-Node shutdown assertion after successful output; the identical new CLI under the packaged runtime exits normally. That investigation does not change the passed native run or imply a freshly rebuilt Phase 3 desktop package. The next phase extends these verified runtime contracts and retains BB dark, the original composer/chat components and one scheduler.

## Alignment checkpoint 13

The CLI shutdown correction now passes real process tests and native command parity. Phase 3's scoped gate is complete. The independent native evidence review is `.arc-verification/phase3-native-acceptance-review.md`; it distinguishes first-turn provider acceptance from cancellation of an already executing tool. Actual process-tree cancellation and exhausted-limit behavior have separate targeted tests, rather than being claimed as part of the one native end-to-end run.

Phase 4 begins with an actual Workspace over retained runs. The main BB conversation owns the sole composer; worker panes show existing timelines, pinned identities and selectable attempts. Focus and conversation selection expose every retained worker while limiting simultaneous rendering to four worker chats. The typed `getWorkspace` RPC and `bb arc workspace` share this data. Its cursor projects recorded admitted/prepared/native-accepted milestones; a queued request is not treated as provider acceptance. Initial load and reconnect establish a baseline, and reduced motion suppresses animation.

The review corrected transient-outage handoff replay, confirmed pre-dispatch cancellation status and navigation to earlier workers in a six-node layout. The complete 86-test ARC suite and typecheck pass. Actual browser and fresh native handoff acceptance remain underway. [ARC-TEAMS.md](ARC-TEAMS.md) records the remaining full graph, control and policy implementation sequence; this Workspace increment does not mark the entire Phase 4 gate complete or invent team colors for runs without a published team snapshot.

## Alignment checkpoint 14

The live Workspace increment passed after an explicit restart of the owning development server/daemon. Core source is loaded once by the development supervisor; plugin hot reload alone would not have exercised the new notification guard. The restarted instance `54c0df7a-8a0a-4f6b-a96e-f8374662dae3`, session `hses_f6mbnz8htr`, ran the fresh native acceptance through the same Workflows scheduler. No code edits, builds or restarts occurred during that run.

The run was durably paused with zero calls before browser observation began. After resume, one writer created the exact proof file in its isolated worktree; serial integration and the trusted native check passed. The reviewer approved the exact final commit. Both newly accepted dispatches produced real completed animations in the same browser document; a fresh reload produced none. The parent's accepted and completed event sets remained unchanged, with one passive completion notice per worker. Manual main-chat turns remain outside the worker counter, and future automatic orchestrator responses still require their own budgeted admission.

The earlier Phase 3 run had five automatic parent acknowledgments outside its six charged worker calls. That historical limitation is retained in ARC-TEAMS; it is not erased by the new acceptance. New opted-in preparations record child updates without dispatching parent turns, while old reservations retain their original semantics. Independent review covered ordinary mixed batches, queued sender bypasses, deduplication and terminal display. The current package remains older and unsigned.

The next bounded increment is the complete versioned authoring surface from ARC-TEAMS Step 2. It retains the full graph vocabulary, immutable revisions, dependency-preserving project copies and explicit collaboration grants. Structural validity and available execution capabilities will remain distinct until the general compiler, durable controls and project/session policy are connected. Team authoring alone will not close Phase 4 or substitute for the later Context, factory, appearance and release gates.

## Alignment checkpoint 15

Team authoring is now connected across the visual editor, persisted drafts and immutable revisions, reviewed suggestions, project copies, CLI and typed plugin RPC. Its graph represents agent tasks, parallel paths, joins, native checks, reviews, conditions, bounded repair, approvals, integration, delegation and release. Organizational nesting and colors remain separate from directed collaboration grants. Drafts may be incomplete; publication requires structural validation. Execution capabilities remain explicitly unavailable until the general Workflows compiler and policy are implemented.

Independent store/service review found no concrete scope, copying, revision or concurrency defect. The backend has 23 focused passing tests after adding copy-specific agent invalidation, plus 10 assistant binding tests and 12 graph-coordinate tests from their scoped runs. Five integrated editor tests pass with real SQLite, including lost-edit conflicts, saved-draft assistant submission, required any/all results and stale proposal rejection. The registered server integration file passes all 15 tests; current ARC typecheck passes four Turbo tasks. Evidence: `.arc-verification/teams-editor-server-tests.log` for editor/service passes, `.arc-verification/phase4-team-server-tests-passed.log` and `.arc-verification/phase4-team-server-typecheck-passed.log` for corrected server fixtures. Earlier fixture failures remain recorded.

The tests exposed and corrected a local draft restoration bug that trimmed unfinished text. The actual browser pass then found React Flow's default light class overriding BB dark; its correction and actual graph interaction acceptance are underway. The native Team assistant harness is prepared, but has not yet run. A 200-node round trip in a data test is not the required measured graph-plus-four-live-chats performance gate. General graph execution, project/session autonomy and the later Context, factory, appearance and release phases remain open.

## Alignment checkpoint 16

The integrated Team authoring regression run passed 81 tests across 10 files and all four ARC typecheck tasks: `.arc-verification/teams-integrated-regressions.log` and `.arc-verification/teams-integrated-regressions-typecheck.log`. Actual canvas acceptance passed six gates for dark styling, nested dragging, keyboard movement and connection outcomes, persisted reload and a 200-node/199-edge graph. Its evidence is `.arc-verification/team-canvas-browser-2026-09-10T09-14-00-452Z/result.json`. A second explicit bounds check found and fixed a laptop-height clipping issue; eight subsequent layout gates passed at 1366/1920/2560 with visible keyboard controls, open/closed assistant, the agent picker and reduced motion: `.arc-verification/team-canvas-layout-2026-09-10T09-16-36-614Z/result.json`. Those graphs did not have four simultaneously running chats, so the combined performance gate remains open.

The first real Team assistant attempt failed before provider acceptance. Its exact error was `Failed to fetch skill tree: 404 Not Found - Skill tree not found`; retained evidence is `.arc-verification/team-authoring-native/2026-09-10T09-17-08-989Z-adf7749d/result.json`. The unavailable hash belongs to the generated plugin-command skill, not the Team snapshot. Core investigation confirmed that this Windows volume enables last-access updates, Node 24.14's Windows watcher requests last-access notifications, and the plugin development loop treated every notification as a source change. Read-only inspection therefore caused reloads. Those reloads rewrite the generated guide, exposing a race between its advertised hash and the host fetch; the timing and mutable-source route support this as the cause of this failure. The source files' modification times had not changed. This is a necessary inherited startup correction discovered by native acceptance, not a new ARC product phase.

The pending correction filters development reloads by real source content and retains bounded immutable registered skill manifests so a later source rewrite cannot invalidate an in-flight fetch. The runtime's required-skill failure remains explicit; it is not bypassed. A further screenshot review found overlapping connection labels and missing automatic group bounds; those authoring refinements are being verified before the next native attempt. No Team native acceptance or general graph execution completion is claimed. The next gate is the actual assistant proposal, explicit review/application, immutable publication and CLI/SDK parity, followed by graph V2 compilation and durable controls in the same Workflows scheduler.

## Alignment checkpoint 17

Team authoring's native acceptance passed at `2026-09-10T09:44:45.808Z`: `.arc-verification/team-authoring-native/2026-09-10T09-43-56-309Z-26caef8e/result.json`. The actual UI created the library team, applied and saved its full canonical definition, and published version 1. This uses the advanced JSON editor; separate canvas gates cover visual wiring and group manipulation. The assistant's native request `creq_8mtsdcjdin` was accepted as turn `da7b49aefe-t1` and completed normally. Its three tools read the bound snapshot, read the saved draft and stored exactly one pending proposal. Twenty-one streamed deltas and the provider's returned context marker corroborate the real interaction. The UI then reviewed and applied the exact change, published version 2 and reloaded its history. Version 1 remained immutable; CLI/SDK output matched; a project copy preserved its remapped agent revision; the original disposable checkout remained unchanged. Native screenshots were inspected.

The owning server/daemon was restarted before acceptance: instance `cc3a5235-6434-4194-a8e6-fcf09042e52d`, session `hses_4zb985298c`. The Windows reload correction passed six filesystem, ten loop and 31 builtin tests; immutable skill delivery passed 47 tests across five suites. Relevant typechecks passed. A bounded source-read observation after restart produced no reloads: `.arc-verification/plugin-source-read-restarted-final.json`. Its earlier malformed probe is explicitly marked invalid and is not evidence. The graph's final 16 tests and four post-restart browser geometry gates passed, with no captured console/page/HTTP errors. Evidence: `.arc-verification/phase4-team-canvas-final-tests.log`, `.arc-verification/phase4-team-canvas-final-typecheck.log` and `.arc-verification/team-canvas-autobounds-2026-09-10T09-41-53-197Z/result.json`. The original skill-tree failure remains retained separately.

The next implementation partition is graph V2 in the existing Workflows ledger, preserving V1 definitions and replay hashes. It adds enforceable selected dependencies, durable owner controls and candidate-derived worktree creation so repair rounds preserve prior check evidence. Project/session policy resolves and seals before admission. Pure user/CI waits may stop the active-work clock only when no native work remains active; owner reconciliation and dependency waits stay charged. This is an explicit interpretation of active work, not a reset of the existing budget. Main-orchestrator continuation admission, serial non-Git execution, graph-plus-four-live-chat performance and the remaining Phase 4 controls still need implementation and acceptance. Shared Context, factory, appearance and public release remain later phases; the desktop package is still older and unsigned.

## Alignment checkpoint 18

Graph V2 now extends the existing Workflows scheduler and retains the unversioned V1 manifest/receipt path. The full Workflows suite passed 274 tests before the additional race correction; 35 scoped ledger/worker tests then passed after reproducing and fixing delayed control revisions. The compiler passed 17 graph and six legacy cases, including alternative repair coverage and explicit full-candidate integration. Native Windows Git acceptance passed four new candidate-merge cases for sequential ancestry, shared ancestors, conflicts and preflight rejection; V1 integration remains unchanged. Evidence: `.arc-verification/workflows-v2-full-tests.log`, `.arc-verification/owned-control-revision-green.log`, `.arc-verification/graph-compiler-merge-final-tests.log` and `.arc-verification/phase4-native-merge-tests.log`.

Independent integration review reproduced duplicate proof admission, consumed Guided approval replay, stale-source cancellation and incomplete transitive integration. Their fixes retain exact proof identities and all outcome constraints, recognize only an actually accepted bound writer as an approval consumer, cancel local waiting controls before unavailable-source checks, and merge complete V2 candidate histories. Seven new actual-SQLite ARC/Workflows recovery tests passed, with the 14 existing adapter/recovery tests passing in the prior scoped command. The new fixture initially passed a whole step instead of its node/iteration reference; its separate failed log is retained. Proof admission plus legacy store tests passed ten cases. Relevant combined ARC/Workflows typechecks passed before the subsequent UI/test additions. Evidence: `.arc-verification/graph-control-recovery-second.log`, `.arc-verification/graph-control-recovery-initial.log`, `.arc-verification/graph-admission-green.log` and `.arc-verification/graph-admission-typecheck.log`.

Project/session policy persistence passed six tests; its actual-store UI passed eight and a subsequent exact-version display regression. The settings and graph-run/decision surfaces are being integrated with the typed SDK and CLI. The native published-Team run and policy browser acceptance have not started. Protocol 185 requires a restart of the owning development server/daemon before those gates. [ARC-GRAPH-RUNTIME.md](ARC-GRAPH-RUNTIME.md) records current contracts and remaining limits. This remains Phase 4: the main-orchestrator continuation, serial non-Git execution, reviewed rule changes on active work, combined performance and later Context/factory/appearance/public-release gates are still required.

## Alignment checkpoint 19

The graph service, admission, recovery and actual control-driver regression batch passed 52 tests across four real-SQLite suites, with four ARC typecheck tasks passing: `.arc-verification/graph-driver-service-final.log` and `.arc-verification/graph-driver-service-typecheck.log`. The driver exercises compiled QuickJS through Workflows admission and ARC controls using explicit native receipt fixtures; this is not provider acceptance. Its initial failures came from incomplete condition branches and an incorrectly projected strict failure receipt in the test bridge. Those failed logs remain retained, and no production driver defect was found in this increment.

Pending-start recovery now durably retires the exact request before allowing setup changes. The final tombstone check and run reservation share a transaction after asynchronous preflight, so a late request cannot allocate work after retirement. An already reserved request is returned for reconciliation. The browser persists the recovery action before sending it and retries that action after response loss. Sixteen graph-service cases, fifteen graph UI cases and three legacy UI cases passed in scoped runs; independent read-only review found no concrete discard race or lost-reservation defect.

Published supported teams now expose their runtime capability and a Run version action. Workspace panes display the sealed team group name and color. Integrated verification initially passed 209 tests with one stale pre-runtime capability assertion failing; that assertion and the daemon protocol test were updated. The final integrated run passed all 254 ARC tests across 26 files, including 20 real native Git cases, and all 56 daemon contract tests: `.arc-verification/graph-integrated-final-tests.log`. ARC, Workflows and server typechecks passed all eight Turbo tasks: `.arc-verification/graph-integrated-final-typecheck.log`. No later gate is promoted by these tests.

## Alignment checkpoint 20

Published-Team graph execution passed actual native acceptance at `2026-09-10T11:18:34.198Z`: `.arc-verification/graph-runtime/2026-09-10T11-06-11-362Z-1bcdb6b4/result.json`. Run `run_a1a8be4b-bc15-43c3-9bee-db4e585d19bf` paused at its Collaborative plan approval with zero calls and only one owner-control effect. An actual owning server/daemon restart changed workflow generation 1 to 2 while retaining the same control ID, context hash, revision and limits. The browser then approved that exact decision. No source edits, builds or restarts occurred during native execution.

Two native writers produced independent frontend/backend commits. Two serial full-candidate merges retained their histories. The native required check failed on the actual pricing assertion, 201 cents instead of 200. A separately admitted repair used a fresh fork and corrected pricing; repair and final checks passed. The reviewer explicitly approved `e78964bfe3954324af48f3f06bd4de76a75e9813`, matching final verification and clean workspace state. All 25 effects settled with exactly four charged worker calls. Original checkout files and Git state were unchanged; the failed candidate remained unchanged; CLI and SDK inspection matched.

The same visible browser document recorded four native-acceptance handoffs, each with a completed 650 ms animation. Reload recorded none. All four worker panes showed sealed group colors and provider icons, with one main composer and read-only worker conversations. The parent's accepted/started/completed event sets were unchanged, and exactly four passive child notices were recorded. This does not claim automatic main-orchestrator response admission or the 200-node/four-simultaneously-running-chat performance gate. Visual review also found misleading retained provisioning status rows and an initial-message scroll position that needs investigation; runtime acceptance does not erase those presentation issues.

The settings browser passed nine gates with no captured page/console/HTTP errors: `.arc-verification/policy-browser-2026-09-10T11-07-19-776Z/result.json`. It verified exact version-1 team pins while version 2 exists, millisecond limit persistence, project/session isolation, concurrent edit recovery, immutable history, CLI/SDK parity and 1366/1920/2560/768 layouts. The session override was restored through a new revision with identical native turn-event sets. Two earlier harness-only failures are preserved separately: exact label matching and hit-testing an intentionally disabled button.

The graph acceptance used protocol 185, first restarted under host instance `7477b29f-1ec9-43a6-9767-6d81cadd9732` / session `hses_nd8gc2iki7`, then resumed under `8979d60d-ac2d-4691-a7f0-be3d6475463b` / `hses_zs5j4ju8s8`. Evidence logs are `.arc-verification/arc-graph-native-dev-185.log` and `.arc-verification/arc-graph-native-resume-dev-185.log`. Next work remains Phase 4: correct the observed presentation issues, admit main-orchestrator work under explicit limits, support serial non-Git candidates, apply reviewed rule changes and close combined performance/accessibility gates. Context, factory, appearance and public release remain later phases.

## Alignment checkpoint 21

Independent read-only audit passed the retained native graph run, its 25 receipt hashes, actual Git ancestry, preserved original and failed candidate, and final check/review identity. The audit is retained beside checkpoint 20's result as `independent-audit.md` and `independent-audit.json`. Its acceptance does not erase the two observed UI defects.

Contained worker transcripts now use the shared bottom-anchored timeline, retaining manual history scrolling while opening at the latest response. Four reproduction cases failed before the correction; the final scoped batch passed 41 tests across six files and all three app typecheck tasks. Actual read-only browser verification passed at `.arc-verification/transcript-scroll-browser-2026-09-10T11-30-49-190Z/result.json`: all four native final responses visible, history retained across polling, reload/focus/compact behavior correct, no provider calls or blocked mutations. The main screenshot was inspected. Existing Jotai deprecation warnings remain recorded.

The misleading provisioning row had a separate cause: the prepared-worker environment-ready path omitted its terminal provisioning event. Its correction atomically records the event, settled preparation and idle lifecycle before notifying subscribers. Seven new real-SQLite cases plus twelve existing preparation cases passed in `.arc-verification/prepared-provisioning-event-tests-fixed.log`. Core typecheck and native zero-call readiness verification remain pending the next coordinated core restart; this is not yet a native presentation pass. The additional main-origin admission guard passed 27 scoped graph-service/policy-UI tests and four ARC typecheck tasks.

The next slice remains Phase 4: native main-composer team discovery and admission, followed by one separately counted response in the same conversation. Additive ARC definition V3 wraps unchanged V2 graph execution in the existing Workflows ledger. The required invocation field is null only for direct trusted user SDK/CLI admission; native tool requests derive identity from core, never model-supplied IDs. Automatic owned turns cannot use the native ARC admission tool to mint independent run budgets. Inherited shell/network access and trusted local user HTTP routes remain BB's trust boundary, not an ARC sandbox. Further automatic episodes require the later finite Factory grant.

Initial published-team discovery passed 17 tests in `.arc-verification/orchestrator-discovery-second-tests.log`; nine prior failures were a fixture's incorrect fake-host property and remain preserved. Subsequent review found cancellation during preflight and discovery selecting the wrong enrolled host. Both are corrected in source with regressions awaiting the next ARC batch. Native-call response loss now has explicit user reconciliation of the retained run ID. V3 SDK/CLI/tools, continuation adapter, existing-thread preparation and their integrated acceptance are in progress; no native main-orchestrator pass is claimed.

The conditional stop prerequisite passed ten real daemon/provider-child-process tests, 57 daemon-contract tests, and six host/runtime/contract typecheck tasks. Evidence: `.arc-verification/conditional-stop-native-tests-final.log`, `.arc-verification/conditional-stop-contract-tests.log`, `.arc-verification/conditional-stop-typecheck-final.log`. It preserves old unconditional stop and adds protocol 186 exact-request/turn stop observations. Earlier failures exposed a missing RPC-success union member and an incorrectly nested scripted-provider fixture; both failed logs remain retained. The currently running development host still uses protocol 185 until the coordinated restart.

## Alignment checkpoint 22

Main-orchestrator V3 is connected across native tools, contextual discovery, trusted-user SDK/CLI, Runs recovery/approval display, Workspace, immutable completion preparation and the existing Workflows budget. The integrated partition has 134 distinct passing scoped ARC tests and four passing ARC typecheck tasks. Evidence: `.arc-verification/orchestrated-runtime-focused-tests.log` (nine compiler/real-ledger and ten adapter cases), `.arc-verification/orchestrated-service-ui-tests.log` (33 service and 16 graph-UI cases), and the other green suites recorded in `.arc-verification/orchestrated-runtime-verification.md`. Superseded import, selector, failure-envelope and type-fixture errors remain recorded. Scoped lint had zero errors and six existing React effect/ref warnings in Runs panels; `.arc-verification/orchestrator-scoped-lint.log` records them.

Core's additive prepared-turn API is backed by generated migration `0115_worthless_sheva_callister.sql`. It binds owner, operation, input, provider settings, environment, client request and native evidence without changing the main conversation's permanent identity. The singleton queue path rejects send-now/batching/retry/history escapes. Editing a history suffix containing owned native evidence is rejected before deletion, including a completed turn not yet observed by its owner. Its exact conditional stop never claims terminal state without native evidence. Core verification: expanded server 99, final prepared-turn 12, SDK 73, database queue/migration 80, and eight server/SDK/database typecheck tasks. Logs: `.arc-verification/prepared-turn-tests-third.log`, `.arc-verification/prepared-turn-final-focused.log`, `.arc-verification/prepared-turn-db-tests-final.log`, `.arc-verification/prepared-turn-typecheck-green.log`. These are scoped overlapping suites, not an additive unique-test total. Independent source review corrected malformed SQL and the history-suffix edge before native work.

The read-only retained-native-graph probe compiled V3's additional step around the prior accepted 25-effect graph and correctly classified both original final gates as settled/successful. `.arc-verification/orchestrated-retained-graph-probe.json` records 26 declared steps and zero provider calls. This checks graph/evidence compatibility only; it neither reuses V2 acceptance as V3 admission nor claims a new native result. The initial standalone probe could not resolve the plugin-scoped SQLite dependency; its failed log is retained separately.

The next gate is actual native V3 acceptance through the main conversation, exact preferred revision, initial approval, owning restart, four real worker turns and one counted main response. The extended graph harness preserves its V2 modes. Read-only review found a mismatched resume hash and gaps in send-response-loss cleanup/native identity evidence; these are being corrected before provider invocation. Production source is frozen for that window. The owned protocol-185 development session was stopped and all three ports verified clear; the protocol-186 development session is starting under `.arc-verification/arc-orchestrator-dev-186.log`. No V3 native acceptance is claimed yet. Serial non-Git candidates, reviewed active-rule changes, combined graph/chat performance/accessibility and the later Context, Factory, appearance and public-release phases remain required.

## Alignment checkpoint 23

Protocol 186 started successfully with migration 0115, host instance `c3ec4d6c-ce73-4316-bd06-96972de2b913` and host session `hses_kjwypwqtnq`. The harness's offline review verified five retained native event chains, rejected six tampered identity cases and preserved the prior V2 plan hash. It now persists exact input/queue/event baselines before sending, binds requested/accepted/started/completed events and the native tool item, and refuses guessed cleanup after an ambiguous send. These checks are not new native acceptance.

The first native V3 attempt failed before run admission: `.arc-verification/orchestrator-runtime/2026-09-10T12-19-23-162Z-05ddd2bc/result.json`. In project `proj_w2b5txee56`, main conversation `thr_ucbrdihtey` correctly discovered preferred/restricted team revision 1 while revision 2 existed. Its exact native `arc_team_run_request` returned `execution_configuration_missing`. The turn completed; no team run was created and cleanup confirmed no run from that settled request without sending cancellation. Actual BB default execution options include `source: "client/turn/requested"`; spreading that response into ARC's strict execution tuple rejected valid settings. A focused regression reproduced the same error in `.arc-verification/orchestrator-execution-source-before.log`. The correction explicitly projects the five execution settings instead of accepting or persisting unrelated response fields.

Independent V3 review also found that shared original-source validity checked HEAD, state digest and common Git directory but omitted the canonical path. A same-repository worktree redirection could therefore retain current validity. Its narrowly scoped regression and fix are in progress. The failed native window is closed, the owned development session was stopped, and all three ports were verified clear before the next corrected build. Phase 4 remains in progress; neither failure is treated as a harness-only issue or a passed acceptance gate.

Both corrections now passed 48 focused tests across the shared native adapter, graph service and main-turn adapter, plus all four ARC typecheck tasks. Evidence: `.arc-verification/orchestrator-native-prerequisites-after.log` and `.arc-verification/orchestrator-native-prerequisites-typecheck.log`. The source-redirection case failed before the fix in `.arc-verification/origin-canonical-path-before.log`; after correction the historical receipt hash is preserved while current validity becomes stale. The corrected app is restarting under `.arc-verification/arc-orchestrator-fixed-dev-186.log` for a fresh disposable native attempt.

The second native attempt reached actual V3 admission through main native tool item `da32399369-i6`, provider thread `01a08b46-639e-74b3-8c2d-faad46197d08`, turn `da32399369-t2`. It preserved the requested Codex execution tuple and exact team/policy/source binding. Artifact: `.arc-verification/orchestrator-runtime/2026-09-10T12-23-54-568Z-f9bdd7fa/result.json`. Run `run_f12018df-7733-400f-b9da-2360beb79a27` reached its plan approval and durable pause with zero calls and one retained pending control. The harness then incorrectly required an observation resource after pause had cancelled an in-flight source lookup. The durable control ID/context/revision remained present, while that observation had `resource: null` and a cancellation reason. Exact-owner cleanup cancelled this run with zero active agents. This partial native evidence is retained as a failed attempt; the pause/restart assertions are being corrected to distinguish retained authority from transient observation resources before repeating acceptance.

## Alignment checkpoint 24

The corrected harness passed initial admission and durable pause for run `run_0cc4d78f-2569-4dfb-974b-967b40f6ddff`, workflow `wfo_9e589315-1625-4f57-9a08-53b6d116b685`: `.arc-verification/orchestrator-runtime/2026-09-10T12-28-03-770Z-1399c137/result.json`. An actual server/daemon restart retained control `wfe_1daf5528-f8ee-44df-b01d-f962877e4eeb`, context hash `0503bb7da5a1eb31d8ac57518ab6f4803ee709fea567b6e2a40c7ccd9b2404c6` and revision 1 while increasing workflow generation 1 to 2. The resumed host instance was `2d45d688-5a02-4eb3-93a4-721fda2e018d`, session `hses_gz2fgasrn3`, in `.arc-verification/arc-orchestrator-resume-dev-186.log`. Browser approval then dispatched two isolated native writers. The required native check failed on the actual 201-versus-200-cent assertion; a fresh repair candidate passed repair and final checks, and the fourth native worker completed review. The newly provisioned writers and repair worker had terminal completed provisioning events before native acceptance. The reviewer reused an existing ready environment and therefore had no new provisioning attempt; the harness is being corrected to prove that distinct path through retained preparation/environment identity.

The V3 completion gate exposed a production timeout rather than passing. A fifth call was reserved in Workflows, but a targeted read-only core query found no main-turn preparation or native completion. The new completion adapter revalidated 25 retained graph effects sequentially, starting a separate dependency cache for each and repeating source/candidate inspection. The callback timed out repeatedly; caught cancelled inspections also overwrote retained validity with stale observations. The final main response therefore never started. The run was cancelled through its exact typed SDK identity/control version after this repeated failure; `root-timeout-cancellation.json` in the artifact retains intent and pre-cancellation state. The harness exited with the run cancelled and zero active agents, and the owning dev session was stopped with all ports clear.

The next correction shares fresh receipt and inspection proofs within one validation pass, preserves exact source/candidate checks, and prevents aborted reads from persisting stale conclusions. It must pass regressions and a provider-free probe against the actual retained native files before a new native V3 attempt. That probe will use a copied real SQLite store and explicit intercepted core boundaries; it will not be presented as provider acceptance. Source identity, budget and evidence requirements are unchanged. Phase 4 remains incomplete, followed by its remaining serial non-Git/rule-change/performance gates and later Context, Factory, appearance and public release phases.

## Alignment checkpoint 25

The validation correction passed 90 focused tests across nine suites, all four ARC typecheck tasks, scoped formatting/lint and diff checks. Logs: `.arc-verification/orchestrated-validation-regressions.log`, `.arc-verification/orchestrated-validation-final-typecheck.log`, and `.arc-verification/orchestrated-validation-scoped-lint.log`. New real-SQLite/compiler/internal-callback tests cover delayed repeated evidence under the actual deadline, movement after preparation, fresh busy-retry validation and abort-safe retained evidence. Earlier fixture setup failures and an overbroad cancellation assertion remain in the first through fifth test logs; the final assertion preserves aborted candidate receipts while allowing independently completed current observations.

The final-source provider-free probe reached intercepted preparation in 1,368 ms and completed its callback in 1,373 ms under the unchanged 10,000 ms deadline: `.arc-verification/orchestrated-validation-probe.2026-09-10T12-53-55-257Z.final.json`. It used a copied retained ARC SQLite store and actual Git/filesystem inspection of the original, failed and final candidates, each once. All seven before/after workspace states were unchanged; no unexpected mutation method, core main preparation or provider call occurred. The earlier in-progress probe and root-resolution import failure remain separate. This is preflight timing and evidence validation, not native provider acceptance.

The V3-only harness now accepts absent reviewer provisioning only when read-only core records prove the exact prepared request, execution context/input/provider/native request and already-ready environment, plus an earlier same-run worker's core-backed completed provisioning for that environment. Actual provisioning attempts must still complete before acceptance. The retained native artifact passed the three-new/one-reused offline cases; altered context, input and missing prior proof were rejected. V2 behavior remains unchanged. A fresh protocol-186 development session is starting under `.arc-verification/arc-orchestrator-coalesced-dev-186.log` for the next full native gate, with source frozen for that window.

## Alignment checkpoint 26

The fresh native main-composer acceptance passed at `2026-09-10T13:07:00.448Z`: `.arc-verification/orchestrator-runtime/2026-09-10T12-55-58-745Z-2124c987/result.json`. Run `run_ccc48de5-16df-488a-a78b-44d65cf79a4b`, workflow `wfo_e7676fc2-61ff-41a2-b675-057cdb2ae2e3`, settled all 26 effects with five charged agent calls and zero active agents. Admission came from the actual main conversation's native tool, pinned preferred/restricted team revision 1 while revision 2 existed. An actual owning server/daemon restart changed generation 1 to 2 with the same zero-call pending approval, control `wfe_0080f451-fc33-4b28-ab1f-e6a18ab29b3a`, context hash `8c565228915d7ff8ea8a5b2b4b4a632bae1aba8255e2f444e09b48f538eb1c01` and revision 1. The resumed protocol-186 host instance was `2163a50c-d38f-448c-8fcb-2411781be07e`, session `hses_x83qx49t3e`; log: `.arc-verification/arc-orchestrator-coalesced-resume-dev-186.log`.

After browser approval, two native writers produced isolated candidates. The required check failed on the real 201-versus-200-cent defect; a fresh repair candidate passed repair and final checks. The fourth worker explicitly approved final commit `6cfd41048f4301a0b611871f63c912446f1252a1`. Original files/Git state and the retained failed candidate were unchanged, full candidate ancestry was preserved, and CLI/SDK inspection matched. New worker provisioning and the reviewer's already-ready environment reuse both passed their distinct core-backed evidence checks.

The fifth call was an actual prepared and completed response in main conversation `thr_3rfe4m45w7`, not merely a reserved budget slot. Effect `wfe_ebaf7387-e52c-4ddb-a2df-8bf1eafa7cb5` binds execution context `run-execution_893da7b1-3a72-4964-9603-83dbfa0861db`, request `creq_cchvx2upyn`, provider thread `01a08b63-bec5-7690-8fc0-bcc668fb6286`, turn `da83625e2f-t1`, accepted event `evt_vz6yvc3mq9` and terminal event `evt_qdj46p7i4k`. The harness verified the exact preparation/input/execution/receipt chain and no extra main tools or automatic turns.

Root inspected `completed-live-graph-workspace-1920.png`: one main composer with its completed response and four finished, team-colored worker panes. Four actual native-acceptance handoffs finished their 650 ms animations; reload replayed zero, and captured browser errors were empty. Independent full evidence audit passed, retained beside the result as `independent-audit.md`, `independent-audit.json` and `independent-core-completion.json`. It separately accounts for the setup and manual admission turns, verifies all receipt hashes and current Git/files, and records five provider warnings. This closes the native V3 reproduction gate; the earlier three failed attempts remain recorded. It does not close all Phase 4 or certify every provider, combined graph/chat performance, or a packaged Windows release. The owned development session was then stopped and all three ports verified clear before ending the source freeze.

The next implementation stays in Phase 4: serial non-Git projects using run-owned copies, exact directory manifests and the existing Workflows scheduler. Read-only host/runtime proposals are `.arc-verification/non-git-host-contract-proposal.md` and `.arc-verification/non-git-runtime-partition.md`. They identify additive directory contracts, explicit unsupported graph/filesystem diagnostics and pending fresh-validation support for scans longer than an owner callback. No non-Git production or acceptance pass is claimed by these proposals. Reviewed active-rule changes, remaining autonomy/delegation and accessibility/performance gates, and Phases 5–8 remain required.

## Alignment checkpoint 27

Phase 4 serial directory implementation is now in progress. V4 adds explicit source inspection, a single-use inspection bound atomically to the saved request, retained run-owned snapshots, serial graph validation and a counted response in the original main conversation. V1–V3 definition/compiler compatibility fixtures were captured before graph-lowering extraction and passed again afterward. The new directory compiler and existing graph/main compiler suites passed 34 tests: `.arc-verification/directory-compiler-initial-green.log`. Initial source/setup services passed 11 tests using real migrated SQLite: `.arc-verification/directory-source-setup-second.log`.

Host protocol 187 adds typed directory operations to the existing native journal. The final directory suite passed 21 actual Windows filesystem/process tests; the existing 20 Git tests passed within a combined command whose separate directory cleanup failure is retained, and 38 protocol tests passed. Exact evidence and bounded coverage are in `.arc-verification/non-git-host-verification.md`. This includes hidden/dependency files, empty directories, Unicode/short paths, links/junction refusal, complete manifest limits, changed checks, ownership/fences and crash recovery. No new native V4 application run or Windows package pass is implied.

Workflows pending validation retains terminal evidence while exact scan IDs are acknowledged, quiesced and rechecked. The recovered `.arc-verification/workflows-validation-full.log` passed all 293 tests in 20 files. The resumed Workflows typecheck passed in `.arc-verification/workflows-validation-typecheck-resumed.log`; subsequent recovery review is still active. The earlier integrated ARC typecheck failed with 53 concurrent integration errors and is preserved in `.arc-verification/non-git-host-typecheck.log`.

The first resumed product suite, `.arc-verification/directory-product-integration-first.log`, passed 27 tests, failed one UI assertion and could not load three suites because the directory runtime adapter was still being implemented. The UI assertion counted repeated reads of one retained inspection as separate scans; the corrected test checks the operation identity. Final verification views now use exact retained proof and an explicit last-verified timestamp, without starting unacknowledged scans. Runtime integration, current product regressions, actual UI inspection and native folder acceptance remain open. The plan and delivery order are unchanged.

## Alignment checkpoint 28

The integrated product suite passed 98 tests across eight files in `.arc-verification/directory-product-integration-second.log`: source setup, folder inspection, exact saved-request retry, graph admission, native tool exposure, orchestration context and run UI. ARC typecheck then passed all four Turbo tasks in `.arc-verification/directory-runtime-integration-types-second.log`. These are integration evidence at those source points, not native folder acceptance.

Independent Workflows review reproduced a restart deadlock when a completed non-gate branch was skipped by replay but its older-generation proof still blocked admission. Targeted maintenance now refreshes those receipts, and final completion waits even when no new native node remains. The full package passed 297 tests in 20 files plus typecheck in `.arc-verification/workflows-validation-integrated-green.log`. A later review identified additional pause/lost-reply cases, so new validation-request intent and quiescence amendments are under test before another full pass. Exact earlier evidence is `.arc-verification/workflows-pending-freshness-evidence.md`.

Root reproduced simultaneous identical folder admission incorrectly rejecting the second request with `directory_inspection_used`. The service now rechecks the exact retained request inside the same start-control transaction before consuming its inspection. The regression confirms one ARC row, one Workflows row and identical submission payloads; a different request still cannot reuse the inspection. Seven real SQLite proof tests cover complete scan sets, exact consumer identity, missing/foreign proof IDs, changed path/root/digest, quiescence and immutable receipt preservation. These and the admission/UI suites passed 48 tests in `.arc-verification/directory-evidence-and-concurrent-admission-green.log`; the initial failure remains in the matching `-first.log`.

The V4 native harness is prepared at `scripts/arc-directory-runtime-smoke.mjs`; syntax/import/lint and offline compilation of its actual serial team passed. Its outside-Git fixture, real provider calls, restart, retained candidate comparison, main response and UI/CLI checks are described in `.arc-verification/non-git-native-acceptance-plan.md`. No V4 services, projects or provider turns have been started. Native acceptance waits for pending runtime recovery regressions; the original plan, source/profile separation and later phases remain unchanged.

## Alignment checkpoint 29

Independent recovery review closed concrete lost-reply and pause gaps before any V4 native provider run. Workflows now persists an exact unresolved validation-request ID and generation before a callback can start scans; late replies cannot clear newer intent. Pause/cancel waits for confirmed quiescence, including when a new scan starts after an acknowledged current proof but its reply is lost. The full Workflows package passed 301 tests before one final unknown-first-reply extension; all 27 focused validation tests and typecheck then passed. `.arc-verification/workflows-pending-freshness-evidence.md` and `.arc-verification/directory-workflows-integration-review.md` retain the exact checks and source review.

The host journal is now version 3. Only new read-only scans retain owning-process identity, and their metadata checks run entirely in that process. Recovery requires actual local completion or OS evidence that the owner no longer exists; it produces an interrupted receipt without an inventory. Live/PID-reused/legacy owners and unknown copies/checks remain unresolved. Four real subprocess/filesystem/journal recovery tests passed, plus the 21 current directory inventory/effect tests. `.arc-verification/non-git-host-crash-recovery.md` records evidence and initial fixture-launch failures. The full 20 Git effect tests still require a pass under journal version 3.

The product adds typed `resolveDirectoryRunControl` checking/resolved results. UI approval polling preserves one exact user decision, stops on unmount or observed pause, and displays verification progress. SDK/CLI use the same request and result; the earlier control method remains compatible. A fresh read-only inspection is available after a permanent saved-setup conflict, and the setup budget now says agent calls including the main response. The updated product suite passed 80 tests across five files in `.arc-verification/directory-decision-product-tests-first.log`.

Final run inspection requires the retained validation to cover both the exact original source and final candidate. A real SQLite service test proves that reading a run starts no host scans and rejects a same-effect proof that omits its candidate. The first attempt lacked its test internal transport; the fixture now uses actual Workflows SQLite creation/inspection and passed in the broad runtime run. That command passed 206 of 208 tests; its only failures were the two newly added conditional-branch fixture assertions. The corrected directory driver suite then passed all 13 tests in `.arc-verification/directory-adapter-final-gates.log`, including both condition branches, selected joins, serial delegation, repair and counted main completion. These use the test native boundary and are not real-provider acceptance. Current full ARC/Workflows compatibility, live browser/provider acceptance and the remaining original Phase 4 gates are next.

## Alignment checkpoint 30

The full current preflight passed: ARC 387 tests in 40 files and Workflows 302 tests in 20 files, with all five Turbo tasks successful in `.arc-verification/directory-full-preflight-tests.log`. This includes all 20 actual Git effect tests under journal version 3, all 25 directory inventory/effect/crashed-owner tests, legacy compiler snapshots, branch/delegation/repair execution and product approval behavior. The earlier two conditional failures were test assertions that incorrectly treated hashed runtime node IDs as graph node IDs; both actual test runs had completed successfully before that assertion correction. Typecheck passed all five tasks in `.arc-verification/directory-full-preflight-types.log`.

The V4 native harness now also requires the public last-verified timestamp to match the exact selected retained original-and-final-candidate proof. Syntax, source imports, scoped lint/format and its actual graph's offline V4 compilation pass. Harness SHA-256 before launch is `65b5a8648de82377323d51dd9b7aebbcfd4b8a89d667b78194310aa89f03dbe3`. Source is frozen for the next native protocol-187 acceptance window; no V4 provider result is claimed yet. The required run will use an ordinary folder outside Git, restart the same isolated ARC development profile while paused, and verify real serial workers, failure/repair/review, counted main response, complete unchanged original inventory and actual browser/CLI behavior. Remaining Phase 4 rule-change/autonomy/accessibility/performance gates and Phases 5–8 remain open.

## Alignment checkpoint 31

The first V4 native attempt is a retained failure, not acceptance: `.arc-verification/directory-runtime/2026-09-10T14-49-34-267Z-c5a36700/result.json`, run `run_05b9c568-5f62-4aa7-9026-60863bfeeb66`, workflow `wfo_44740187-d596-4588-8db3-aeb3487415da`. Native main admission pinned preferred/restricted team revision 1 while revision 2 existed. A real owning server/daemon restart retained the zero-call approval `wfe_a5bf1846-37c1-4f4d-8195-ff44fa395312`, context `433484f8032895afe37875452213089fe68c10ef4edf9b7ad0c7e4d77fa84496`, revision 1, while dispatch generation increased from 1 to 2. Actual browser approval succeeded. Independent admission/restart audit passed in the artifact directory; root visually inspected the saved approval screen. Startup/resume logs are `.arc-verification/arc-directory-native-dev-187.log` and `.arc-verification/arc-directory-native-resume-dev-187.log`.

The native builder rendered `200 cents` in the order-total element. Its sealed task asked for a total "in cents" without requiring digits-only content, while the fixture check required exact `>200<`. That check therefore failed before reaching the planted pricing defect. The actual repair obeyed its pricing-only scope, and the recheck still failed on the frontend mismatch. The run settled failed with 15 effects, three charged calls, zero active agents and `Required gate did not succeed: team:final-verification`; no reviewer or successful final verification was claimed. The third call was the retained main failure response. The independent briefing finding preserves exact instructions and untruncated failure output. This calls for clarifying the fixture's builder task, not weakening checks or changing production behavior.

The harness exited with the failure and the owning development session was stopped; all three ports were clear. The source comparison `.arc-verification/directory-native-source-first-check.json` found only the normally regenerated official-marketplace JSON changed among 5,079 captured files; the other 5,078 matched. Freeze ended for the bounded harness instruction correction. A fresh native attempt will retain the strict numeric check and pricing-only repair contract. The approved phase and later gates remain unchanged.

## Alignment checkpoint 32

The corrected V4 native run passed at `2026-09-10T15:13:40.899Z`: `.arc-verification/directory-runtime/2026-09-10T14-58-50-745Z-7bbf043a/result.json`. Run `run_331d93db-b19d-4404-bb9f-9008bde90b5b`, workflow `wfo_d0e83407-5543-424e-af54-557054c4b2f4`, settled 21 effects with four charged calls and zero active agents. The strict check first caught the intended 201-versus-200-cent pricing defect; a fresh repair copy passed its recheck and final check. The third worker explicitly approved snapshot `snapshot_a1dd712f9bdc3cc1b813402b81f2677e`, manifest `5c9f3998cee0a9b1868a706d22a12e6c6eb14ec0c2dc0212decdb14ba4b3fe21`. The original full inventory, failed candidate and all retained snapshots remained unchanged; only the assigned builder files and pricing repair changed in their respective outputs.

Native main admission pinned preferred/restricted revision 1 while revision 2 existed. The actual owning restart changed dispatch generation 1 to 2 while preserving zero-call approval `wfe_cec9a75f-4f39-48c3-a540-c0e796206e1a`, context `37e824ec762e29f2e0bf1f6d3a8feeb5f5733305431b69c5c32851773307898b` and revision 1. The initial resume read arrived four seconds before Workflows loaded and returned HTTP 500 without a resume intent. Immutable failed/recovered reports and `premature-resume-recovery-audit.json` retain that event. Before retry, actual SDK and SQLite checks proved the same paused run, only the original pause operation, unchanged original and zero calls. Artifact recovery preserved the failure in explicit retry history; it made no run or provider mutation. The retry passed actual browser approval and full execution in the same run.

The fourth call completed in main conversation `thr_uf3mzq6i3w`: effect `wfe_53d0b3f5-d6df-4b91-974d-c2b6e2795860`, execution context `run-execution_5e90cac5-4d80-425c-b9a1-970ea7e78f86`, request `creq_4m3k2tmif8`, provider thread `01a08bd4-4ca5-72f3-9357-8d350170d037`, turn `da30ee10ef-t1`, accepted event `evt_a4i2gnpzfn` and terminal event `evt_tqyiwiq7r7`. Independent core inspection counted exactly six native requests: two manual setup turns plus the four admitted calls. Final verification uses effect `wfe_cc76fbd3-6cee-4a57-8f25-740de085b71a`, validation `arc_validation_9128330a-0bd7-4c02-a7c1-a189cb17bb5d`; the public timestamp exactly matches the retained complete original-and-candidate proof at `2026-09-10T15:13:09.386Z`.

The independent final audit passed against current files and read-only core/ARC SQLite, and is retained beside the report as `independent-final-audit.json` and `.md`. SDK and CLI match; three actual accepted handoffs completed, reload replayed zero, three child notices remained passive and browser errors were empty. Root inspected `completed-live-graph-workspace-1920.png`: one main composer, the completed response and three team-colored, model-labelled worker conversations. All 5,079 captured source files matched throughout the second attempt: `.arc-verification/directory-native-source-second-check.json`. The owning dev session was stopped after audit and all three ports cleared before ending the source freeze. Logs: `.arc-verification/arc-directory-native-second-dev-187.log`, `.arc-verification/arc-directory-native-second-resume-dev-187.log` and `.arc-verification/directory-native-resume-second-retry.log`.

This closes the bounded native Codex ordinary-folder gate. It does not close all Phase 4, large-project or combined 200-node/four-live-chat performance, other provider variants, linked dependency support, current Windows packaging or later phases. No production code was changed for either native fixture/retry correction. The original plan remains the scope reference.

Documentation closeout updates the current-phase summary, graph/team/folder contracts, configuration and agent-facing skill/overview to the observed acceptance status. The overview remains below its 4,000-character boundary at 3,985 characters. All 16 server/tool/documentation-surface tests and four Turbo tasks passed in `.arc-verification/directory-documentation-surface-closeout.log`; scoped diff checks passed. The inspected completed Workspace screenshot is available beside the native result. The development service remains stopped.

### Alignment checkpoint 33 — reviewed instruction continuation implementation

The next Phase 4 slice follows the approved immutable-revision rule: publication leaves active work unchanged; an explicit reviewed update pauses affected work and invalidates prior results as authority. The implementation uses linked immutable runs in the existing Workflows scheduler. This first slice accepts agent instruction-body changes only. Broader operational-rule changes, remaining autonomy/delegation variants and the combined 200-node/four-live-chat gate remain open.

Workflows continuation reservations and cumulative usage transfer passed 314 real-SQLite/runtime tests across 21 files and all six test/type Turbo tasks in `.arc-verification/workflows-continuation-full.log`. ARC backend integration, exact-diff review UI, retained Apply/Cancel identity, read-only lineage, migration wiring and SDK/CLI discovery are in progress. These additions are not yet native acceptance. A nullable missing-reservation inspection refinement is being verified before final integration. The earlier first ARC typecheck found one backend narrowing issue and four UI test option errors; fixes are in progress, with no passing ARC claim yet.

### Alignment checkpoint 34 — connected review UI and recovery checks

The instruction review is connected to Run details with exact old/new bodies, affected steps, original-source identity and the whole-team rerun consequence. Workspace and Run details expose lineage and label replaced candidates as historical. Ordinary controls and approvals are blocked while an update owns the run. Typed SDK operations and `bb arc runs rpc` discovery, the CLI guide, configuration reference and agent skill ship with this UI.

The integrated UI/CLI run passed 61 tests across six files and all four Turbo tasks in `.arc-verification/instruction-update-ui-tests-third.log`. This includes nine instruction-update UI regressions: explicit review without auto-apply, exact retained Apply/Cancel requests, unmount cleanup, stale control versions, sealed cancellation refusal, lineage and a fresh review after read-only confirmation of a lost cancellation response. The first run found a real sibling-key collision between instruction and approval panels; it was fixed before the pass. Two accompanying test setup mistakes were corrected. No UI/native pass is inferred from backend tests.

ARC's first 13 backend continuation cases passed in `.arc-verification/instruction-update-tests-third.log`. Independent review corrected cancellation before Workflows seals a successor and consumption of the V4 source inspection. Workflows' nullable missing-reservation inspection passed 22 focused tests plus typechecking; the prior full314 result predates that added regression. The first combined preflight stopped on a nullable native-resource type in a new test, now fixed. A supplemental source inspection for interrupted pre-seal retries is being completed and tested before the final integrated gate and source freeze. Phase 4 remains open; no native launch for this slice has occurred.

### Alignment checkpoint 35 — full regression pass and stricter repair-stage limit

The integrated preflight passed all 411 ARC tests across 42 files, all 315 Workflows tests across 21 files and both package typechecks. Evidence: `.arc-verification/instruction-update-full-tests.log` and `.arc-verification/instruction-update-full-types.log`. This includes all 20 real Git host checks, directory filesystem/recovery coverage, V1 compatibility, 14 backend continuation cases and nine instruction-update UI cases. The final pre-seal recovery patch performs a new durable source inspection when Workflows has not accepted the successor, retaining only an unfinished inspection across polls and consuming its proof for the exact successor.

The prepared native harness passed eight offline gates with no service, provider or fixture mutation; evidence is `.arc-verification/active-instructions-runtime/offline-gates.json`. Its six-call/two-repair scenario is ready for actual UI Apply, provider interruption and restart recovery once source is frozen.

Before native launch, follow-on design review confirmed a current continuation gap: inherited repairs were enforced against the project-wide repair limit but not a repair node's lower compiled ceiling. The fix and a failing-before/passing-after regression are required before native acceptance. The preceding full pass is preserved as evidence of its tested state, not a claim that this newly identified case was already covered. No scope expansion into Factory, retrieval or appearance has occurred.

### Alignment checkpoint 36 — repair ceilings and native restart boundary

The repair-stage correction is complete. Admission now enforces inherited plus local usage against both the overall limit and each immutable stage ceiling; ordinary continuation rejects changed stage ceilings. Three new real-SQLite cases reproduced the failure before the fix in `.arc-verification/workflows-stage-ceiling-regression-red.log`. All 318 Workflows tests across 21 files and six test/type Turbo tasks passed in `.arc-verification/workflows-stage-ceiling-full-second.log`; 114 affected ARC tests across nine files passed in `.arc-verification/instruction-stage-ceiling-arc-regressions.log`. Scoped lint, formatting and diff checks passed. The earlier misleadingly named `workflows-stage-ceiling-full-green.log` contains a test-only narrowing failure and is not passing evidence.

The native instruction-update attempt reached its actual durable pause boundary at `2026-09-10T17:03:34Z`. Report: `.arc-verification/active-instructions-runtime/2026-09-10T16-59-31-235Z-31843120/result.json`; immutable boundary: `paused-boundary.json` beside it. Run `run_95805661-b347-485f-be99-7f399634fc38` retained two charged calls, one repair and zero active agents after real UI Apply operation `91456145-cd88-442b-a839-cb54ea927e60`. The interrupted native repair's finite child process was recorded separately as still alive; provider interruption is not described as OS suspension. No successor was admitted before restart.

Root stopped the owning development session, verified all three ports clear, and restarted the same isolated ARC profile. Actual ARC and Workflows SDK readiness passed at `2026-09-10T17:08:47.245Z`; the deterministic source inventory confirmed all 5,820 entries unchanged. Evidence: `.arc-verification/instruction-native-readiness-restart.json` and `.arc-verification/instruction-native-restart-freeze-check.json`. The first pre-start inventory check is retained separately: startup regenerated the official marketplace artifact, and two unchanged tracked scripts appeared in its `rg` inventory. The final freeze uses Git's tracked/untracked inventory, retains missing tracked paths and was captured after startup generation. Native continuation completion and independent final audit remain pending.

The next approved Phase 4 work is broader reviewed operational-rule application, followed by remaining native autonomy/delegation and combined accessibility/performance gates. Read-only preparation is retained in `.arc-verification/operational-rule-update-design.md`; it does not constitute implementation or native acceptance. The plan and later phases remain unchanged.

### Alignment checkpoint 37 — native instruction evidence and operational-rule implementation

The original instruction-update run was retired and successor `run_d3fbbd8a-df19-4f9b-93b5-91abe791f5f9` succeeded at `2026-09-10T17:19:16.994Z` with 21 effects and zero active agents. Its cumulative ledger retains two original plus four successor calls, one original plus one successor repair, and 169,993 original plus 554,041 successor milliseconds of active time. Exactly eight native requests exist including two manual setup turns. The successor repair received published agent revision 2 before its first turn and produced the reviewed instruction marker while fixing the pricing defect. The required check failed as intended, then repair and final checks passed. The real reviewer approved snapshot `snapshot_b7a844f607a110324565abd54e6067c9`, manifest `5a7c77f6bcc6af64adcfe5dcc8adbd5d65075aab7d72314bdd89ccf62fe8f53`; final verification's exact timestamp is `2026-09-10T17:19:04.418Z`. The counted main response completed with native terminal event `evt_9tdnem39j6`.

The native harness subsequently failed its raw-SQLite versus SDK event comparison because the actual stored-event parser supplies `systemMessageKind: "unlabeled"` and `systemMessageSubject: null`. The immutable failed report remains `failed-final-audit.json`, SHA-256 `7277c074b33af4ffe53e627bb9bcfc69b7c44d5b805399d31e3200d7ba3670ff`; the original driver was not edited. A separate read-only recovery used the actual canonical event parser and verified all 24 lifecycle events, current source/candidate inventories, old receipts, checks, exact review, cumulative ledger, SDK/CLI lineage and completed/reloaded UI. It made no Apply/Poll/Resume, provider or run mutation. Evidence beside the native result: `recovered-final-audit.json`, immutable `recovered-completed-boundary.json`, `recovered-final-audit.md` and four recovered screenshots. Recovery JSON SHA-256 is `87468281e123aa392df45d64d3c45273109ba216883552a135582122686e26e5`.

Root inspected the recovered completed Workspace and original history: BB dark, one main composer, three finished model-labelled/color-bordered worker panes, the repair's v2 identity, cumulative 6/6 calls, exact verification timestamp and linked historical predecessor. Recovery browser errors were empty and opening/reloading history replayed no handoffs. The original harness's unconditional `crypto.randomUUID()` instrumentation error was reproduced on `about:blank`; the actual localhost app exposes that API in a secure context. That original error remains recorded. Live animation arrays were lost when the first assertion closed the browser, so no start/finish/duration claim is made for this attempt. Both recovery and independent audit deliberately retain `overallAcceptanceComplete: false` for that missing gate. The later native variant must retain animation observations incrementally.

The independent audit passed all 11 native functional gates against the stopped native profile, immutable reports, current fixture files and the final source boundary. It verifies one atomic continuation, five first-turn worker bindings, eight unchanged original receipts, complete inventories, exact checks/review/main proof and canonical stored-event equality. Evidence: `independent-audit.json` and `.md` beside the native report. The owning dev session was stopped, all three ports cleared, and all 5,820 captured source entries matched at `2026-09-10T17:27:49.296Z`: `.arc-verification/instruction-native-final-source-check.json`. The source freeze ended after that boundary; subsequent implementation is not part of this native evidence.

The next Phase 4 implementation uses the existing update ledger, source verification, cancellation and Workflows continuation engine for reviewed changes to policy, limits, checks, gates, collaboration grants and resolved agent execution. Existing instruction RPCs and retained requests remain compatible. A unified status read guards both update kinds. Purely cosmetic, preferred-team or effectively masked settings changes do not pause or restart a run. Contributor-based review enforcement closes an existing gap between displayed Team review grants and actual runtime authority; it preserves authors through serial, merged and repaired candidates. Legacy raw receipts and valid compiler bytes remain unchanged; newly attempted ungranted review work is blocked with a clear diagnostic. Source/contracts and UI integration are in progress, with no operational-rule test or native pass claimed yet.

### Alignment checkpoint 38 — operational-rule backend and grant enforcement

Reviewed operational rules now share the existing immutable instruction-update ledger, pause/cancel recovery, verified-source continuation and Workflows scheduler. Typed previews distinguish a restart from a blocked or ineffective change. Successors retain used calls, active time and repair rounds against the reviewed total limits. An appended Workflows migration preserves a complete repair-stage catalog, including disabled stages, across amended and ordinary continuations. Original instruction request shapes and stored request hashes remain compatible. No daemon wire change or ARC migration is needed.

The full current Workflows suite passed 346 tests across 22 files, including 27 rule-continuation SQLite cases and 11 existing-worker cases, with no source fix required during that full gate. Evidence: `.arc-verification/rule-workflows-full-tests.log` and the passing four-task typecheck `.arc-verification/rule-workflows-final-types.log`. Tests include old sealed-row migration, exact method/request replay, cumulative limits, actual retirement receipts, source parsing before sealing, transaction rollback and deterministic catalog ordering. Root independently reviewed catalog admission and both continuation paths without finding a concrete defect.

ARC's rule/instruction backend and graph admission passed 67 tests in `.arc-verification/rule-backend-focused-second.log`; typecheck passed all four tasks in `.arc-verification/rule-backend-types-second.log`. Coverage includes policy version changes during asynchronous review, limits below actual consumption, disabled repair stages carried through successive continuation kinds and reviewed instructions/execution bound before the successor's first prepared turn. Initial test failures and their fixture corrections remain in the first log.

Contributor-based review enforcement passed 108 focused cases across the retained suite results documented in `.arc-verification/review-grants-enforcement-result.md`; final ARC typecheck passed in `.arc-verification/review-grants-types-final.log`. This covers declared contributors through serial, integrated, delegated and repaired candidates, V4's exact materialized review input, runtime preparation and verdict checks, physical verification recovery and unchanged legacy compiler snapshots. Intermediate failed test attempts are preserved. A separate source review found no concrete permission or quiescence defect.

The product now has a unified status/lineage read and separate instruction/rule editors using the same retained-request recovery behavior. The rule review explains authority changes, exact checks/execution, used and remaining allowance, no-op/blocked outcomes and the whole-team restart. SDK/CLI discovery and configuration/agent guidance are updated in [ARC-RULE-UPDATES.md](ARC-RULE-UPDATES.md). UI tests and independent integration review are still running; no current native rule-update or rendered-UI pass is claimed. Native preparation targets a real 7-to-6-call and 3-to-2-repair update with active interruption and restart, and will persist animation observations incrementally. Development services remain stopped. Remaining Phase 4 autonomy/delegation and combined performance/accessibility gates, followed by Phases 5–8, remain open and unchanged.

### Alignment checkpoint 39 — current regression coverage and frozen rendered UI

Independent UI review corrected ambiguous permission labels when multiple Team members reuse one Agent, and corrected rule-update history being labelled as an instruction update. Grant endpoints and delegation choices now retain member identities; other authority-sensitive stage labels include their exact stage IDs. Revision paging preserves the selected revision visibly instead of showing an unrelated option. The CLI regression found public polling performed an update lookup before its user-only check; both polling entry points now enforce that check before lookup or queueing. No worker was allowed to mutate the run through the old ordering. The boundary/backend rerun passed 46 tests in `.arc-verification/rule-product-boundary-second.log`.

The full ARC command ran all 501 tests in 47 files: 492 passed, eight failed because an existing positive delegation fixture lacked the newly required review grants, and one UI assertion omitted the adjacent candidate digest. The complete failure log remains `.arc-verification/rule-arc-full-tests-first.log`. Explicit reviewer grants were added only to the positive fixture; its rejection/slot/candidate assertions remain unchanged. All 21 control-driver cases then passed in `.arc-verification/rule-control-driver-second.log`, and all 10 rule UI cases passed in `.arc-verification/rule-product-ui-second.log`. Together these cover all 501 tests at the final source state; the seven-minute full command was not repeated for two test-only corrections. This includes all 20 actual Windows Git effect cases and 25 directory filesystem/recovery cases. Joint ARC/Workflows typechecking passed all five tasks in `.arc-verification/rule-final-types.log`; the full Workflows 346-test pass remains applicable. Scoped product lint returned no errors and 11 effect/state synchronization warnings in `.arc-verification/rule-product-lint.log`; formatting and diff checks passed.

The owning ARC development session was started with the same isolated checkout profile. The first read-only readiness probe arrived before Workflows loaded and returned `HTTP 500: Internal plugin RPC generation is unavailable`; `.arc-verification/rule-native-readiness-first.log` retains it. The confirmed new mixed-lineage and review-authority reads plus actual Workflows run inspection passed at `2026-09-10T18:11:42.887Z` in `.arc-verification/rule-native-readiness-confirmed.json`. No run control or provider request was issued by readiness probes.

Source is frozen in `.arc-verification/rule-native-source-freeze.json`. All 5,837 captured entries and HEAD matched after rendered checks at `2026-09-10T18:15:18.069Z`: `.arc-verification/rule-native-source-render-check.json`. Current BB-dark controls were exercised at 1366×768, 1920×1080 and 2560×1440, with completed-run Apply disabled, legacy instruction lineage preserved and a completed successor Workspace displayed. The settled evidence is `.arc-verification/rule-product-rendered/2026-09-10T18-14-06.356Z/result.json` plus five screenshots; root visually inspected the laptop controls and Workspace. No browser errors, update mutations, app-root inert/hidden state or document horizontal overflow were observed. The first screenshot set captured an intermediate settings-loading state and is retained separately; the settled run explicitly awaited current settings. These are rendered read-only checks, not a live operational-rule acceptance pass.

The native rule harness is under final artifact review. It will lower seven total calls to six, three repair rounds to two, and the repair stage ceiling from three to two, alongside the explicitly displayed repair-body revision. Checks and review grants stay exact. An offline browser probe proves the new recorder persists animation starts/finishes before later failure/closure, but it is labelled synthetic instrumentation rather than ARC handoff evidence. Native launch/restart/final audit remain pending. Read-only preparation for the remaining combined Workspace/autonomy gates and Phase 5 offline Windows assets is recorded in `.arc-verification/phase4-remaining-acceptance-design.md` and `.arc-verification/context-windows-feasibility.md`; neither is implementation or acceptance. The delivery order and outstanding plan requirements remain unchanged.

### Alignment checkpoint 40 — native rule application and restart boundary

The reviewed native driver is frozen at SHA-256 `94a8c1af68f686558402e06279880cc90f7fcfa762c68dccd66787c5391c60be`. Ten offline gates passed in `.arc-verification/operational-rules-runtime/offline-gates.json`; the separate real-Edge recorder durability probe remains explicitly synthetic. Root and independent review corrected harness assumptions about an initial run's absent stored repair catalog and the current main-completion disclosure, then added exact persisted authorization/request binding and settled-interval active-time conservation. The original instruction harness and evidence remain unchanged.

Native evidence is under `.arc-verification/operational-rules-runtime/2026-09-10T18-21-41-123Z-0f4941c6/`. Disposable project `proj_wfiqfyvw3i` admitted original run `run_ff4c34e1-59ff-4960-bc96-a0933b84d85c` in main conversation `thr_37cdu7yfwm`. While the second native worker was active, the real no-op preview left its immutable snapshot/control/calls unchanged. Publishing the reviewed repair-body/team revision and saving lower limits likewise left active work unchanged. Actual UI review displayed seven-to-six total calls and three-to-two global/stage repair ceilings, used/remaining allowance and the exact instruction-body diff; root inspected `reviewed-operational-rule-diff.png`.

Actual UI Apply retained operation `b7fc9ef2-116f-43b6-ac75-8e94d8df5d37`, preview `rule_preview_416b3ee9-f0cc-407c-8536-8f3e4f1dc17f` and hash `995a5a4b5305e4e85ffab8a7ae8196553cca34093bfb6f505240a0f2f8e13074`. The original reached paused at `2026-09-10T18:24:38Z`: two calls, one repair, zero active agents, 135,930 ms charged active time, control version 1 and dispatch generation 1. The immutable boundary is `start-apply-awaiting-restart-boundary.json` beside the report. Native interruption is observed separately from the OS child-process inventory; no suspended-token/process claim is made.

Root stopped the owning development launcher, verified all three ports clear and all 5,837 source entries unchanged, then restarted the same isolated ARC profile. Actual ARC/Workflows readiness and the unchanged source check passed before continuing: `.arc-verification/rule-native-readiness-restart.json` and `.arc-verification/rule-native-source-restart-check.json`. Independent read-only boundary audit passed six checks after restart, retaining generation 2 with the same usage, no unresolved validation, unchanged original files/receipts, an unsealed application and no ARC or Workflows successor. Evidence: `independent-boundary-audit-1789064764374.json`, SHA-256 `89c5ef4ce77f2324b601848acf93c7867595a71b3e919cecc68fec437011a327`. Its separate OS observation found no held child process.

Retrying the exact saved operation admitted successor `run_77682ece-7668-494d-a084-ff31084e16c8` at `2026-09-10T18:26:50Z`, retaining two used calls and one used repair against the newly reviewed six-call/two-repair totals. A fresh actual UI approval preceded its first new worker, observed at cumulative call three. Logs are `.arc-verification/rule-native-start-apply.log`, `.arc-verification/rule-native-resume.log`, `.arc-verification/rule-native-dev.log` and `.arc-verification/rule-native-restart-dev.log`. The successor's completion, final exact-candidate audit and durable live handoff proof are still pending. Source remains frozen; the remaining Phase 4 and later phase gates are not closed by this boundary.

### Alignment checkpoint 41 — completed native rules and Workspace graph integration

The native operational-rule successor succeeded at `2026-09-10T18:35:27Z` with 21 effects and zero active agents. The original's two calls plus four successor calls exactly reached the reviewed six-call total. One original and one successor repair reached the reviewed two-round total. All active-time intervals settled: 135,930 inherited plus 473,814 local equals 609,744 cumulative milliseconds. Eight unique actual provider requests include the two explicitly disclosed setup turns. No budget was reset by Apply or service restart. The first required check failed on the planted pricing defect, the revised repair fixed it, and repair/final checks passed. The native reviewer approved snapshot `snapshot_0d503d7dd16947bd30c443cfe66c2e7c`, manifest `b4b056c1a81dbb5d0a1dff6df0ac106960d8fab0cac12cfa5ff94b77762642a7`; final physical verification is timestamped `2026-09-10T18:35:13.888Z`. The counted main response completed against that evidence.

Evidence under `.arc-verification/operational-rules-runtime/2026-09-10T18-21-41-123Z-0f4941c6/` includes the passing result, immutable `resume-completed-boundary.json` (SHA-256 `e8db728415062c614b9767a4ba530e785a7ce83715dcd5420970b9a42e22c77d`), native transcripts, source/candidate inventories, checks, exact review, saved SDK/CLI responses and incremental `handoffs.jsonl`. Three real successor handoffs each started and finished once in the foreground Workspace: configured duration 650 ms, observed durations approximately 659–661 ms. Reload replayed zero handoffs; browser errors were empty. Root visually inspected completed and reloaded screenshots: BB dark, one composer, actual colored/model-labelled worker transcripts, revised repair instructions, correct cumulative usage and operational-rule history.

Independent final audit passed five gates in `independent-final-audit-1789065434031.json`, SHA-256 `7c907bd818fc1bff0a82ae9815ddbb2c01b3426f695aa4f24f9b3de5fe5b3c33`, with summary `independent-audit.md`. It verified one exact sealed successor, 24 canonical lifecycle events, five definitions bound before native preparation, preserved original files/receipts and five snapshots, both final-check and main-response validation bindings, all 706 targeted host effects terminal, and no held OS child process. The first audit-only assertion incorrectly expected `operationId` inside directory-agent receipts; its failed artifact remains, and the corrected check uses actual persisted preparation identity. No production defect or source edit was needed.

The owning dev session was stopped, all three ports cleared, and all 5,837 source entries plus HEAD matched at `2026-09-10T18:38:07.060Z`: `.arc-verification/rule-native-final-source-check.json`. Source edits resumed only after this boundary. Current integration adds an inspection mode to the existing Team canvas, exact compiled stage-to-worker projection, saved-plan search/details and a bounded graph region beside existing transcripts. Opening/collapsing the graph must preserve chat instances and an unsent composer draft. The combined 200-stage/four-live-worker fixture and remaining native autonomy/delegation checks are being prepared; they have not passed yet. This closes the bounded rule-update native gate, not all Phase 4 or later phases.

### Alignment checkpoint 42 — connected graph inspection and focused verification

Workspace now includes optional saved-plan inspection using the existing Team canvas and exact immutable Team/member revisions. The canvas inspection contract forbids an edit callback and disables movement, connection/reconnection and deletion. Search reaches all declared stages, connections and groups; selection shows pinned identities, configured checks and collaboration permissions, exact available attempts and missing-history disclosures. Worker `graphNodeId` is projected from the retained compiler origin for V2/V3/V4, including repair rounds and delegated child slots; V1 and missing mappings return explicit null. Existing compiled data is reused rather than reloaded for each worker. SDK/CLI and discoverable guidance describe the same projection.

The graph sits above worker panes beside the main composer. Desktop retains up to four worker panes; laptop has selected-chat and vertically scrollable Worker overview modes. A visible count distinguishes displayed from loaded conversations. Opening/hiding/resizing the graph preserves visible chat instances and the unsent composer draft, including compact graph focus. Hiding the graph closes its persistent details drawer and returns focus. Layout/member maps remain stable across repeated RPC parsing. Saved-plan stages are not painted with inferred execution outcomes from an incomplete worker window.

Independent source review found and corrected three interaction/identity defects: React Flow's paired node/edge callbacks could erase the next selection, reopening retained details could skip focus restoration, and canvas labels/search did not perform the same exact member pin check as details. Regression coverage now includes both paired-selection directions, unrelated deselection, compact hide/reopen/Escape focus, and mismatched agent ID/revision. Existing editor behavior remains covered. Reduced motion suppresses double-click animated zoom; selection, focus and fit use zero-duration navigation.

The final focused set covers 64 distinct passing cases: 15 actual-SQLite Workspace projections, nine Workspace layout/draft/identity cases, 12 graph-inspection cases, seven canvas-interaction cases, 16 canvas-layout/editor cases and five Team Builder cases. Evidence: `.arc-verification/workspace-graph-projection-view-second.log`, `.arc-verification/workspace-graph-review-fixes-tests.log` and `.arc-verification/workspace-graph-ui-second.log`. Final typecheck passed all four tasks in `.arc-verification/workspace-graph-final-types.log`. Earlier failures remain recorded: four new fixture admissions passed whole manifest steps instead of exact node/iteration references, one graph test selected the wrong canonical edge, and five test locators supplied a Playwright-only option to Testing Library. All were corrected without weakening expected behavior. Scoped formatting/diff checks passed. Ordinary root lint found no errors and one existing effect/state warning; an attempted type-aware lint command lacked `tsgolint`, so its failed log remains and no dependency was added. TypeScript verification completed separately.

Actual rendered application checks are starting; no new browser or combined live-performance pass is claimed yet. Artifact-only preparation validates a connected 200-stage Autonomous fixture with four real writers, one explicit observation gate, integration/check/review and six bounded owned calls. Its 189 failure-only diagnostic stages are inspectable but skipped on the successful branch; a separate pure failure witness reaches them and fails the required completion gate. This is compiler/fixture evidence, not native execution. The remaining Phase 4 native/autonomy/accessibility gates and Phases 5–8 remain required.

### Alignment checkpoint 43 — rendered graph and combined-test readiness

Read-only checks against the actual completed V4 successor passed at 1366×768, 1920×1080 and 2560×1440. Evidence is `.arc-verification/workspace-graph-rendered/2026-09-10T19-03-04.394Z/result.json` with six screenshots. Five open/close cycles retained exact worker DOM instances and the unsent main draft; saved repair details showed the pinned revision. SDK reads before/after retained the same definition, plan and call count. No provider request, advancing ARC mutation, browser error, outer horizontal overflow or app-root inert/hidden state was observed. Laptop Worker overview is scrollable; mounted workers are not reported as simultaneously visible.

The first rendered pass exposed a laptop canvas only 78 pixels high despite functional assertions passing. The graph now reserves at least 280 pixels including its header, removes a redundant visible run-ID row while retaining that identity in the heading title, and uses 40/65 percent normal/expanded proportions. The repeated actual screenshots show approximately 162, 215 and 359 pixels of canvas at the three target sizes. Root inspected the corrected laptop composition. These bounded layout changes follow the 64-case focused and typechecking evidence in checkpoint 42; they were verified through the affected rendered workflow.

Independent review of the upcoming seven-turn native harness found two launch blockers: cancellation must be followed through confirmed quiescence, including recovery of an admitted run after a lost start reply; and sustained transcript updates must be tied to actual verification output while all four desktop panes are visible, rather than counted from an initial event burst. Artifact-only corrections are in progress. No combined native workload has started. The next gate remains the 200-stage graph with four real concurrent workers; this completed-run browser pass does not satisfy it. Phase 5 packaging/context investigation may proceed read-only while the earlier acceptance remains open. The approved delivery order and remaining requirements are unchanged.

### Alignment checkpoint 44 — blocked native launch and Context packaging foundation

Both combined-harness review findings were closed before launch: exact saved-request recovery/discard fencing and bounded observed cleanup, plus fresh same-turn command-output pulses matched to visible terminal content for all four workers during one common desktop interval. The 200-stage/218-step pure fixture passed, with 29 effects on its successful branch and six owned calls; all 13 positive/negative streaming-evidence checks passed. The final driver SHA-256 is `8928a28967a949950792a1279812ce1b4fab9eda7fa4c0788222eae14117c0cc`. Independent review reported no remaining concrete launch blocker.

The actual seven-turn launch command was rejected before process creation by automatic approval review with only `blocked by policy`. No provider request, test project or run was created by that command, and it was not retried through another mechanism. The owning development session was stopped, ports 12008/20008/28008 cleared, and all 5,840 frozen source entries plus HEAD matched at `2026-09-10T19:24:42.702Z`: `.arc-verification/workspace-graph-source-final-check.json`. The source freeze ended at that boundary. Combined live performance and the other native autonomy variants remain unverified. Separate delegation preparation passed 12 compiler/QuickJS branch witnesses and five offline gates; its plan distinguishes zero calls at rejection from the existing counted failure-summary main turn. No delegation native run was started.

Independent foundational work proceeds under Phase 5A while that earlier gate remains open. The isolated `.arc-verification/context-runtime/` investigation acquired and rehashed the exact seven model files, then ran real local CPU/q8 inference and SQLite FTS5 through Node 24.14.0. Three prose/code/Unicode samples returned finite normalized 384-dimensional vectors with repeatability and zero fetch calls. This was not packaged or OS-enforced offline acceptance. A fresh dependency audit superseded the earlier Sharp 0.35.3 recommendation with the maintainer's patched 0.35.4; three moderate dependency entries still trace to installer-only adm-zip. Actual reports and the shipping-closure limitation are retained in that directory.

Implementation now adds a fixed private Context child/client, checksum-verified model/native staging, bounded input/queue/cancellation and package/smoke wiring. Host dependencies are pinned, source model checksums and upstream notices are committed inputs, and the build-time `context:assets` Turbo task passed. The staged Node runtime excludes ONNX installer scripts and their unused dependencies; its actual loading and package inventory still require verification. No public host/SDK wire capability or Context UI is claimed yet. Phases 5B onward must still connect incremental retrieval, document provenance and knowledge authority; all retrieval/scale/offline, Factory, appearance and release gates remain unchanged.

### Alignment checkpoint 45 — real Context bundle and distribution inventory

Phase 5A remains a private infrastructure slice. The client launches only its fixed sibling worker using the running executable, validates bounded versioned IPC and manifest identity, limits queue/batch/token sizes, and waits for actual child exit during cancellation/disposal. The worker checks the complete installed inventory before loading local CPU/q8 inference. Caller-chosen paths/modules and remote model fallback are unavailable. No public Context capability, retrieval index or UI is represented as shipped by this slice.

The first focused runtime command passed 15 cases with one unsupported-platform case skipped. The final worker-only rerun passed seven cases with that one platform skip, including a real initialized IPC child rejecting another generation: `.arc-verification/context-runtime/context-worker-second.log`. The single TypeScript return-inference error was corrected; host typechecking then passed, although its combined command still failed on filesystem-test cleanup. That failed combined log remains. Actual filesystem tests now pass four cases, covering ordinary/escaped paths, output and cache junctions, and detaching aliased bundle leaves before writing: `context-build-paths-fifth.log`. Earlier failed cleanup and an accidentally nested new test remain recorded and were corrected. The guard runs before the direct bundle entry can write, as well as before staging/cache operations.

Independent review corrected missing notices for components embedded in the Transformers Node bundle. Exact model/runtime metadata, original package metadata and upstream licenses are retained in the staged payload. The four tasks that produce or copy target-dependent Context assets are uncached because direct Turbo invocations do not guarantee a target discriminator; disabling only the upstream task would leave consumer cache reuse incorrect. Model acquisition still reuses files only after exact length/digest verification.

The actual host build passed six Turbo tasks, staging 189 files totaling 109,004,650 bytes. `staged-probe-first.log` records the real private client loading that bundle through Node 24.14.0 and returning finite normalized 384-dimensional vectors for prose, code and Unicode, followed by disposal. The observed manifest digest is `8c63250ba7618e8eea1cbc3f568698607ad78703d24143df662ee72770be1fe9`. Actual npm dry-runs using both distribution allowlists retained all 189 assets plus the manifest, including all 162 nested dependency files and eight native binaries: `.arc-verification/context-packlist-1789069887503/result.json`. This is stronger than a copy check, but is still not packaged Electron or clean-machine acceptance.

The coordinated Windows directory package is now being built through the explicit uncached `@bb/desktop#package:windows` Turbo task. Its forthcoming smoke must verify the exact packaged executable and payload, real inference/FTS5, corrupt/missing assets, token limits and owned-process cleanup. OS-enforced offline, retrieval quality/scale, document ingestion, knowledge governance and all later phases remain open. The blocked Phase 4 native launch has not been retried or replaced with another mechanism.

### Alignment checkpoint 46 — Windows package exposed a real asset omission

The first Windows package attempt stalled before executing tasks. A diagnostic dry run showed Turbo enumerating the broad plugin tree and then separately walking the excluded plugin dependency junctions. Removing only the redundant desktop-wide input was insufficient. The three plugin include/exclude globs were removed from the already-uncached bb-app payload task after reviewing its consumers: plugin content is rebuilt from source on every execution, cached desktop JavaScript keeps bb-app external, and payload packaging is uncached. The corrected dry run finished in approximately three seconds. The stalled commands were stopped; logs remain in `.arc-verification/context-runtime/context-package-dry-debug.log` and adjacent first/second/third reports.

The actual Windows directory package then passed all 15 Turbo tasks in `context-windows-package-second.log`. Its first Context smoke failed before launching any child: Electron's dependency collector had omitted the entire nested `context/node_modules` tree. Evidence: `context-packaged-smoke-first.log` and `C:/Users/Collin Chen/AppData/Local/Temp/ARC Context smoke Δ JCDLui/result.json`, whose process/case lists are empty. npm's successful listing did not prove Electron's different copy behavior. An explicit desktop resource-copy fix and actual copy regression are being implemented; this failed package is not accepted as a working Context runtime.

The final independent host typecheck passed all four tasks (`context-types-final.log`). The actual enrolled-host build passed all seven tasks (`context-enrolled-host-build-first.log`). All 189 asset bytes and the manifest match across host dist, full bb-app and the generated enrolled-host package; the actual enrolled-host npm listing contains all 190 Context files: `context-distribution-inventory.json`. Runtime/build/smoke scoped lint and formatting passed. The smoke also now includes positive real inference from its complete disposable Unicode/spaced-path copy before the negative cases, with canonicalized Windows temporary paths. No additional provider workload or native-team launcher was started. Phase 5A remains open until the corrected desktop artifact passes; later Context and release requirements remain unchanged.

### Alignment checkpoint 47 — packaged Context inference and cleanup verified

Electron now copies the complete Context payload through a dedicated `extraResources` file set, rooted at `host-daemon/dist` and filtered to `context/**/*`. Starting at the Context directory itself would still trigger the resource copier's special root `node_modules` exclusion. The ordinary application/dependency pass excludes Context to avoid duplication. Two regression cases execute the installed builder's actual matcher/copier for latest and nightly: 12 file byte sequences include nested/scoped packages, hidden nested assets, licenses, model/config files and native .node/.dll files; neighboring host files are preserved by the normal path and omitted from the extra copy. The full focused desktop configuration file passed all 24 cases in `context-desktop-copy-tests-second.log`. Desktop typechecking passed separately in `context-desktop-types-final.log`. The first combined invocation incorrectly forwarded Vitest flags to TypeScript; that command error is retained and both checks were then run with their proper arguments.

The corrected Windows directory package passed all 15 Turbo tasks in 3m13s (`context-windows-package-third.log`). The actual packaged Context smoke passed all four cases at `2026-09-10T20:16:25.532Z`–`20:16:39.618Z`: normal installed assets, positive inference from a complete Unicode/spaced-path copy outside the workspace, missing configuration, and corrupt configuration. Both positive cases used the selected ARC executable in the parent and its real fixed worker, Electron 41.7.0 / Node 24.15.0 / ABI 145, with local CPU/q8 inference and SQLite 3.51.3 FTS5. Prose/code/Unicode inputs produced finite normalized nonconstant 384-dimensional vectors; repeated vectors had zero observed delta. Wrong-manifest and token-limit inputs were rejected. Cancellation returned no successful late vector reply, disposal completed, all four runner processes exited zero and every recorded remaining-child list was empty. This cancellation observation does not assert that the native kernel had already begun when cancellation was requested.

The final manifest still has digest `8c63250ba7618e8eea1cbc3f568698607ad78703d24143df662ee72770be1fe9`; all 189 declared assets survived desktop packaging. Executable SHA-256 is `a3779d5d34c47fd52b5ac21829fc0f73fd03afb9d7d4cd46b2ecf9707b056f8f`. Byte-preserved reports/logs are retained under `.arc-verification/context-runtime/packaged-smoke-20260910T201625Z/`; immutable `final-result.json` SHA-256 is `5757c8fa82ca2edce938073b6b2eccd30daa359ec29d7bd273ec591adee288ee`. The original temporary run directory and disposable assets remain for inspection. Observed cold initialization was approximately 2.1–2.3 seconds and a three-item warm batch approximately 8–9 ms; these are single-run model observations, not retrieval/performance acceptance targets.

This closes the bounded Windows package/loading/lifecycle proof for the private Context engine. It does not complete Phase 5 or establish OS-enforced offline operation, clean-machine loading, indexing, retrieval quality/scale, document ingestion or knowledge authority. The executable is actually `NotSigned` according to Authenticode; builder log lines saying “signing” are not signature proof. The earlier native-team acceptance remains blocked by automatic approval review and was not retried. The next connected slice is the fixed typed host capability and project text indexing/retrieval, with the concrete source investigation in `.arc-verification/context-5b-integration.md`; Factory, appearance and public-release requirements remain in the approved plan.

Independent final audit passed seven gates in `.arc-verification/context-runtime/packaged-final-audit.json`, SHA-256 `d6fb560f976806ffdb6a54a493493b0ec5c53ff0c89c5a115ed4f3432df32f61`. It rehashed all installed/staged/full-package/restored-copy assets and retained reports, verified actual positive/negative/cancellation observations, matched the resource-copy regression, and independently found none of the six recorded runner/worker PIDs still present. An initial audit-only assertion did not strip ANSI formatting from a test log; its failed report is retained separately, and correcting that log parser required no product edit or test rerun. No concrete product/package defect remained in the reviewed slice.

### Alignment checkpoint 48 — connecting project Context through the existing IDE

The current implementation cycle remains Phase 5B: fixed typed Context capability in the existing daemon/plugin bridge, a watched host-local text index, server-owned immutable reference originals, matching ARC RPC/SDK/CLI operations, and a compact Context panel in the existing ARC navigation. BB remains the runtime and UI foundation; the MonoCode reference guides density and presentation. No replacement scheduler or separate application shell is being added. Existing Agent Studio, Teams, Workspace and orchestration remain in place.

Reference imports now have content-addressed durable originals, immutable replacement revisions, operation retry reconciliation, project checks and explicit text/collection bounds. The server resolves roots from registered project sources or verified ready environments; public calls cannot nominate filesystem paths. Current reference catalog digests accompany index requests so an unavailable host cannot leave superseded imported content marked current. Context reads are presently a user surface; wiring agent retrieval before immutable execution-snapshot binding would violate the approved architecture and is deliberately not advertised.

Production wiring and verification are in progress. No new passing result is claimed at this checkpoint. The packaged Phase 5A report remains evidence for its exact earlier source only; count-token and bridge changes require fresh relevant verification. PDF/DOCX/XLSX, authority/promotion, snapshot refresh, retrieval/scale gates, Factory, appearance and public release remain accepted work. The previously rejected native-team launch remains unrun; no alternate launcher has been used.

### Alignment checkpoint 49 — connected Context behavior and failures under test

The first Context UI/server gate passed 24 cases (13 UI, six real-SQLite reference-store, five server-scope/retry tests) in `context-runtime/context-ui-server-tests-second.log`. The first attempt caught ambiguous accessible names on adjacent source-row spans; explicit names now identify source, lines and revision. Exact original replacement/removal, retained revisions, offline-host import retention, catalog changes during a host reply, and foreign source/environment refusal are covered. The live Workspace now links directly to its run's project Context. A later small Stop watching action releases the retained ready index's exact operation and awaits the next focused UI gate.

The runtime gate exposed a real source-development regression: plugin-host-worker.ts had previously needed no source-module loader; its new ./context/proxy.js import could not resolve to the .ts sibling under plain Node type stripping. Source worker launches now explicitly load the existing tsx dependency while packaged JavaScript worker launch behavior remains unchanged. The corrected focused rerun passed all 28 manager/worker/bridge cases in `context-runtime/context-bridge-tests-third.log`. Earlier unchanged private client/worker/proxy/schema scopes, SDK harness and shared-contract cases passed in `context-bridge-tests-second.log`; that full run also retains the pre-fix startup failures. The initial combined command used the wrong directory for the contract test; its log is preserved and the test was moved under the package's actual test include before rerun.

Independent review found two host index races before product acceptance: cross-scope concurrent starts could exceed the four-watched-index bound, and previously collected hits could become stale while later excerpts were read. Both fixes and targeted real-filesystem/SQLite regressions are now in the first host-index gate. Actual rendered SDK/CLI/daemon/model acceptance and refreshed packaging remain pending at this checkpoint. No new provider workload, alternate native-team launcher, release or source-fork change was made. Phase 5 remains incomplete; the full accepted formats, authority, execution snapshots, retrieval/scale, Factory and release requirements remain in ARC-PLAN.

### Alignment checkpoint 50 — real Context flow and recovery corrections

All 14 host-index cases passed in `context-runtime/context-index-tests-final.log`, followed by the four-task ARC typecheck in `context-index-types-final.log`. The failed attempts are retained. The actual native watcher test exposed a production error: reusing the execution helper's directory timestamp stability check treated ordinary atomic child-file saves as root replacement. Context now verifies canonical path, no-link ancestry and physical device/file identity without treating directory timestamps as identity. The real physical-root replacement and two-worktree/HEAD tests still pass. Test fixture corrections canonicalized Windows short temporary paths, awaited watcher closure and avoided shared-prototype mutation; those corrections did not weaken assertions.

The running application passed its first complete Context flow at `2026-09-10T22:24:18.597Z`, through the actual BB SDK, CLI, enrolled daemon, local model and rendered Edge 152 interface. Evidence is retained outside source at `C:/Users/Collin Chen/AppData/Local/Temp/ARC Context UI Δ QGfxYV/result.json` and six screenshots. The disposable project is `proj_pffup82pvj`. Real indexing produced two embedded sources and explicit skips; hybrid search returned current provenance; UI upload/replace/remove preserved exact earlier originals; atomic save changed the hash and invalidated the old excerpt; deletion removed retrieval hits. The app root remained interactive at 1366x768, 1920x1080 and 2560x1440, with no horizontal overflow or page errors. Root visually inspected the captured laptop, desktop details and large-desktop layouts. Browser plugin was unavailable, so the existing Playwright/Edge runtime was used. This does not establish iOS, Windows scaling, screen-reader, retrieval quality/scale or clean-machine acceptance.

Review found two UI corrections before final packaging: older polled indexing status could briefly label fresh results outdated, and a definite capacity/revision rejection retained a pending retry that disabled corrective reference actions. Status now selects the newest index observation. Import/archive return typed `applied` or `rejected` outcomes for known transactional refusals, while uncertain transport failures retain identical operation retries. Final UI/server regression and rendered checks are pending at this checkpoint. The first application's own launcher was stopped and its printed ports cleared before these edits. BB dark, the existing shell and Workflows remain the architectural foundation; no coding-provider turn or previously blocked native-team launch was started.

### Alignment checkpoint 51 — Context recovery and final rendered checks

The corrected Context flow passed 47 focused cases: 20 UI, seven server service, six migrated reference-store and 14 host-index tests in `context-runtime/context-5b-recovery-tests.log`. The separate ARC typecheck passed four tasks in `context-5b-recovery-types.log`. Definite revision/capacity refusals now release the pending action so users can correct the reference collection; uncertain replies keep the identical operation and input for reconciliation. Status ordering accounts for index identity before comparing generations, and the Stop watching action covers retained ready/stale/failed indexes.

The complete real application flow passed again at `2026-09-10T22:31:58.927Z`, with no page errors and current provenance at all three target sizes. The retained report is `C:/Users/Collin Chen/AppData/Local/Temp/ARC Context UI Δ G2WPMG/result.json`, SHA-256 `fdd4c012222ca98660852947f688e907890faad33df2c0937607839891479e7d`. The laptop screen `context-1366.png` has SHA-256 `dcc83871053c0b05dd1b07affe5a9924225eefc2e13653641b3161cb8ec90337`. Root inspected the ready and expanded-provenance laptop views and desktop details. Exact UTF-8 reference originals, replacement/archive history, atomic-save and delete invalidation, hybrid retrieval, current excerpts and the actual CLI were exercised through the registered project and host. Its own development launcher was then stopped and the printed ports cleared. This pass used protocol 188 before the following cancellation correction; it is not claimed as verification of that later runtime change.

Final runtime review found that an unresolved helper shutdown could be hidden by ordinary cancellation handling. The host now retains a migrated SQLite stop fence and owned leases, reports failed/unconfirmed cleanup and refuses replacement until a different confirmed helper generation proves the known old generation is gone. An unobserved generation remains fenced across plugin reload, with no recovery API claimed. A second review found that manifest-only count/embed admission could silently initialize a newer helper and undermine that proof. Requests now require the exact expected generation before enqueue and dispatch; only explicit status initializes a helper. The daemon, private IPC and plugin-host handshake versions are being advanced with focused compatibility tests. The earlier 22-case cancellation pass and four-task typecheck are intermediate evidence; the final expanded gates remain pending here.

The intermediate Windows package passed all 15 tasks in `context-5b-windows-package.log`, but predates these cancellation changes and requires a fresh build. The refreshed smoke will prove generation refusal and owned-child cleanup, followed by the full packaged BB renderer/server/daemon/CLI/terminal flow and saved Context across two launches. No provider turn, rejected native-team launch or public release was started. These steps stay within Phase 5B; the snapshot-binding investigation in `.arc-verification/context-5c-snapshot-integration.md` is a design only, and accepted later phases remain unchanged.

### Alignment checkpoint 52 — exact generation admission and coordinated source gates

Daemon protocol 189, plugin-host handshake 4 and private Context IPC 3 now require `expectedGeneration` for count/embed. Only an explicit status request initializes a helper. The private client checks the requested identity before admission and again before dispatch; a stale generation returns `worker_stopped` without initializing or replacing a helper. A matching helper whose stop remains unconfirmed still returns `stop_unresolved`. Newly arriving bound bridge requests during an unbound reset are refused before dispatch, so the reset cannot be misattributed to their old generation. Original unconfirmed-stop failures take precedence over late cancellation acknowledgements. The controlled late-ack regression establishes error precedence, not an observed native IPC timing race.

The final coordinated source evidence covers 151 distinct passing cases plus one unsupported-target skip. `context-generation-shared-tests.log` has three shared-contract cases, `context-generation-sdk-tests.log` five capability cases, and `context-generation-sdk-host-tests.log` five host-harness cases. `context-generation-arc-tests.log` passes all 75 UI/store/service/host/server cases, including the 25-case real-filesystem/SQLite index suite. The daemon gate's unaffected client/proxy/private-contract/worker/manager/plugin-worker scopes passed in `context-generation-daemon-tests.log`; its one crash-fixture failure is retained. That fixture had treated OS process absence as proof that Node had received the child's exit event. It now waits for an explicitly ready replacement generation. The affected bridge suite then passed all 11 cases in `context-generation-bridge-final.log`, including actual plugin crash cleanup, token-count cancellation and the new precedence checks.

The separate integrated typecheck passed all 10 tasks across ARC, server, host daemon, shared contract and SDK in `context-generation-integrated-types.log`. Scoped formatting/lint and the root diff check passed. Independent review found no remaining concrete generation-admission or fence-clearing defect: newer stop fences cannot be cleared by older asynchronous reconciliation, and unknown generations stay fenced. This remains the fixed Phase 5B capability; no new scheduler, retrieval authority, reset API or provider workload was introduced.

The source snapshot contains 5,896 entries at HEAD `06aeaa994942ae7527dc49d2268c1f801e8542a0`: `context-runtime/context-5b-source-189.json`. The refreshed Windows package is being built from that source in `context-5b-windows-package-final.log`; package/model/restart results are pending at this checkpoint. Later immutable worker snapshots require tracked Workflows prerequisites so snapshot preparation cannot escape pause accounting. That next slice remains design only and does not weaken the existing native, retrieval/scale, document, Factory or release gates.

### Alignment checkpoint 53 — packaged BB and connected Context acceptance

The refreshed Windows directory package passed all 15 tasks in 3m43s: `context-runtime/context-5b-windows-package-final.log`. Executable SHA-256 is `aeb0a366860e559b7d5e722778492e152bd483ae5d0c1c45ff2cf172ef20707d`; `resources/app.asar` is `188097ba7fadadde6d5a03df907126aa6bb6cd4de9a35b0ed70aaee5b037c771`. The shipped Context manifest is `5e0294ac22cb1708ddd4a6b7389353d61f13c420cf84542fc8cd6154ddabba41`, containing all 189 assets totaling 109,008,261 bytes. Actual Authenticode inspection still reports `NotSigned`.

All four packaged model cases passed at `2026-09-10T23:11:56.645Z`–`23:12:13.172Z`: installed assets, positive Unicode/spaced-path copy, missing configuration and corrupt configuration. Electron 41.7.0 / Node 24.15.0 performed real local CPU/q8 inference and SQLite FTS5. Exact tokenizer counts matched embedding counts, the long input counted 302 tokens without truncation, repeated vectors had zero observed delta, and invalid manifest/token inputs were rejected. Wrong-generation count/embed calls preserved the ready helper identity; calls for the stopped generation started no replacement. Cancellation and disposal left no owned children. Independent `context-5b-model-audit.json` passed nine gates and independently found none of the six recorded runner/worker PIDs present. The immutable model report SHA-256 is `4b7224bcc81fbef137a5756279d9176c92dca8a9694139061a051cc106ce6bc8`. Cold initialization was approximately 2.3 seconds and warm three-item inference 8–9 ms; these are observations, not retrieval performance acceptance.

The first full-application attempt is retained as failed in `context-5b-packaged-app-final.log` and `C:/Users/Collin Chen/AppData/Local/Temp/ARC packaged smoke Δ SCdMrI/result.json`. Its first-load UI, project and native Context checks passed. PowerShell then correctly reported the long `Collin Chen` path while the temporary fixture used the equivalent `COLLIN~1` alias. Both CLI calls exited zero and the sentinel contained the expected data, but the driver's string comparison failed. Only the smoke driver changed: initial and observed working directories now compare canonical filesystem paths. `context-5b-source-after-first-smoke.json` records that single file difference from the build snapshot. No shipping code or package changed, and the initial failed result remains byte-exact.

The corrected full-application smoke passed both launches and all 13 checks in 47 seconds: `context-5b-packaged-app-second.log`. It exercised the actual packaged first-load renderer, ARC title, BB dark theme, navigation/composer, native server/daemon, provider catalog and absence of forecasting/Python payloads. A real project and exact original file survived restart. Before retrying the import, pass two verified that the same reference ID, revision, hash and original bytes already existed. Both launches freshly indexed through the packaged local model, retrieved hybrid results and exact current excerpts, then cancelled the exact watch operation and rejected the old excerpt as stale. The retained second import reports its older indexing operation was superseded; the immutable reference retry is applied and the new explicit indexing operation succeeds.

Both launches also passed the actual packaged CLI through Electron with no Node on PATH, project SDK read, PowerShell/ConPTY input/output, Unicode file write and terminal cleanup. The second launch created and saved an Agent Studio draft using native input, then reloaded the styled Guide editor. Root visually inspected the packaged composer and Studio screenshots. Both applications quit normally; the test ports and runtime supervisors closed, with no forced cleanup or cleanup error. A subsequent exact-executable process query returned no packaged ARC processes, including model helpers. The original artifact directory is `C:/Users/Collin Chen/AppData/Local/Temp/ARC packaged smoke Δ FaRUfn/`; the immutable application report SHA-256 is `b50b54fbd70553022cca43f6401a8aa3be4add01add85331163d515630cbe364`. Byte-preserved model, application and initial failed reports are also retained in `context-runtime/packaged-5b-20260910T231156Z/`. Screenshots and runtime profiles remain outside source.

This closes the bounded Phase 5B user Context and refreshed developer-machine package checks. It does not complete shared agent knowledge or the whole ARC plan: worker-bound immutable snapshots, document extraction, promotion/refresh, quality/scale and OS-offline gates remain open, followed by Factory, appearance and public release. Unknown-generation unconfirmed stops remain durably fenced without a claimed recovery API. No provider turn or previously rejected native-team launch was made. All 5,896 entries plus HEAD matched the corrected-driver snapshot at `2026-09-10T23:17:05.925Z` in `context-5b-source-final-check.json`; that verification window is closed. The later application audit retains transient startup/network observations rather than claiming error-free renderer logs.

Independent final application review passed all nine gates with no concrete findings: `context-5b-app-audit.json`, SHA-256 `e43962130f540aefe2e5c045f43cb9fafdf17f2fa67ddf79a73e80a73565eceb`. It independently viewed all three screenshots, matched package and corrected-driver hashes, verified both Context/CLI/restart records, and found the four test ports, exact selected-executable processes and both recorded terminal PIDs absent. The retained renderer intervals contain five refused connections, one cancelled request and one provider-status HTTP 502 during startup; no runtime exceptions or console errors were observed in those intervals. These observations remain in the audit, including unavailable failed-request URLs, without recategorizing them as a clean network log or invalidating the separately observed successful product gates. The model audit SHA-256 is `ce932474938d720c576ee02ed94c373572dd46d982305ed5a9659883d57338c9`.

### Alignment checkpoint 54 — unified Threads implementation

The user approved the dedicated project/thread/conversation layout and direct Chat/Team integration. Implementation is isolated in B:/arc-threads-20260910 on codex/arc-threads-workspace, beginning at snapshot 974af76 of the existing dirty ARC state. The source checkout remains unchanged while integration is developed; .arc-verification/threads-workspace/baseline.json records all source bytes and absent files for safe reconciliation.

The first two work cycles separated project navigation from thread presentation, retained BB row actions/tree organization, and introduced provider/model/title/status row hierarchy. Independent slices add project-scoped search and retained model projection, generic experimental plugin contributions, and ARC's exact run/worker annotations. The default active-run lookup remains exact across historical pages through a targeted read-only Workflows query. No replacement scheduler, forecasting work, appearance filters, provider turn or native-team acceptance launch is included.

Early focused checks are passing for plugin contribution behavior and ARC/Workflows reads; integration typechecking and rendered verification are still underway. No completed UI or release gate is claimed here. The next cycle connects the browser shell, exercises real navigation and addresses visual mismatches against the supplied MonoCode reference. Existing Phase 5/6/7/8 obligations remain in the plan.

### Alignment checkpoint 55 — integrated Threads behavior and rendered review

The MonoCode-inspired milestone is connected in the isolated `B:/arc-threads-20260910` worktree, on the exact dirty-source baseline `974af76`. It keeps the BB dark UI and existing thread actions, custom sections, pins and project ordering. The core browser owns project-scoped search and live activity views; ARC contributes exact team/agent identities and the Team companion through documented experimental SDK slots. Workflows remains the scheduler.

The coordinated focused app gate passes 215 cases across 12 files in `.arc-verification/threads-workspace/app-tests-second.log`, including existing row/actions, project keyboard reorder, content-search/cache consumers, companion fallback and a 1,000-row windowing regression. The data/search slice passes 106 DB, SDK, CLI and server cases. ARC's final handoff/link gate passes 45 cases with no worker teardown timeout; ARC/Workflows types pass. The generic companion's focused 16 UI cases, 19 authoring-documentation cases, SDK declaration build and server typecheck pass. Earlier type/lint/test-driver failures remain in their logs; app lint now reports zero errors with existing and inherited React compiler warnings. Final combined app types remain pending while a test-only Testing Library option mismatch is corrected.

The real Edge browser exercised an isolated checkout profile and a deterministic 1,000-thread, 21,477-event fixture. `browser-result.json` records eight passing checks: default layout with 11 mounted rows, server-scoped search, Chat/Team/history with the exact main/composer retained, hide/reopen query retention, keyboard pane resize, compact drawer without app-root inert/aria-hidden or page overflow, reduced-motion use, and no provider turn dispatch or page errors. Screenshots cover 1366, 1920, 2560, 900 and 390 widths. Visual inspection found a compact toolbar collision with BB's floating navigation toggle; the Threads action now reserves that toggle's space. The empty Team setup state is actual server output; seeded conversation contents are QA fixtures, not completed coding work.

Scope check: backgrounds/glass/filters, Context retrieval/scale and Factory gates remain outside this Threads milestone. The Windows package navigation check and remaining layout regressions are in progress. No source WNDR files, production profiles or native orchestration acceptance runs were used.

### Alignment checkpoint 56 — Threads milestone integrated and Windows navigation verified

The approved Threads milestone is implemented in the original `B:/Collin's Vibe Code Projects/ARC IDE` checkout. The 93 changed/new files were reconciled against the exact dirty-source baseline and byte-verified without conflicts; original HEAD remains `06aeaa994942ae7527dc49d2268c1f801e8542a0`. Existing unrelated edits and the original index were preserved. `.arc-verification/threads-workspace/integration-result.json` records the applied files. The verified unpacked Windows build remains at `B:/arc-threads-20260910/apps/desktop/release/win-unpacked/ARC IDE.exe`; this is not an installed application update.

Final review fixed collapsed-parent attention, root-only Team availability, and retained hidden-drawer annotation subscriptions. The affected app suites pass all 85 cases in `final-review-fixes-tests.log`; the additional real annotation lifecycle regression passes in `browser-rows-enabled-test-final.log`. The remaining layout regressions pass all 25 cases across the initial run and focused rerun in `.arc-verification/threads/layout-regressions-*`; the only correction was an obsolete test storage key. These results supplement the earlier 215-case app and 106-case data/search gates without summing overlapping reruns. Combined app/server/ARC/Workflows/plugin API typechecking passes all 10 tasks in `integrated-types-final.log`. App lint passes with zero errors and 187 warnings; it is not warning-free. Scoped formatting and `git diff --check` pass. Public contribution, search and read surfaces are documented in the SDK, CLI and Plugin Guide. The host-daemon wire contract is unchanged by this milestone.

The final source also passes all eight browser checks again in `browser-result.json` and `browser-final.log`, including 1,000-thread windowing, project-scoped content search, persistent main/composer DOM and draft through Chat/Team and history, pane sizing, compact drawer, reduced motion and no provider-turn dispatch. Root inspected the corrected 390px toolbar and the 1920px Team layout. The isolated development launcher was stopped; its three known listeners were absent afterward.

The final Windows package passes 15/15 Turbo tasks in 4m7s (`windows-package-current.log`). Its actual packaged renderer passes all six navigation checks at `2026-09-11T01:51:04.871Z`–`01:51:31.380Z`: BB dark Threads entry; four Chat/Team cycles preserving the exact main/composer and native unsent draft; Back/Forward; Agents, Teams, Orchestration and Context navigation; draft restoration after that roundtrip; and graceful shutdown with the owned runtime and listeners gone. Fresh idle UI fixtures had zero events, queued inputs and turn preparations before and after. The immutable report is retained in `packaged-navigation-20260911T015131Z/final-result.json`, SHA-256 `32dfa3d4f1550701b2eace85bf6f905c65afc9a171a82f7814fc748372c8464d`. Root inspected the packaged Team and Agent Studio images. The post-smoke source comparison matches all 4,514 shipping files plus HEAD, including new untracked production files; `shipping-source-final-check.json` reports no changes.

Executable SHA-256 is `a3d8ee8a14007a106e500d19561e96ac206f8c95673f15cea5a77c3fe388fa14`; `resources/app.asar` is `3f7eb5ec2209b621a7cba97979645d8e3f33d62cdf40d2339376b66abe689d4a`. Independent current hashes match the smoke report and Authenticode reports `NotSigned`. Two earlier screenshot-only attempts timed out while the owned Electron window was completely hidden. Their reports, driver bytes and cleanup observations are retained. The successful driver kept interaction hidden and briefly showed only its owned window inactive for compositor capture. No shipping changes or package rebuild were needed for that correction.

`packaged-navigation-final-audit.json` retains the visible model-loading error and actual diagnostics. This smoke deliberately used a fresh HOME/profile/data directory, an allowlist of Windows environment variables, and a PATH limited to Windows system/PowerShell directories. Automatic model discovery therefore could not locate the Codex CLI in that isolated launch; server and daemon logs report `command_failed` / HTTP 502. The navigation result does not establish model availability or live provider execution. All 33 package evidence files, including failed attempts, were byte-verified into the retained bundle. The package check does not cover streaming, a real handoff, native-team execution, an installer, clean-machine loading or signing. iOS Simulator Safari was unavailable on this Windows host. The accepted later Context, Factory, appearance and public-release gates remain open in ARC-PLAN; this checkpoint completes the bounded Threads integration and its applicable local verification.

### Alignment checkpoint 57 — reliability fixes on the latest MonoCode source

The user requested the audit corrections on the latest MonoCode work. The combined checkout `B:/arc-monocode-reliability-20260910` starts at immutable snapshot `ce1eb943320337262e8631836bd44a8a9c420885` of the original checkout after checkpoint 56, including its existing dirty source. This carries the completed Threads layout forward. The 25 changed/new files were reconciled and byte-verified into the original `B:/Collin's Vibe Code Projects/ARC IDE` checkout without conflicts, preserving its unrelated edits, index and HEAD `06aeaa994942ae7527dc49d2268c1f801e8542a0`. The backup and exact file hashes are in `integration-apply.json` and `integration-backup/` under the evidence directory below. No commit or upstream push was made.

Startup now retries transient system-configuration transport failures while rejecting malformed or incompatible responses. The desktop waits for the actual child spawn event and surfaces missing-executable or working-directory failures. ARC evicts a failed native-effects initialization so a repaired local storage path can be retried in the same host. Failed shutdown retains the runtime and exact recovery marker; stop and exit callbacks share cleanup, stale callbacks cannot release replacement ownership, and signal/update failures permit retry. Normal Quit consumes and reports cleanup failures. Real temporary ownership files and an injected stop failure exercise recovery without claiming an observed operating-system kill failure.

Git author and committer identity are resolved using Git itself before managed worktree preparation. Missing identity returns an actionable native receipt before workspace creation or worker dispatch; repository, global and environment identities remain authoritative. Reproduction established the original late commit failure and two failing preflight regressions before the fix. ARC's six navigation destinations and Assistant/Test tabs now use distinct supported glyphs, while its original artwork remains on logo surfaces. The first Windows build exposed a generator assumption that every bundled plugin declares a compact icon. The generator and catalog now accept the already-supported logo-only manifest, preserve compact/named-icon precedence, and serve the logo with its actual media type without tinting it.

Final source gates pass 997 distinct tests: 315 desktop cases with three platform skips, all 613 ARC cases across 58 files, 57 server catalog/branding cases and 12 rendered icon/catalog cases. The overlapping focused 18-case native rerun and initial eight icon cases are not added to this total. Combined desktop/app/ARC typechecking passes six tasks; server typechecking passes five. The identity cases use disposable real Git repositories and migrated SQLite with isolated Git configuration; they do not change the user's Git identity. Scoped formatting and diff checks pass. Focused server lint retains two pre-existing restricted filesystem imports; no suppression or warning-free claim is made. Evidence, failed attempts and diagnostic drivers are outside source at `C:/Users/Collin Chen/AppData/Local/Temp/arc-monocode-fixes-20260910/`.

The refreshed Windows directory package passes all 15 Turbo tasks in 4m44s (`windows-package-final.log`). The real executable passes both launches and all 13 existing application smoke checks in 50 seconds (`windows-smoke.log`; original report at `C:/Users/Collin Chen/AppData/Local/Temp/ARC packaged smoke Δ Knoltp/result.json`). These cover persisted project/reference originals, actual local Context inference and hybrid retrieval, current provenance and watch cancellation, the packaged SDK/CLI with no Node on PATH, PowerShell/ConPTY input/output and Unicode writes, Agent Studio native editing/save/reload, and graceful runtime cleanup on both quits.

The same executable passes seven additional packaged navigation checks at `2026-09-11T02:28:44.811Z`–`02:29:11.939Z`: BB dark Threads entry, four Chat/Team cycles preserving the exact main/composer DOM and native unsent draft, Back/Forward, ARC's original untinted PNG served through the actual catalog route, Agents/Teams/Orchestration/Context navigation, draft restoration with zero provider events or queued/prepared work, and graceful cleanup. The immutable report is `C:/Users/Collin Chen/AppData/Local/Temp/ARC Threads navigation Δ PtoiKG/final-result.json`, SHA-256 `b85e14fd5d164c3e4f887a93996305256eefa754b950c45eab3d79bfbdd3735b`. Root visually inspected the current packaged Team/draft and Agent Studio screens, including the distinct navigation and Assistant/Test glyphs; all inspected plugin pages retained an interactive app root with no horizontal overflow at 1424px content width. The first navigation attempt failed before application launch on the reused driver's previous build-log filename. Its report is retained; only the external diagnostic driver changed before the passing rerun.

The tested executable is `B:/arc-monocode-reliability-20260910/apps/desktop/release/win-unpacked/ARC IDE.exe`, SHA-256 `cbb71bc6135d9a4e6d88c4ce57f307dbdf46434089c2b385413014191e8136ad`; `resources/app.asar` is `a245c185857e92d61a99b2e3b17f12367722f3cb249f9f2c8ee5a1bb205b9155`. Authenticode reports `NotSigned`. This is a tested local unpacked build, not an installed update or clean-machine certification. The isolated profiles intentionally have no coding-provider CLI on PATH, and the visible model-discovery error is retained rather than presented as a working provider flow. The native Electron diagnostic harness was used; the Browser plugin was unavailable. Existing user application sessions were not stopped or replaced. Provider authentication/streaming/recovery, installer lifecycle, signing, iOS and scaling acceptance remain unverified by this pass.

Independent final evidence review passed nine gates in `packaged-final-audit.json`, SHA-256 `6c3b77897dc4fdd62faeea203c6d517058ab860c60f0c4f1cdb88e48a6004000`. It matched package hashes and seven screenshot hashes, inspected five screenshots and independently found all six known test ports, exact-package processes and recorded runtime/terminal PIDs absent. No renderer exceptions or console errors were recorded. Network diagnostics retain three connection refusals, eight cancelled requests, one Codex provider-logo HTTP 404 during restart and one provider-status HTTP 502. Failed-request URLs are unavailable for the connection refusals/cancellations; this is not a clean-network claim. Desktop ownership markers were removed, while inner runtime metadata retains dead PIDs. The post-smoke inventory rechecked all 6,275 source entries: only this progress document changed, and shipping source remained unchanged.

This is a reliability correction to the existing approved Threads/ARC implementation, with BB dark, Workflows, the current public API contracts and the WNDR source boundary preserved. It introduces no new scheduler, provider turn or native-team acceptance launch. Later Context, Factory, appearance and public-release gates remain open in ARC-PLAN.

### Alignment checkpoint 58 — actual Windows streaming and handoff verification

The same unpacked Windows package was exercised with an isolated ARC data directory and Electron profile while inheriting normal signed-in Codex CLI discovery. No credentials were copied or logged, and no global provider configuration changed. Codex CLI 0.147.0 supplied the real model catalog with `modelLoadError: null`; the test used `gpt-5.6-sol`, low reasoning, default service and Accept Edits. Executable and ASAR hashes still match checkpoint 56. All 4,514 shipping files and worktree HEAD match the build snapshot in `shipping-source-live-check.json`; this verification required no production code changes.

One disposable Git fixture and one real ARC run were used: `run_78978c05-903c-4136-b436-a42272285bab`, plan `fc25c8404cf1526849b72282315ef144c9e473172f67c8180e49ea939ade5fd4`. After an explicit observation gate, a native source check and one read-only blue reviewer completed. The reviewer obtained its immutable snapshot, checked the actual fixture and recorded an approved verdict against candidate `aa4b54efbfd82a7eb4cf8deda98a1eef104fc28d`. Both required gates succeeded, verification became current, and the counted main completion returned to the parent. The run used two owned calls, one concurrent agent and no repairs. `run-audit.json` passes 11 checks for the bounded three-turn run observation; two later standalone display probes bring the final session total to five native turns. They created no additional team run. Original fixture files, HEAD and branch remained unchanged; the candidate was not promoted.

The actual packaged renderer showed one handoff for the reviewer's exact `native-accepted` request. Its 650 ms animation completed; the capture records 85 animation-frame samples and 45 distinct positions from the main chat toward the blue reviewer panel. Chat/Team remount did not replay the event. During the final text-only provider response, Chat and Team retained main/composer DOM identities 1/2 and the exact unsent draft. Four fully visible partial states grew from 536 to 1,129, 1,696 and 2,203 characters and matched retained native deltas. Team returned 7,867 ms before message completion. `finalUIaudit.json` binds preserved capture snapshots and records these checks. Root inspected the actual working and completed 1904-by-1015 renderer images. This establishes observed motion and live text retention; it does not establish frame-rate performance, multiworker scale or scroll-offset conservation. The final switch helper explicitly scrolled the response into view.

The command-output probes exposed a verification limitation: the provider/runtime delivered their stdout with command completion, with no retained intermediate command-output delta for the focused probe. Completed stdout and the provider's later narrative are not live-terminal-streaming proof. Initial driver attempts missed disclosure groups or the live switching window; their errors and observations are preserved, including an incorrect thread-list result shape, an early editable-target check and inspector timeouts. Renderer diagnostics retain three refused connections, twelve cancelled requests, an initial provider-CLI-status HTTP 502 and the driver's editable-target exception. This is not an error-free log claim. Successful model discovery and the subsequent native turns are separately evidenced.

Original artifacts are in `C:/Users/Collin Chen/AppData/Local/Temp/ARC live UI f3af5f7fc7a84e1e937a59e052c05b89/`. The iOS discovery report is in `C:/Users/Collin Chen/AppData/Local/Temp/arc-ios-availability-20260911T020850Z/`: this Windows session exposes no Mac/Xcode simulator or iPhone/iPad test target. No actual iOS Safari test was run; desktop viewport emulation does not close that gate. This work stays within the approved Threads verification milestone. Context, Factory, appearance, signing/installer and public-release gates remain open.

While this live package check was running, a separate reliability task integrated checkpoint 57 and its newer package. The original checkout's changed progress file and ARC icons were detected by the integration guard, which refused to overwrite them. Only this evidence section is appended to the current progress document; that separate implementation is preserved. This live evidence applies to the checkpoint 56 executable at `B:/arc-threads-20260910/apps/desktop/release/win-unpacked/ARC IDE.exe`, not the later reliability executable. Its live-provider acceptance must be established separately; package evidence is not transferred between executable hashes.

The final session audit passes seven checks and confirms all five native turns completed, with no unresolved work. The original shutdown driver's raw preparation count included retained terminal evidence. A separate exact-process helper validated the native completion chains and requested Electron Quit without deleting those records. Its first wait timed out because the original capture controller still held a debugger connection; that failed report is preserved. After the controller was interrupted to release its debugger, independent queries found application PID 43096, supervisor PID 37008, ports 56855/56856/56857 and the desktop ownership marker absent. `post-controller-release-cleanup.json` records that final absence; it does not turn the helper timeout into a passing shutdown test. No application-process kill command was issued. The retained evidence bundle is `.arc-verification/threads-workspace/live-20260911T022327Z/`, with per-file hashes; it excludes the disposable profiles and databases.

### Alignment checkpoint 59 — Agent Studio assistant composer

The requested Ask assistant adjustment is integrated into the current original checkout after isolated implementation in B:/arc-monocode-reliability-20260910. The two source files are plugins/arc/studio/chat.tsx and its new chat.test.tsx. The original composer matched the exact pre-edit baseline, the new test path was absent, both integrated files were byte-verified, and HEAD and index were preserved. Existing checkpoint 58 and unrelated concurrent source changes remain intact. Integration evidence is C:/Users/Collin Chen/AppData/Local/Temp/arc-agent-composer-20260910/integration-result.json.

Agent Assistant now uses the main composer's shared visual tokens, bottom placement, input and send affordance. The opening guidance explicitly asks what this agent should do, with three starters that fill and focus the draft without sending. The project selector and execution settings are integrated into the footer. Desktop Enter sends and Shift+Enter adds a line; touch Return adds a line, IME composition is preserved, and duplicate submissions are guarded. Archived, unavailable-project, blank, pending, error and retry states retain the existing admission rules. Existing local draft persistence, version checking, RPCs, session history and embedded thread chat remain connected. This is a UI adjustment to the existing capability, with no new SDK, CLI, daemon or backend contract.

The final Turbo gate passes 26 cases across the assistant composer and existing editor suites (17 plus nine), and ARC typechecking passes all four tasks. Scoped lint, formatting and diff checks pass. Final logs are studio-final-tests.log and studio-final-types.log in the same evidence directory. Independent review identified touch Return and IME fallback cases before completion; both are fixed and covered.

The real isolated development application at http://127.0.0.1:13305 passes seven browser checks in browser-result.json, with screenshots at 1920x1080, 1366x768, 900x900 and an emulated 390x844 touch viewport. Root visually inspected the laptop and touch drawer images. The composer stays visible, the app root remains interactive without inert or aria-hidden, and no horizontal overflow was observed. Starter focus, newline behavior, exact submission payload and draft version, failed-send retention/retry, and persistence after a real page reload are verified. Browser tooling was unavailable, so bundled Playwright controlled Edge. No runtime page errors or framework error overlay were observed; the only console error is the explicitly injected HTTP 503 used to exercise submission failure. Initial diagnostic-driver failures remain in the evidence directory alongside the passing final result.

The submission check deliberately intercepts the assistant-start request; it does not certify a live provider turn. Phone coverage is desktop touch emulation, not actual iOS Safari. The source and development preview include this change; no new Windows package was built for this UI adjustment, and earlier packaged evidence is not transferred to it. This stays within the existing Agent Studio and BB dark UI scope. Context, Factory, appearance and public-release obligations remain as recorded in ARC-PLAN.

### Alignment checkpoint 60 — centered New thread composer

The requested New thread layout is integrated into the current original checkout from B:/arc-monocode-reliability-20260910. RootComposeView centers the existing composer and displays the shared ARC logo above it. Explicit New thread actions select the centered scrolling layout on compact screens; the idle mobile home retains its recent-thread layout. Existing draft persistence, composer settings and submission behavior are preserved. AppLayout restores the root page inset expected by the existing compose wrappers. A baseline browser reproduction established that the old inset cancellation made the document 20 pixels wider than the 1366-pixel viewport before this change; the corrected layout fits the inspected viewports. No new API, CLI, daemon or provider behavior was introduced.

Final Turbo verification passes 91 existing tests across RootComposeView, RootComposeSecondaryContent, RootComposeCompactHome and AppLayout, plus all three app typecheck tasks. Scoped lint, formatting and diff checks pass. The real isolated development application at http://127.0.0.1:13305/ was exercised by bundled Playwright using Edge because the Browser plugin was unavailable. Browser evidence covers actual New thread clicks, repeated clicks preserving a draft, the loaded original ARC artwork, centered geometry, responsive sizing, and a short touch viewport. Desktop sizes are 1366x768, 1920x1080, 2560x1440 and 900x900; emulated touch sizes are 390x844 and 390x480. The short viewport check is not a physical-device soft-keyboard test. No provider submission was dispatched. Final browser errors and screenshots are retained in browser-result.json, alongside earlier driver failures. Root visually inspected the final laptop and touch screenshots.

Evidence is outside source at C:/Users/Collin Chen/AppData/Local/Temp/arc-new-thread-center-20260911/. Integration requires both original files to match their pre-edit bytes, verifies the integrated candidate bytes and preserves original HEAD/index and the progress document's existing prefix. This is a source and development-preview update; no new Windows package or iOS Safari acceptance is claimed. It stays within the requested UI adjustment and existing BB dark design. Other ARC-PLAN gates remain open.

### Alignment checkpoint 61 — Astra discovery and provider update recovery

The missing GPT-6 Astra selector entry was traced to the installed Codex CLI, not a frontend allowlist. ARC's actual model endpoint and a fresh Codex CLI 0.147.0 process returned five visible models without Astra; the app-bundled 0.153.4 returned Astra. Hidden-model inspection did not reveal Astra in the older CLI. The main composer and Agent Studio both consume the shared live catalog. Raw model-only evidence is retained at C:/Users/Collin Chen/AppData/Local/Temp/arc-astra-models-20260911/. No credentials or account tokens were captured.

The first actual ARC provider update failed: its bare codex update command launched the separate app executable on Windows instead of the detected npm wrapper, and the updater could not detect its installation method. Codex maintenance now uses the existing npm installer for npmGlobal installations and the exact discovered executable for external installations; the displayed update command matches the executed plan. The corrected native update through ARC's existing host API exited zero and verified Codex CLI 0.154.0. The installed global npm Codex CLI was updated; this is an actual machine change, not a mocked catalog or a source-only workaround. The initial failed update receipt remains alongside the successful receipt.

Successful provider install/update now clears the server's model memo immediately. A probe already pending at invalidation cannot repopulate or overwrite that cache afterward. Existing maintenance-runtime invalidation remains responsible for restarting the provider's catalog process. The live execution-options response immediately after the update contains gpt-6-astra with all six reasoning levels and modelLoadError null. No model is hard-coded into ARC and no wire, SDK, CLI flag or endpoint contract changed.

Verification passes 26 focused tests: seven Codex maintenance cases and 19 provider-installation/cache cases. Codex types pass four Turbo tasks; server types pass five. Scoped lint, formatting and diff checks pass. A broader existing execution-options suite retains 11 ACP provider-visibility expectation failures; a representative case reproduced with both changed production files virtually loaded from their untouched baseline backups. These unrelated failures were retained and not recategorized as passing. Earlier diagnostic command and browser-driver failures also remain in the evidence directory.

The actual development application at http://127.0.0.1:13305/ passes four browser checks in browser-result.json using bundled Playwright Edge at 1366x900 because the Browser plugin was unavailable. The main composer switches from Sol to Astra, accepts Ultra reasoning and retains its unsent draft. Agent Studio selects Astra and serializes gpt-6-astra in an unsaved definition; project-model inheritance was restored without saving. Root inspected both selector screenshots. There were no page exceptions, console errors, framework error overlays or provider-turn submissions. This verifies catalog discovery and selection, not an Astra inference turn or iOS behavior.

Six focused source/test files are integrated from B:/arc-monocode-reliability-20260910 after exact original-baseline checks and candidate byte verification; the original HEAD, index and existing progress prefix are preserved. integration-result.json records the hashes. The development runtime and installed CLI include the correction, while the packaged ARC executable was not rebuilt. This remains a correction to existing provider discovery and maintenance; the remaining ARC-PLAN gates are unchanged.

### Alignment checkpoint 62 — restrained panel motion

The desktop Threads pane previously unmounted on close, while its reopen toolbar appeared at its full 40px height immediately. The pane now retains its content and animates its outer width with the existing 180ms ARC panel easing. Its fixed-width inner surface adds a subtle 12px slide and fade; the reopen toolbar expands and fades in the same interval. The resize handle stays outside the clipped content and live pointer resizing disables width easing. Collapsed pane and toolbar controls are inert and hidden from assistive technology; keyboard focus transfers between the opener and pane. Native browser bounds synchronization is dispatched on layout state changes and transition completion.

This is a two-file adjustment to ThreadBrowser.tsx and thread-browser.css. It extends existing ARC motion tokens, preserves shared persistent compact drawers, and adds no dependency, public API, SDK, CLI, runtime, or storage contract. Main navigation and secondary panels retain their established 180–220ms transitions. Search/archive activity, keyboard handlers, annotation feeds and timers follow the pane's open state; retained contents continue sharing the existing navigation and provider catalogs. The original dirty checkout and WNDR boundary are preserved.

Verification passes all 59 tests across thread browser state/projects/rows, responsive overlays, secondary panel layout and compact shelf behavior. App typechecking and lint complete successfully through Turbo; full app lint reports 187 existing warnings and zero errors, while scoped lint on the edited component is clean. Formatting and focused diff checks pass. No new implementation-mirroring unit tests were added for this reversible motion change; the actual rendering and interaction behavior is exercised in the external browser driver.

The live development app at http://127.0.0.1:13305/ passes six browser checks in Playwright Edge at 1440x900 and 390x844. The driver records 147 animation-frame samples across opening and closing, verifies intermediate panel widths and toolbar heights, retained search DOM/value and unsent draft, focus return, three rapid keyboard toggle cycles, immediate pointer resize with retained width, reduced-motion final states, and compact drawer dismissal without app-root inert/aria-hidden or horizontal overflow. Returning across the responsive boundary requires reopening the existing welcome composer; its draft is retained. No page exceptions, console errors, framework overlay, or provider submissions occurred. Root visually inspected the final desktop and compact drawer screenshots. Initial driver failures from welcome-screen entry and accessible-name assumptions are retained alongside the passing run.

Evidence and baseline backups are outside source at C:/Users/Collin Chen/AppData/Local/Temp/arc-panel-motion-20260913/. Integration checks both original files against exact pre-edit hashes, copies only the two verified candidates, preserves the existing progress prefix, and verifies HEAD/index are unchanged. This is source and live development-preview evidence; the packaged executable was not rebuilt and iOS Simulator Safari was unavailable on this Windows host. The change remains within the requested interaction polish; other ARC-PLAN acceptance gates remain open.

### Alignment checkpoint 63 — optional Liquid Glass appearance

The user requested MonoCode's Liquid Glass mode in Appearance. The reference was inspected read-only at hardbeat920/monocode commit 9b404e691addf576c6c59b145479cccbc2c49e90. Its relevant behavior is translucent chrome over Windows acrylic or macOS vibrancy, independent of the color palette. ARC implements that behavior with its existing theme tokens and desktop bridge; no MonoCode source, new rendering dependency, wallpaper system, or shader library was copied or introduced.

Settings → Appearance now includes a Liquid Glass switch, defaulting off for existing installations. The server persists surfaceStyle as default or liquid-glass in app_theme through generated migration 0116_arc_liquid_glass.sql. All 116 earlier migration snapshots remain unchanged. Palette and favicon edits preserve the material selection; legacy requests that omit surfaceStyle retain the stored value. The SDK theme selection accepts the same field, and bb theme style set liquid-glass / bb theme style reset expose the mode through the CLI. The configuration guide, CLI guide template and built-in CLI references document these surfaces. No host-daemon wire contract changed.

The renderer composes the material CSS with the selected palette and reuses the existing pre-paint CSS cache. Sidebar, Threads, secondary panels, composers, settings cards and popup surfaces receive restrained translucency, blur and edge highlights. Core canvas and code/terminal color tokens remain opaque. Browser clients use a static palette-derived backdrop. Native root transparency is enabled only after the desktop acknowledges a supported material; light mode, unsupported platforms and accessibility preferences retain opaque fallback behavior. Review fixed tooltip color pairing by restricting glass popup backgrounds to their matching surface classes. Visual inspection also caught the persistent compact sidebar showing through the main view; the compact main inset now retains its opaque backdrop while the panels remain frosted.

The optional desktop API validates its enum/result and accepts material requests only from a registered application main frame. Windows 11 build 22621+ uses acrylic, macOS uses under-window vibrancy, and other platforms return none. Disabling or failing material setup restores the captured background. The renderer handles old desktop wrappers, stale responses, unmount, reduced transparency and forced colors. Actual Electron 41.7.0 execution on Windows 10.0.22631 used the production handler and sandboxed preload in a disposable window: Windows DWM reported backdrop 2 → 3 (Desktop Acrylic) → 1 (none), the original RGB background was restored, light mode returned none, and an unregistered window was rejected. This is native Windows material evidence, not a complete packaged ARC acceptance run. macOS/Linux behavior has unit coverage only.

Verification passes 222 distinct focused cases: 94 server/database/SDK/CLI cases, 76 application theme/pre-paint/drawer/panel cases, and 52 desktop/contract/theme-sync cases. The final 38-case exact theme/pre-paint rerun is included in that count. Typechecks pass for app, desktop, desktop-contract, domain, database, server, SDK and CLI through Turbo. The production frontend build passes three Turbo tasks and precompresses 440 assets; existing chunk-size/deprecation warnings remain. Scoped UI/native lint and formatting pass. Backend lint reports 59 baseline-matched findings and zero new findings. An overly broad test filter also selected three failing plugin-code-theme cases; all three reproduce with untouched baseline code because the old tests assume light mode while the default is dark. The separate initial backend overmatch selected an unrelated plugin SDK timeout. These retained failures are not counted as passing or represented as a green repository-wide suite.

The live application at http://127.0.0.1:13305/ passes six browser workflows in Edge at 1440x1000 and 390x844: switch/reload persistence; palette, favicon and light/dark preservation; forced-color and reduced-transparency fallback; composer draft retention through panel toggling; compact layout and persistent drawer interaction; and a fresh browser context reading the stored setting. Four additional real-tooltip checks cover light/dark with and without reduced transparency, measuring contrast above 9.9:1 and 15.1:1 respectively. The right panel also renders its glass material. Root visually inspected desktop, compact, drawer and light-mode captures. There were no browser page exceptions or console errors in the six-workflow run. The actual built CLI reset and enabled the live setting, preserving the Default palette and Blue favicon. No provider prompt was submitted. Liquid Glass is left enabled in the development preview.

Evidence, exact baseline backups, native smoke results, screenshots and failed diagnostic attempts are retained outside source at C:/Users/Collin Chen/AppData/Local/Temp/arc-liquid-glass-20260913/. Integration reconciles only the 36 changed/new files against their exact original baselines and candidate hashes; original HEAD 06aeaa994942ae7527dc49d2268c1f801e8542a0, index and progress prefix remain unchanged. The local runtime was restarted to apply the migration after the development restart helper rejected SIGUSR1 on Windows; the existing launcher shutdown/start path succeeded. The original WNDR source and production profiles remain untouched. No commit, push, installer or ARC desktop package rebuild was performed. iOS Simulator Safari is unavailable on this Windows host. This implements the explicitly requested material option within the appearance phase; the broader wallpaper/filter, scaling, native acceptance and public-release gates remain open.

### Alignment checkpoint 64 — Windows title bar and personal installer

ARC desktop 0.42.4 implements the approved Windows chrome and personal-installer plan. The application uses a 36px native Window Controls Overlay with the existing ARC artwork, centered document context, palette-derived caption colors, inactive-window dimming, Liquid Glass surfaces and a fine lower border. Windows retains its resizable frame and native caption buttons; the normal menu is hidden until Alt, and View now exposes Full Screen with F11. The renderer capability is optional, preserving older shells and browser/non-Windows layouts. Caption-color IPC accepts strictly validated colors only from a registered application main frame. No server, database, host-daemon protocol, SDK or CLI feature contract changed for window chrome.

One shared inset follows Window Controls Overlay geometry, including page zoom, and positions the application, fixed sidebars, panel shelves, drawers, floating controls and image preview below the bar. Existing page headers and native browser bounds synchronization remain in use. Production loading/error views use matching geometry, the existing strict CSP and validated preload state for native fullscreen. Native focus describes the BrowserWindow, so activating an embedded browser does not incorrectly dim the title bar.

Actual packaged testing exposed Electron's Windows fullscreen event ordering: the synchronous getter still reports the previous value inside enter/leave callbacks. The bridge now retains the known state carried by each event, and both preload and renderer reject stale initial snapshots. A failing pre-fix native regression is retained. Testing also found the image lightbox's existing broad direct-child CSS rule hid its custom close and navigation buttons; the component now uses the shared dialog's hideCloseButton option, preserving its own controls and placing them below desktop chrome.

The isolated worktree B:/arc-desktop-0424-20260914 contains an exact 6,282-file snapshot of the original integrated source, including the prior composer, assistant, provider-discovery, motion and Liquid Glass changes. Original HEAD 06aeaa994942ae7527dc49d2268c1f801e8542a0 and index SHA-256 070dda450f794b82131dbcc7ed87174a5fc64c246a4af8fdcf31f3dca4f794af are preserved. The source snapshot commit is 1e8808840138c3c47784b55fcd12082e3c0f1836 on codex/arc-desktop-0424. Integration is restricted to 42 reviewed changed/new files after checking every original source hash; the existing progress prefix is preserved byte-for-byte. No commit or push is made in the original checkout.

Close-path review also established two destroyed-window lifecycle failures: the browser broker read the host WebContents ID after native destruction, and embedded-view cleanup recomputed its key from the destroyed host. Both now capture immutable identities while the window is alive. Two real-manager regression cases reproduce the exceptions before the fix and pass afterward, covering empty and browser-populated closing windows, surviving-window isolation, lease invalidation and absence of sends to destroyed contents. These tests establish the code defects; a prior diagnostic inspector stall alone is not treated as proof of a native exception dialog.

Verification passes 270 distinct focused tests: 52 renderer/layout/image-preview cases, 170 desktop/native-contract/browser-manager cases, and 48 installer/build-configuration/safety cases. Relevant app, desktop and contract Turbo typechecks, scoped lint/format checks and git diff checks pass. The production installer build passes all 15 Turbo tasks; existing chunk-size, package metadata and dependency/deprecation warnings remain. Explicit uncached dist:windows and smoke:installer Turbo tasks use the existing build and guarded lifecycle machinery. Windows signing validation is independent of macOS credentials.

The final ARC-0.42.4-x64.exe is 203,850,402 bytes with SHA-256 b03ebd650e8189be0360e33f3b7221b0b23b974bf918ed260bd5120c2d1d1cb5. Windows reports NotSigned for both installer and packaged application. The build uses --publish never and no ARC update feed, so publishing and automatic updates are disabled. Electron, the private Node runtime, frontend, server, daemon, CLI, plugins and native Context assets are included. Per-user NSIS retains installation-directory selection, shortcuts and data-preserving uninstall. Git and coding-provider installation/authentication remain external prerequisites; the installation guide explains the distinction between ARC's private Node runtime and npm-based provider setup.

The packaged UI evidence includes 19 passed checks in packaged-titlebar-6twiE1 before the final three-file native close fix: actual Win32 drag, double-click maximize, caption minimize/maximize/restore, right-edge resizing, Snap Layout flyout, Alt menus, Alt+Space, real F11 fullscreen, narrow layout, page zoom, centered new-thread composer, attachment preview controls, draft-preserving panel animations, drawer interaction, dark/light Liquid Glass, reduced-transparency and forced-color emulation, centered/truncated document title, multiple-window creation, saved Agent Studio draft and assistant, the installed Codex 0.154.0 GPT-6 Astra catalog, and real embedded-browser bounds/focus. The run's final close diagnostic stalled, so its overall report remains failed; the 19 earlier checks are preserved individually, not relabeled as a fully passed run. Its first production Opening ARC load and second-window load both measured a 36px bar and inset. Two initial fetch failures lacked captured URLs; no renderer Runtime.exceptionThrown was recorded, and these logs are not claimed to be wholly error-free.

Independent packaged-titlebar-A8JSvM passed a separate process with device scale factor 1.25, including native-frame screenshot and graceful process/port cleanup. This is per-process scaling on the existing 96-DPI Windows host, not certification across physical monitors. Packaged-titlebar-4koVVh rendered the real production ARC stopped error screen after stopping its owned runtime, verified the 36px bar, native fullscreen with zero inset and restoration, and 80%/125% page zoom matching the overlay geometry. F11 input did not toggle the error view during that diagnostic; its fullscreen verification used BrowserWindow.setFullScreen. Fullscreen via actual F11 passed in the normal application. The frontend/local-view code is unchanged in the final close-fix rebuild.

Final-build packaged-titlebar-D2PD1q passes four native lifecycle checks. Both actual Windows caption close clicks succeeded with hit-test code 20; no programmatic close fallback ran. Closing the second window destroyed its real embedded browser while the first window stayed visible and the same main process remained alive. No uncaught exception or owned native error dialog was recorded. Closing the last window exited with code 0, closed server/daemon ports and removed the runtime marker. Foreground state was restored. Its disposable thread ledger contained no provider events, queued messages or turn preparations; provider inference was not performed.

The guarded installer run also exposed pre-existing harness defects. Its version parser rejected the installed executable's valid Windows ProductVersion 0.42.4.0, and cleanup expected Uninstall ARC.exe instead of the configured Uninstall ARC IDE.exe. The harness now normalizes only a trailing zero Windows revision, checks the installed version against the actual installer before launch, and derives the exact uninstaller name from its strictly validated configuration. Regression tests reject nonzero revisions, malformed versions, wrong versions, unsafe names and destructive configuration changes. The first harness-owned installation was removed with the actual uninstaller after exact path/realpath/registration checks; the subsequent state had no ARC installation, shortcut or process. Verification-only scripts, declarations and tests changed after the final package build and are not included in the runtime payload. A separate initial test-environment failure came from the Codex PowerShell 7 module path inherited by Windows PowerShell 5; the external runner clears that child-only environment variable so Windows can load its own signature module. No host setting or installer safeguard was changed.

The first reinstall smoke preserved the project and file bytes but exposed a Context verification resume defect: it discarded prior Context pass/reference evidence and reused operation IDs deliberately canceled by the preceding watch-cancellation check. Resume now requires a previously passed report for the same QA project, verifies the retained reference ID/revision/hash and original text before import retry, and continues with new indexing IDs. It retains every semantic-model, hybrid-retrieval, provenance and cancellation assertion. Three additional regressions cover the resumed operation sequence and reject lost/changed reference evidence before retry. Installer diagnostics now distinguish a smoke-check failure from an actual project-identity mismatch. The failed diagnostic run's owned installation was cleaned successfully, and its reports remain preserved.

Final guarded installer lifecycle passed through Turbo at B:/arc-desktop-0424-20260914/.arc-verification/NSIS install Δ 4wZv9N using the delivered installer SHA-256. Installation used only its owned spaced/Unicode per-user directory. Both the initial installed-app run and resumed run passed all 13 smoke checks, totaling four actual application launches. These verify packaged renderer/composer, all eight provider catalog entries, native Context model and hybrid retrieval/provenance/reference persistence/watch cancellation, PowerShell ConPTY input/output, bundled CLI without Node on PATH, Unicode workspace file writes, Agent Studio canonical draft save/reload, and graceful runtime cleanup. The same project proj_4pzup4jb8t survived reinstall; resumed Context passes continued at 3 and 4. Same-version reinstall preserved QA data and existing profiles byte-for-byte. Silent uninstall removed the exact installation, registry entries and shortcuts while retaining project/profile bytes. The lifecycle result is passed and its Turbo process exited 0.

The installer, SHA-256 file and brief setup instructions are delivered under apps/desktop/release/. Permanent evidence, including screenshots, raw reports, source integration hashes, signature metadata and build/test/lifecycle logs, is collected under .arc-verification/desktop-0.42.4-20260914/verification.md. The working evidence remains at C:/Users/Collin Chen/AppData/Local/Temp/arc-desktop-0424-20260914. This completes the approved personal installer/chrome scope. It does not certify a clean Windows machine, physical multi-monitor DPI transitions, authenticated provider inference, iOS Safari, public signing/distribution, cross-version update migration or rollback. Phase 8 public-release gates remain separate; no public release or automatic update feed is enabled.

## Checkpoint 65 - ARC animated personal installer (2026-09-14)

Implemented the requested ARC-themed setup in an isolated snapshot at B:/arc-installer-theme-20260914 on codex/arc-installer-theme, preserving all 6,289 snapshotted original files before scoped integration. Desktop version is 0.42.5; the previous delivered 0.42.4 installer remains unchanged. The native NSIS flow now has a dark ARC rail, original mark and wordmark, subtle four-second light/orbit animation, readable native controls and a consistent Finish page. Silent mode exits before allocating theme resources; Windows client-area motion preferences or high contrast choose a static bitmap. Four artwork variants cover 100/125/150/200 percent scaling, and intermediate-DPI footer spacing was corrected in source. No server, daemon, SDK, CLI, provider or database behavior was changed.

Artwork generation uses the existing sharp version and a small validated silent BI_RGB AVI writer, with a Turbo task declaring source inputs and generated outputs. The native SysAnimate32 control provides motion without a browser or script timer. Generated setup assets are excluded from the installed app. Actual ASAR inspection caught electron-builder normalization reordering exclusions around the scaffold FileSet, duplicating artwork and defeating source-map exclusion. Keeping root patterns contiguous fixed that cause; a real normalization/filter/copy regression protects branding, scaffold and exclusions. The final ASAR reports 9271 entries and zero installer-art entries.

Final verification: 46 focused desktop tests passed, desktop typecheck passed, scoped formatting/lint and diff checks passed. The initial cold test run hit an existing 5-second Context fixture import timeout; its unchanged rerun passed, and the final suite includes the new matcher regression. The final full Turbo dist:windows build passed all 16 tasks. The exact frozen NSIS include SHA-256 is c7140e7140716484dc24425ffbe7479c25863b9cbea529e22892795fcfd0332a. Earlier preview paint failures and the subsequent fixed screenshots are retained, rather than relabeled as passed. Actual native preview checks covered radio selection, Tab/Shift+Tab, Directory input/Browse focus, 20 distinct animation frames with 1,448-9,192 changed pixels per adjacent sample, checkbox toggling, normal Finish exit and /S exit0 with no visible window. The final Finish wrapper uses exact MUI dialog/image handles, removing the stock image and preserving readable checkbox text.

The final packaged app passed all 13 smoke checks over two isolated launches at C:/Users/COLLIN~1/AppData/Local/Temp/ARC packaged smoke Δ fwSphZ. This includes renderer/composer readiness, persisted Unicode project/workspace binding, native Context model/retrieval/provenance/reference persistence, bundled CLI without Node on PATH, PowerShell/ConPTY input/output, canonical agent draft save/reload and graceful owned-process cleanup. The final installer is 203,382,538 bytes, version 0.42.5, Authenticode NotSigned, SHA-256 f802ff39be1cb46e205002e7bd299ced92d6200ff75f79f03f97052d40b11c46. Public publishing and automatic updates remain disabled.

The actual guarded smoke:installer task exited1 before installation because existing Desktop and Start Menu ARC shortcuts point to B:/downloads/ARC IDE/ARC IDE.exe. Their inventory remained unchanged. Windows Sandbox and Hyper-V VM Connect were unavailable; no installation, registration, profile or global accessibility setting was modified to bypass the guard. Therefore real install/reinstall/uninstall for 0.42.5, extraction-time animation, alternate physical DPI and live motion-off/high-contrast behavior remain unverified. The native preview used the production include with an empty install section and no-op launch callback; it is not installer-lifecycle or clean-machine certification. The prior checkpoint64 lifecycle result applies only to 0.42.4.

The new installer, checksum and setup instructions are delivered under apps/desktop/release/. Durable screenshots, logs, final package/preview reports, signature metadata and source hashes are collected in .arc-verification/installer-theme-0.42.5-20260914/verification.md. The approved installer-theme scope is implemented; public release, signing, authenticated provider inference and cross-version migration remain separate gates.

## Checkpoint 66 - Collapsed sidebar header collision (2026-09-14)

Reproduced the user's screenshot in the actual 0.42.5 Windows package using an isolated profile. The fixed Toggle Sidebar button overlapped the Threads heading and All projects subtitle, and the Threads reopen button when its pane was hidden. The thread browser did not use the top-left reservation already implemented by AppPageHeader. The extracted shared hook now supplies the same browser, Windows, compact and macOS/fullscreen policy to both inline thread headers and their reopen row; drawer headings retain their original inset.

The first 0.42.6 diagnostic passed settled geometry but exposed three transient heading-overlap frames while navigation expanded, plus three geometric overlap frames during the thread surface's slide. The final change matches inset padding to the sidebar's 180ms curve and reduces the surface slide from 12px to 4px. Reduced motion disables these transitions. The diff remains within the approved Threads layout and motion maintenance scope, with no change to execution or provider behavior.

Final verification passed 30 focused renderer tests across seven files, app typecheck, formatting, diff checks and lint with zero errors (187 package warnings). Three layout-test mocks were updated for the newly shared helper; the initial failed test log is retained. The final full Turbo Windows installer build passed all 16 tasks. Actual packaged Windows QA at C:/Users/Collin Chen/AppData/Local/Temp/arc-sidebar-collision-20260914/final-p3R7Lf passed 16 settled cases and 862 sampled animation frames with zero painted overlap. Checks cover repeated navigation and Threads toggles, hidden-pane reopen controls, 125 percent page zoom, narrow drawers and reduced-motion emulation. Native screenshots and actual renderer mouse input were used; the Browser plugin was not available. No renderer exceptions or console errors were recorded. Network cancellations remain listed in the raw report rather than being represented as an entirely clean network log. The owned app exited 0, closed its ports and removed its runtime marker.

ARC 0.42.6 is an unsigned personal installer (203382839 bytes), SHA-256 d7799c91b396ada30c3f6a9587a89a1957530f613e261dacddf7b636b5e8dbf3. Publishing and automatic updates remain disabled. It includes the animated installer and existing custom title bar. Installer lifecycle was not rerun against the known existing ARC installation; no installed app, user profile or shortcut was changed. This targeted packaged UI check does not certify clean-machine installation, physical DPI, macOS, iOS Safari, provider authentication/inference, cross-version migration or the broader Phase 8 release gates. A source review also identified a potential transient macOS fullscreen-reserve overlap, recorded as a cross-platform follow-up outside this Windows verification.

Scoped integration copies seven files from B:/arc-sidebar-collision-20260914, preserves the original HEAD/index and unrelated source bytes, and retains the previous delivered installers. Installer and setup instructions are under apps/desktop/release/. Durable evidence is at .arc-verification/sidebar-collision-0.42.6-20260914/verification.md.

## Checkpoint 67 — Windows installer choices (2026-09-14)

- Implemented the requested standard setup choices in ARC 0.42.7: installation scope/folder remain available; Additional options independently enables Desktop and Start menu shortcuts; Finish offers Launch ARC even without shortcuts. Choices default on for a fresh install, persist per installation, and survive Back navigation in the wired callbacks. Native stock installer/update/uninstaller behavior remains in use.
- Fixed invalid registry-type fallback, preference caching before old-registration deletion, final silent all-user scope preload, and update retention of user-deleted shortcuts. No server, daemon, SDK, CLI or provider feature changes. This cycle remains within the approved personal Windows installer scope.
- Evidence: 59 focused desktop tests across 4 files passed, including actual native NSIS shortcut cases and both compiler branches; desktop typecheck passed; scoped format and diff checks passed; full production build and final NSIS rebuild passed. Installer ARC-0.42.7-x64.exe: 203383235 bytes, SHA-256 e80e01fc3d9cc173c27fb69be6edff5e3a2fe9b1ae95636bdac9f246ecb4d458, Authenticode NotSigned. Public publishing and automatic updates disabled.
- The guarded lifecycle harness refused the pre-existing ARC installation/shortcuts before mutation. No installation, reinstall or uninstall of 0.42.7 is claimed. Required Windows computer-use initialization failed twice (kernel assets os error 3), so no new screenshots or interactive/keyboard/DPI verification are claimed. No disposable Windows environment was available. Native scope/launch-target tests do not certify elevated installation or actual installed-app launch.
- Built in isolated snapshot B:/arc-installer-options-20260914; guarded integration preserves original HEAD/index, all unrelated baseline file bytes and earlier installer deliveries. Detailed evidence: [installer-options-0.42.7 verification](../.arc-verification/installer-options-0.42.7-20260914/verification.md).

## Checkpoint 68 - ARC branding and Windows reliability (2026-09-15)

Completed the requested product-branding pass in isolated snapshot B:/arc-branding-20260914, retaining compatibility identifiers and truthful upstream/third-party provenance. ARC display copy, logo assets, built-in help/prompts and bundled offline release notes now agree; catalog refresh no longer restores legacy marketplace labels. Fixed Windows npm launching, local Git parsing/cache length, remote Tasks file paths and the observed first-run draft reload visibility issue. No protocol/schema or global Windows configuration changes. This remains within branding/product reliability scope, not Phase 8 public release.

Final renderer 97, CLI 55, Tasks 25, desktop startup 14 and category 2 tests passed, with earlier desktop 38 and migration 48 checks passing. All changed-package typechecks passed; the two remaining long Git rollback cases passed with 30-second budgets after the combined 76-pass run. Platform symlink failures, the unchanged hero-tool fixture 409 and an unclassified RPC timeout remain explicitly documented. Final production installer and all 13 packaged functional checks passed, plus light/dark, Liquid Glass persistence, plugin prompt, New thread, draft reload and narrow renderer checks without console errors.

Installer ARC-0.42.8-x64.exe is 203516186 bytes, Authenticode NotSigned, SHA-256 b21225af845c5ac8fbc0eb212c32d3fdc5e8c05ae0b2f1f726904519ec1d686a. The guarded installer lifecycle refused an existing ARC installation/shortcut before installation. The user installation was preserved; install/reinstall/uninstall of this version remains unverified. A disposable Windows environment was not available on this host. Public publishing/automatic updates remain disabled. Windows reported a 0xBE crash before continuation; the driver and ARC causality remain unconfirmed. Resumed verification was sequential.

All 6296 original source files were verified before guarded integration of 343 changed/new files. See .arc-verification/branding-0.42.8-20260914/verification.md for screenshots, raw checks, source hashes, crash triage and limitations. Historical versions are not used as certification for this installer.

## Checkpoint 69 - GitHub release and updater preparation (2026-09-15)

The user explicitly authorized publishing the integrated source and stable Windows installer to grandmasterhilbertporcupine/ARC-IDE, with a README download and GitHub-backed updates. That replaces the earlier local-only publishing default; this remains an unsigned personal build, and the signed/clean-machine/provider requirements in Phase 8 are not claimed complete.

Prepared ARC 0.42.9 in isolated clone B:/arc-release-20260915, based on the destination repository's initial commit and all 6298 original source files. The original checkout has an unavailable inherited Git parent; its HEAD, index, remote and unrelated edits are preserved rather than rewriting that history. Required upstream licensing and the destination repository's existing copyright are retained. Source and release assets are separate: installers, QA profiles, credentials, dumps and dependencies are excluded from Git.

Default stable Windows builds now use electron-updater's public GitHub provider for this repository and its published stable releases, plus ARC's existing JSON version feed. Downloads remain pinned to the selected tag and validated by SHA-512. The packaged name arc-desktop supplies a separate ARC updater cache without renaming internal compatibility packages or changing installation/profile identity. An explicitly empty build-time override still disables updates; existing nonempty HTTPS custom feeds remain supported. Earlier local builds require a one-time manual install. Failed network/native update checks now display a truthful failure and Retry action instead of Up to date, while retaining valid prior state. The optional bridge field remains compatible with older shells; no server/daemon protocol change was needed.

The README, release guide, installer setup instructions and version metadata are updated. A release asset task validates the embedded GitHub identity, native/JSON feed agreement and actual installer hashes before emitting SHA-256 and setup files. A guarded Windows GitHub Actions workflow builds, tests and verifies the package and installer before publishing a complete draft as latest. All inherited upstream-only workflow jobs are repository-restricted. A fresh-checkout generator dependency cycle discovered during installation was corrected through an existing lightweight constants module; generation passed with plugin-build's generated modules absent.

Final production source is commit 56ed3767bf7da33f57fb94501befcb84b9747794. The embedded package reports that exact commit, arc-desktop and 0.42.9. Verification passed 93 desktop tests, 18 desktop-contract tests and 31 update-screen tests, relevant desktop/app/contract/server typechecks, scoped formatting/diff checks, production packaging and release asset validation. All 15 workflow YAML files parsed and repository guards were checked; no GitHub Actions execution is claimed. Initial installation/test diagnostic failures and inherited formatting/diff warnings remain in the evidence rather than being represented as passing runs.

The actual final packaged application passed 13 functional checks over two launches: Electron/server/daemon startup, provider catalog, real Context model and retrieval, CLI without external Node on PATH, PowerShell ConPTY, Unicode workspace writes, restart persistence, Agent Studio draft save/reload and owned shutdown cleanup. Additional packaged UI checks passed for ARC copy, light/dark themes, Liquid Glass persistence, plugin prompt, centered New thread draft reload, narrow layout and update failure/retry, without recorded UI console errors. Root inspected the screenshots. GitHub returned HTTP 404 and No published versions on GitHub because the repository has no published release; the real UI correctly displayed Couldn't check for updates. This proves the unavailable-feed behavior, not a successful public update download/install.

Installer ARC-0.42.9-x64.exe is 203518564 bytes, Authenticode NotSigned, SHA-256 30b3670333135c6fa431fffb77a166cc55c2f7df9f2e9bcaa068b0b454681f65. The installer lifecycle guard refused an existing ARC installation/registry entry/shortcut before mutation. It was repeated only to capture full output after Turbo's errors-only output omitted the reason. The installed app and data were left untouched; no disposable Windows environment was available. This version's install/reinstall/uninstall, cross-version update, clean-machine, authenticated provider inference and new physical DPI/native-caption/macOS/iOS acceptance remain unverified. Heavy work was serial following the reported PC crash; no driver, BIOS or Windows graphics settings were changed.

Publication is blocked: the available GitHub identities have read-only repository access. The publishing helper rechecked access and stopped before pushing source or creating a release. README download links are prepared but not live. Grant Collin-Mesa write access or authenticate GitHub CLI as an owner/writer to finish the authorized publication. The verified source, installer, complete release asset set and exact-commit publishing helper are retained locally. Durable evidence and resumption instructions are in .arc-verification/github-release-0.42.9-20260915/verification.md. Guarded source integration verifies original file hashes and preserves HEAD/index; a separate record identifies copied files and installer delivery.

## Visual teams implementation in progress — September 15, 2026

The owner subsequently paused publication and approved the visual teams, assigned skills and live Preview milestone. No push or release is authorized. Development is isolated in B:\arc-visual-teams-20260915 on codex/visual-teams, based on the integrated release source plus a byte-verified 6,302-file snapshot of the current original checkout. The original checkout, index and HEAD remain untouched during implementation. External evidence is under C:\Users\Collin Chen\AppData\Local\Temp\arc-visual-teams-20260915.

Initial slices connect version-2 agent/team fields while retaining version-1 serialization, immutable skill bundles, Team/Workflow editing, managed Preview configuration and native feedback. Required skill support is checked by the daemon/runtime rather than silently ignored. Collaboration stores source-pinned bounded reports and run-scoped directional messages; response slots are being integrated into immutable Workflows admission. No provider savings are claimed.

First focused verification ran serially: 21 tests passed and one new builder assertion failed because canonical graph ordering differed; the failure is retained in test-slice-1.log and is under correction. New collaboration storage, assigned-skill storage and organization tests passed. Initial typechecks found builder typing and Electron callback/fixture mismatches, now being corrected; broader checks and actual UI/package acceptance remain outstanding. Dependency installation succeeded; no Windows system or graphics configuration was changed. Alignment review retains all approved requirements, including authoritative addressed sends and bounded response admission; UI wiring alone is not completion.

The next serial slice passed 97 ARC compiler, runtime, template, builder, bounded-reader and collaboration tests. Preview/file/assigned-skill server checks passed 15 tests before the stronger host-owned readiness probe and larger HTML-asset rotation landed; those new paths require a fresh pass. SDK/browser evaluation and public declarations passed 16 tests after adding the addressed-dispatch export assertions. Template setup, editable copies, panel and usage checks passed 16 tests. Multi-recipient execution exposed a validation callback mismatch and is being rerun after correction. Native browser regression tests exposed stale console-event and preload-capability fixtures; their 64 failures are retained, not counted as successful verification.

An alignment review confirms that authoritative Send routing, persisted recipient chips, exact revision/skill bindings, original required gates, one shared budget, independent combined review and Windows-owned Preview remain required. Addressed continuation admission and packaged/provider acceptance are still in progress. No savings, fresh-install certification or complete milestone is claimed from the component checks. The original source checkout and installed ARC remain untouched.

Further serial checks passed 49 combined-team compilation, bounded-dialogue, source-pinned handoff and review-permission tests; 86 desktop browser/feedback/IPC tests; and 45 renderer/client-core Preview, HTML lease, recipient, feedback-draft and panel-state tests. The generated workflow source now uses indexed step references to fit the existing source-size limit without removing communication or authority checks. Git and directory multi-team candidates retain their final integration checks and independent review.

The broader ten-package typecheck passed eight packages and exposed new continuation typing plus a host-daemon WebSocket declaration mismatch; owners corrected those findings and a fresh check is required. Provider-runtime capability checks passed 28 tests and the Preview CLI check passed; the host slice passed 18 tests with two Windows-specific failures in inherited executable-mode/file-symlink assumptions, retained in test-host-runtime-cli.log. A real Windows process probe separately identified its owned HTTP child and rejected an unrelated port listener, then cleaned up its child. This is not yet packaged Preview or authenticated provider acceptance.

Guided skill editing, assistant-authored pending skill proposals, recoverable addressed follow-up queues, changed-recipient graph continuation and native HTML Preview are integrated and undergoing full ARC regression checks. Follow-ups pin composition at Send and preserve verified candidates, consumed shared limits and prior verification commands. Hierarchy remains distinct from communication/delegation grants. No new publishing authority has been given.

The ten-package typecheck subsequently passed all fifteen Turbo tasks. A full ARC regression pass exposed a verification-runner PATH error that hid Git, two stale template/tool fixture expectations, and a historical workflow-byte compatibility regression. The runner now retains Git; compact workflow source is restricted to new version-2 teams, preserving older compiled source bytes. The targeted native Git/directory/compiler recovery pass passed 142 tests, with two continuation fixture failures subsequently corrected. The latest continuation pass has thirteen passing cases, including immutable changed recipients, retained required checks, source verification after sealed retries, cancellation before actual admission, restart recovery and shared repair consumption.

Final scoped Windows checks passed twenty injected-skill/readiness tests and seven packaged-harness safety tests. Twenty-eight control-driver tests include a worker-closing-during-await regression that rejects late report/message writes. The earlier Windows file-mode and junction-cleanup fixture failures remain in their original logs. Production packaging and real provider/browser acceptance have not run yet; prepared harnesses and passing unit tests are not those results. Alignment remains the complete connected workflow, with public publishing, unattended work and automatic promotion outside this milestone.

Further review closed distinct-first-Send admission races, added durable Retry saved Send across UI/SDK/CLI, and prevented late screenshots from entering a different conversation or surviving a same-document browser navigation. Server addressing/retry checks pass five cases, renderer retry/feedback checks pass four, timeline checks pass seventy-four, and the native view-manager suite passes sixty-six. Usage/results/collaboration checks pass twenty-one cases, including exact admitted main-response attribution and missing or reset usage baselines. All eleven affected package typechecks have passed across the final full pass and the focused SDK/CLI correction. App lint passes with 192 React compiler warnings and zero errors; warnings are retained in lint-app-2.log. The CLI regression pass found an eager Preview SDK route lookup that broke thread-only clients; its correction and rerun are in progress. No passing CLI/package result is inferred from the typechecks.

The original-checkout integration dry run verifies all 6,302 baseline file hashes, unchanged HEAD/index, and no collisions with the proposed new files. It performs no copy. [ARC-VISUAL-TEAMS.md](ARC-VISUAL-TEAMS.md) now explains the Team/Workflow distinction, skills, addressed work, recoverable failures and Preview controls in user-facing terms. Live packaged acceptance and the two bounded provider comparisons remain required before delivery.

The Preview SDK now resolves its route lazily, preserving thread-only transports; the corrected CLI regression pass has 65 passing cases. The first production Windows package succeeds through fifteen serial Turbo tasks. It remains an unsigned local 0.42.9 build, with no release or installer publication. The exact 6,388-file package-input manifest is retained as package-1-source.json, alongside package-windows-1.log and package-signature.json. The packaged executable SHA-256 is E073F83BF6F7539B879F0313861AE27ADCC907A40B2CCEDF68C48D3F5D4ABD1B; this executable hash alone is not a manifest of the unpacked application resources.

Packaged acceptance is finding issues and remains incomplete. Both first attempts passed native startup, renderer, Unicode project persistence, Context retrieval/provenance and the bundled CLI/PowerShell terminal checks. Preview attempt one exposed the harness invoking a GUI-subsystem Electron child without waiting. A bounded process experiment proves that piping its output through Out-Default waits for completion. With that correction, attempt two retains a running server terminal and discovered URL, but the host readiness probe still fails. The first Team UI attempt reached Efficient Build setup but incorrectly expected the compact library to be open; the revised harness follows the visible library and assistant-panel controls and is being rerun. Retained failed artifacts are not counted as passing feature acceptance.

The readiness failure is traced to a missing terminal.probePreview variant in the manually enumerated online RPC success-response union; the host's successful response was rejected before reaching Preview. The response contract and its regression coverage are being corrected. Read-only final review also found that Preview inherited terminal close acknowledgement even when process termination failed, and that early addressed Send routing omitted normal worker-sender checks. Both failures are being repaired within the approved ownership and controlled-admission requirements before the next package build.

Team UI attempt three passed real compact-library Tab/Enter addition, pinned member inspection, keyboard Undo, native agent-palette drag/drop and native canvas movement. A separate native Electron experiment established that the harness must send the Enter character event, not only key down/up. The attempt then failed its native select helper and remains failed overall. Screenshots show the compact and expanded builder; the source app has not been changed to accommodate incomplete test-driver input.

Subsequent native builder attempts passed hierarchy creation without implicit permissions, Undo/Redo, directional messaging handles, guided/raw skill round trips, supporting-file pinning, linked member/task selection, Delete/Undo, workflow dependencies with a required approval, editable bundled-agent copies, publishing and reload persistence. Remaining harness corrections used proven Electron key names, awaited asynchronous Markdown parsing and checked actual details visibility. Attempt seven captured the final recipient failure precisely: thread and workspace paths returned 200 and ARC returned the expected published Team/Agent suggestions, but never-attached thread storage returned 409. The prompt hook incorrectly hid healthy suggestions when any source failed. It now retains usable results; focused partial/total-failure regressions and a fresh package are required.

The corrected daemon contract suite passes all 40 cases, including the current protocol 190 and probe/stop response variants. Terminal ownership/cleanup verification passes 34 cases with two platform skips. Strict Preview Stop now waits for an actual exit acknowledgement. Unknown exits block automatic replacement and expose Retry Stop. Explicit Detach lost session requires the exact session, current revision and acknowledgement, preserves up to twenty bounded evidence records, and never represents detachment as process cleanup. The server lifecycle/recovery suite passes fourteen cases, including stale detach rejection, missing-session recovery, unchanged process ownership and foreign-port rejection. The CLI regression slice passes 69 cases across addressing, Retry and Preview controls.

Final sender review also corrected the supported addressed creation/Send/Retry paths to preserve and validate the originating worker identity before receipt replay or new admission. This follows ARC's existing trusted-local-process boundary; caller-supplied identity is not an authenticated sandbox against arbitrary local HTTP clients. One new server regression initially omitted the required Send mode and was rejected at schema parsing; its fixture now supplies mode and asserts exact policy errors. Its rerun and renderer checks are pending, not inferred from the passing CLI cases.

The final sender fixture also needed to await asynchronous worker preparation before asserting against an actual thread. The corrected seven-case real-database sender/admission suite passes with exact policy errors and receipt replay protection. Six renderer cases pass for partial mention failure, total failure, Preview controls and explicit detach review. All sixteen Turbo tasks in the eleven-package final typecheck pass. The detached-history display uses the project's supported array operations; no compiler target was expanded. App lint passes with zero errors. Source is being frozen for the second production Windows package; the first package's failed recipient/readiness results are not superseded until the rebuilt app is exercised.

The third successful production package passed all fifteen Turbo tasks after a locked executable was traced to an owned preview fixture left by the first failed harness. Its exact process identity and fixture path were verified before terminating that child; the installed ARC was untouched. The Preview harness now independently records and checks each owned fixture process, including PID reuse and cleanup uncertainty. Package 3 has an 8,726-file output manifest (639,756,161 bytes), SHA-256 manifest digest 6cc204801bd1b959ddad68424723c738e13c0316a0e4256d2bf24b97696f2da7; its executable is NotSigned.

The next actual Windows Preview run retained its terminal but could not prove the owned listener because the native adapter copied ConPTY's PID before its asynchronous initialization. The adapter now reads the native PID when used. The regression exercises the production adapter across unavailable, late PID, ready probe and native exit states. The focused terminal/probe pass has 35 passing cases and two platform skips. The first typecheck rejected a null optional thread ID in the new fixture; removing that field produced a passing focused host typecheck. Packaged Preview acceptance remains pending the rebuilt adapter.

The first packaged provider attempt passed all real Team UI checks: compact keyboard add/Undo, native drag/drop and movement, hierarchy without implicit permissions, directional handles, guided/raw skill round trips and supporting files, linked task selection, workflow dependencies and approval, editable bundled-agent copies, connected template setup without overwritten customization, and persistent published Team/Agent recipient chips through text removal and reload. Selecting recipients dispatched no provider work. The provider harness then failed because its duplicate HTTP Send omitted the required mode. Retained database evidence shows zero admitted provider calls, no worker threads and no native effects; cancellation was requested but settlement was not proved before shutdown. This attempt does not count as a completed provider comparison. The helper now supplies mode and is tightening its bounded cancellation receipt. No source push, release or public publication has occurred.

Visual review of the native template screenshot found overlapping permission labels. The Team view now starts with Reports to and offers a separate Show filter for each relationship or All connections. Draw remains the editing mode; creating a relationship reveals it and selecting a connection keeps it inspectable. Hierarchy links use top/bottom handles. Fourteen focused builder/organization cases pass, including keyboard creation, Undo behavior and preservation of all hidden definition edges and permissions.

Provider attempt two passed duplicate addressed admission and, after approval of its exact disposable null-candidate plan, stopped before agent dispatch because the fixture's one-shot commit identity was not available for later worktree creation under its isolated HOME. Both fixtures now persist identity only in their owned repositories, including baseline clones; global Git settings are unchanged. A bounded helper approves only the exact initial plan control and retains its receipt, never candidate review. The failed run settled as cancelled with zero admitted calls.

Provider attempt three passed native source and fork operations, then exposed a scheduler race. A newly dispatched attempt could be marked uncertain while its executeStep RPC was still live, causing ARC's generation/state guard to deny admission. The run charged one call, but retained records show no worker or provider turn. It settled as cancelled. The scheduler correction will preserve active-call identity without weakening unknown-outcome, pause, cancellation or restart safeguards; no replacement is silently created inside an acceptance attempt.

A final alignment review also found that changed-recipient continuations incorrectly promoted all prior check commands to mandatory checks. The correction preserves prior required any/all and check-condition semantics without making unrelated discovery checks mandatory. Both new runtime corrections require focused tests and a rebuilt package. The approved workflow, explicit permission model, immutable pins and final independent review remain in scope; unattended Factory work, automatic promotion and publication remain excluded. Heavy verification continues serially.

The scheduler fix passes 56 focused Workflows cases, including four deterministic real-SQLite dispatch interleavings for normal completion, pause, cancellation and a lost native reply. Reconciliation exempts only a live, current-generation, un-aborted executeStep call; ARC's admission guard is unchanged. The redundant narrowed comparison found by TypeScript was removed, and the focused scheduler typecheck passes.

The completed continuation projection now retains authoritative compiled check/review pairs as well as required any/all and supported condition semantics. Ten focused continuation cases pass after correcting one stale presentation position in the new test fixture. The tests execute the lowered graph across both conditional branches, reject a failing retained final check, keep inactive alternatives inactive, preserve optional discovery and verify idempotence. Approval/review-controlled or nested verification conditions that cannot be safely projected produce a saved actionable follow-up error; the Threads guide documents retaining the original recipients and gates to continue. All nine tasks in the five-package final runtime/UI typecheck pass. The fresh integration dry run verifies 6,302 baseline files and 270 proposed changes without conflicts or copying.

Package 4 passed all fifteen production tasks and includes the native PID, scheduler and continuation fixes. Its 8,726-file unpacked manifest digest is 4153e639ff1229faf6afc7b798f7f1935e4576151a98201eefa205692fe6ee1b. The unsigned executable SHA-256 is 37c2e795716707978bdfab627eb56d2a52b1f928afd3331c0e0271bbaca221ee. Fifty-nine additional Workflows continuation/rule/version-2 cases passed after the scheduler correction. Final scoped source review found no further pause, stop, shared-budget or restart-uncertainty issue; that review is not a substitute for native acceptance.

Provider attempt four passed eleven packaged Team UI checks, including the readable default hierarchy/filter, and completed the actual six-call reader, two writers, one repair, independent reviewer and main-conversation response. Retained receipts prove both writers read the source-pinned report and assigned-skill proof, the deliberate check failed, repair and final checks passed, and the independent reviewer approved the exact final candidate. The scheduler completed without reconciliation error. The harness then failed its raw check-file hash assertion before baseline comparison. Read-only diagnosis proved identical original/final Git blobs and identical initial/final candidate bytes; Windows Git's system autocrlf setting changed LF to CRLF at checkout. Both disposable fixtures now commit a check-file LF attribute; exact byte assertions remain enabled and no system Git setting changed. The original failed attempt and its byte-level diagnostic are retained. No completed efficiency comparison is claimed yet.

Preview attempts four through six exposed test-driver sequencing failures, retained separately from product results. The inspector now waits for Electron application readiness; browser creation waits for the visible conversation and effect registration. The next native run proved owned HTTP readiness, visible live localhost updates, Windows CLI browser inspection and immediate Take over revocation. The helper incorrectly attempted to release the already-revoked lease; it now asserts the exact unavailable-lease rejection and proceeds without a second release. Fixture processes were verified against exact executable, script, start time and hashes during cleanup. The ongoing next pass must still prove the remaining Preview behavior. User, CLI and bundled skill guidance now explicitly describe the supported continuation boundary and its safe recovery path.

Preview attempt seven passed native selection with screenshot upload into an unsent draft, owned restart/stop, foreign-port preservation and isolated HTML relative assets/renewal/traversal checks. It then exposed a product defect: native tab persistence and synchronization discarded the renderer-owned HTML source, disabling ongoing asset refresh. Both server paths now preserve the validated source, while explicit renderer removal remains removed. All eighteen real-database browser-service cases and the server typecheck pass. The new cases cover native updates, reconnect generations, create-completion races and intentional source removal. The harness now identifies the actual BrowserWindow renderer rather than any same-origin preview webContents. Final formatting passes for 258 changed code files. A fresh package and native recovery pass remain required before completion.

Package 5 passed all fifteen production tasks in 5m34s. Its source digest is 3a31c85a231ede91034785a1a0966f0c540952eeba10445694973a0dbf055f07. The unpacked output contains 8,726 files and 639,792,356 bytes, manifest digest dea2df9ea0570609187350e595edb8e1f9590006a4e922cf37a76b167f7e61fb. ARC IDE.exe is 223,073,280 bytes, Windows ProductVersion 0.42.9.0, Authenticode NotSigned, SHA-256 3ed9f1f4050f5cba0e5d62f2704238918efade41ea13f8805e905df8d9f72846. Generic electron-builder signing messages are not treated as a valid signature. This is a local unpacked build, not a published release or a replacement installer.

Preview attempt eight stopped before application checks on an inspector Promise was collected error; exact-owned-process cleanup ran and the failed evidence is retained. The synchronous startup identity probe now disables promise waiting without retrying mutations or weakening process identity. Attempt nine passed all 29 checks through Turbo over two actual packaged launches at ARC packaged smoke Δ vaUtmk. Each launch passed all eight Preview checks, admitted zero model turns and cleaned three exact-owned fixture processes without cleanup errors. The final native HTML screenshot shows the changed relative stylesheet after expired-session recovery, and the screenshot-to-draft image was visually inspected. Project, original file bytes, Context state and Agent Studio drafts survived restart; server, daemon and supervisor shutdown was verified. This is local Windows evidence, not clean-machine, installer lifecycle or framework-specific Vite/Next certification. The final provider comparison is running separately and serially.

Provider attempt five at ARC packaged smoke Δ LmANxj passed all eleven Team UI checks and the complete pricing team/solo pair. Seven actual Codex gpt-5.6-sol turns completed, including six shared-budget team calls and one baseline call. Reader handoff, both writers, deliberate check failure, one repair round, independent exact-candidate review, combined response, unchanged verifier bytes and baseline correctness all passed. The team took 579,050 ms; the solo baseline took 58,442 ms. Worker input/output/cached counters are available for five team roles. Main-response totals remain Unavailable because its retained cumulative counter epoch has no safe preceding baseline; missing data is not zero. The five observed worker totals are 1,229,348 input, 8,982 output and 1,064,832 cached-input tokens. Solo counters are 293,712 input, 1,512 output and 253,568 cached-input. Cached input is a subset, not an additional total. This asymmetric repair stress fixture is not evidence of token savings.

The subsequent HTML pair in that same attempt stopped on process loss before completion. Its reader successfully saved a report at 04:18:56.055Z and the scheduler remained running, generation zero and error-free at 04:18:56.200Z; fetch failed less than a second later. Server/daemon logs and the bounded Windows Application error query do not establish a crash cause. Electron recorded Waiting for the debugger to disconnect. Cancellation could not be confirmed, and the failed run is left unresumed. No owned ARC executable processes remained. A separate fresh five-turn HTML-only acceptance is running with passive main-process/window exit diagnostics; the passing pricing pair is not repeated or relabeled as a complete overall attempt.

The fresh HTML-only attempt six at ARC packaged smoke Δ Znu4MI passed all fifteen packaged checks over two launches in 8m17s. Five actual Codex gpt-5.6-sol turns completed: reader, builder, independent reviewer, counted main response and one solo baseline. Both candidates passed real native Preview Tab, Enter, Space and Escape checks. The initial check failed as intended, the builder corrected it, independent review approved the exact candidate, verifier bytes stayed unchanged, and restart retained the exact completed run and source without new model calls. Both application exits and owned server/daemon/supervisor cleanup were verified; passive lifecycle records show exit code zero. This separate pass does not establish the cause of the earlier process loss.

The HTML team took 283,200 ms and the solo baseline took 44,829 ms. Three observed team workers reported 616,375 input, 4,603 output and 489,088 cached-input tokens; main-response attribution remains Unavailable for the documented cumulative-counter boundary. The solo baseline reported 116,125 input, 1,096 output and 85,888 cached-input tokens. Both small diagnostic tasks used more observed tokens and time with teams. These results support correctness and coordination evidence, not efficiency or savings claims.

Final delivery includes the user guide and a curated local verification report with source/package manifests, focused checks, native screenshots, failed-attempt diagnostics and measured provider comparisons. The guarded integration verifies original HEAD/index and all baseline source hashes before copying; its applied status and post-copy verification are recorded separately in integration-result.json and integrated-verification.json. No source push, release, replacement installer or public publication is authorized. The existing installer and installed ARC profile are outside this feature build.

## Team builder model identity and appearance — 2026-09-16

The builder now exposes team name and color in its toolbar and model/provider identity on both member cards and assigned Workflow stages. Clicking a model opens that member's settings on the same page. Model selection is staged until Apply model; opening or cancelling the picker does not change the draft. Use agent default removes only the team-specific override. Save, publish, reload and undo use the existing versioned definition. Shared agent revisions and active run snapshots remain unchanged. Cosmetic color edits preserve legacy schema versions and operational hashes; model overrides are operational changes and participate in the existing review/admission protections. Runtime application covers normal, addressed and rule-update admission. Existing SDK/plugin RPC and `bb arc teams rpc` accept the same validated fields, documented in the user guide, configuration reference and discoverable CLI skill. There is no new daemon wire format or permission-mode override.

Focused verification uses serial Turbo tasks. The initial runtime slice passed 104 of 105 tests; the remaining new rule-update fixture incorrectly inspected the outer preview envelope. Its corrected thirty-case suite passes. Final renderer checks pass seventeen cases, followed by nine builder cases including the added legacy color/hash regression. Organization tests passed eight cases in the initial UI slice. ARC/desktop typechecks passed five tasks after removing an unsupported shared Button property. Logs are retained under `C:\Users\Collin Chen\AppData\Local\Temp\arc-visual-teams-20260915` as `team-model-*.log`; overlapping suites are not aggregated as distinct tests.

The first package command was deliberately cancelled to correct the cosmetic schema-version behavior before delivery. The second production package passed all fifteen Turbo tasks in 6m41s, from `team-model-package-2-source.json` (6,394 files; source digest `56ad92aef5673506e21b58155748b71d1104d972a6af467bed76b7c2ffc79bbc`). This remains a local unpacked Windows 0.42.9 build. Subsequent edits affect only verification helpers and documentation.

Native verification failures remain recorded rather than counted as passes: the initial isolated profile lacked Codex catalogue access; several attempts exposed missing native window focus in the test driver; others incorrectly assumed every model list has a search box and that selecting a model closes the combined model/reasoning picker. The helper now restores/shows and verifies focus on its owned window, waits for stable reachable controls, accepts the actual short-list UI and closes the picker with Escape before Apply. This follows Electron's documented [native input focus requirement](https://www.electronjs.org/docs/latest/api/web-contents#contentssendinputeventinputevent). Its optional `--with-model-picker` mode references the existing Codex executable and home in place, without copying credentials or dispatching a model turn. The default helper still supports provider-free checks.

Fresh native attempt eight at `team-customization/ARC packaged smoke Δ 6jSE79` passed all 24 checks over two launches in 1m33s. The real model picker selected Astra; the canvas displayed its provider logo and exact model ID; the renamed, recolored draft saved, published and survived reload and application restart. The pinned reusable agent stayed unchanged. Keyboard/drag building, skills, linked Workflow selection, unsent agent/team recipients and owned process cleanup also passed. The actual `team-name-color-model.png` screenshot was visually inspected. The color-input value was changed through DOM input/change events; the Windows color-picker dialog itself was not exercised. This UI-only run made no provider calls and is local package verification, not installer or clean-machine certification.

The final ARC/desktop typecheck passed all five tasks. `team-model-final-package.json` records all 8,727 output files (639,809,715 bytes), with SHA-256 manifest digest `b51c6fc73b3d7da9d863ed4e9cb07ccc62af75aa329aaf854a9a2160f429d4e4`. Executable signature status and hash are retained separately in `team-customization/package-signature.json`. The final source manifest and guarded copy/post-copy results are retained as `team-model-final-source.json`, `team-customization/integration-result.json` and `team-customization/integrated-verification.json`.

Scope remains the approved builder workflow and this follow-up customization request. Publishing, installed-app replacement, unattended work and automatic promotion remain outside this change. The original checkout's unrelated line-ending-only test edit is preserved by the guarded integration baseline.

## MVP hardening — September 17, 2026

Implementation is isolated in `B:\arc-mvp-hardening-20260916`, branch `codex/mvp-hardening`. The healthy clone retains ARC-IDE commit `4e6db376246f7f4ba43d5d156adee15b27076fcc` and imports the reviewed source in local commit `ba744e0`. The byte-verified import contains 6,386 source files, retains eight intended deletions and excludes eight temporary SDK runtime-build files. High-confidence credential-pattern scanning found no matches in the imported text source. The original checkout, index and broken history are unchanged. Evidence is under `C:\Users\Collin Chen\AppData\Local\Temp\arc-mvp-hardening-20260916`; `source-import.json` records the baseline manifest and identity. Offline frozen-lockfile installation completed successfully using serial child builds.

Current work addresses Preview isolation/assets/status, restricted composition authorization and release provenance. The alias investigation required protocol 191 so older hosts cannot silently omit the new generic read-path policy. No public Preview SDK/CLI shape changed. Native security acceptance uses owned sentinels, roots and browser tabs, with no provider dispatch. Focused and full tests, packaged regression, actual installed-payload verification and final installer delivery remain pending at this checkpoint. No source push, tag or release is authorized.

Serial verification passed 357 Workflows tests; the integrated six-package typecheck completed all eleven Turbo tasks. The corrected server boundary pass has 51 passing cases, and focused daemon contracts have 42. Four real filesystem policy cases pass, including canonical hidden and outside-root junction denial; Windows file symlink creation returns EPERM, so that separate case records an explicit privilege skip. Six renderer tests verify polling from stopped/disconnected/error states and refocus/reconnect behavior. Sixteen release tests reject mismatched installer/payload/source/feed/receipt/log data and exercise valid finalization. These component results do not establish packaged or installed acceptance.

Initial failures and their corrections remain in the evidence directory: missing prompt fixture fields and a continuation union narrowing error; stale protocol expectation; Windows-local joining of POSIX project paths; missing runtime fixture migrations and stored allowed-team revisions. The bounded-delegation case completed in 8.3 seconds on the first hardening pass, with phase timings and cleanup assertions; full-suite and repeated verification remain required. SDK declaration generation now honors the gate's MAX_JOBS=1 setting and waits for worker exit. This fixes an internal concurrency gap without changing production execution budgets.

The fresh delegated candidate reviewer failed with a content-risk flag and returned no review. A separate parent read-only pass inspected raw-response callers, conditional headers, lease/CORS ordering, encoded and canonical paths, request schemas, provenance construction, reconciliation and continuation. Changed restricted compositions with missing legacy provenance now fail closed with a fresh-conversation recovery message; unchanged legacy recipients and unrestricted continuation preserve their existing behavior. The independent-review failure is retained as a limitation, not represented as a passing review. Final native reproduction and installer evidence will be recorded alongside the frozen artifacts so documentation edits do not invalidate their source identity.

The corrected focused ARC pass has 127 passing cases across composition authorization, admission, continuation/restart, instruction successors and directory execution. Delegation completed again in 8.4 seconds with cleanup assertions intact. The remaining release boundary is the shared full source gate, then fresh packaging, credential-free native regressions and guarded installed-payload lifecycle verification. Publication remains paused.
