# Screenshot provenance

Captured on September 17, 2026 from the actual Windows ARC 0.42.10 application built from source commit `32ded9637edca58315157a3496a72c02767980ee`.

The screenshots use an isolated, test-owned profile and editable example data in an **ARC Studio** project. Team configuration and canvas changes used ARC's native controls; the saved agent definition used ARC's API. Screenshots are direct Electron renderer captures, with their original pixels and app styling preserved. Each image was visually inspected for readability and private information.

| Image              | What it shows                                                                                                                                                     |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `team-builder.png` | Product Studio, its purple team color, Project lead and Research agents, a reusable Builder, a Reports to connection, and GPT-6 Astra with the Codex icon.        |
| `workflow.png`     | The same team's linked Workflow view, with a research assignment connected to a required approval stage. The changed canvas layout is saved as an editable draft. |
| `composer.png`     | A new thread with the centered ARC logo and composer, using the actual Astra model selection.                                                                     |
| `agent-studio.png` | An editable Interface builder agent with saved instructions, beside an unsent agent-building assistant request.                                                   |

Team and Workflow captures are 1760 × 1050; composer and Agent Studio captures are 1440 × 960. The capture session reported zero threads, including hidden and archived threads, and zero assistant sessions. No provider turns were dispatched. These are example configurations, not completed agent-run results.
